import test from 'node:test';import assert from 'node:assert/strict';import {buildProjectIndex,indexedChildren} from '../public/src/model-index.js';test('deep ownership indexing does not recurse through containment',()=>{const elements=[];for(let i=0;i<100000;i++)elements.push({id:`e${i}`,ownerId:i?`e${i-1}`:'root'});const p={root:{id:'root'},elements,relationships:[],diagrams:[]};buildProjectIndex(p);assert.equal(indexedChildren(p,'e99998')[0]?.id,'e99999')});
