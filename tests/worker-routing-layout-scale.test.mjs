import test from 'node:test';import assert from 'node:assert/strict';import {routeDiagramPayload,workerRouteSegmentsClear} from '../public/src/worker-routing-core.js';import {layoutDiagramPayload,layoutHasOverlaps} from '../public/src/worker-layout-core.js';
test('worker routing avoids intervening diagram elements',()=>{const payload={diagramId:'d',nodes:[{id:'a',x:0,y:0,width:100,height:60},{id:'blocker',x:220,y:-30,width:140,height:120},{id:'b',x:520,y:0,width:100,height:60}],edges:[{id:'e',sourceNodeId:'a',targetNodeId:'b'}],options:{padding:20}};const result=routeDiagramPayload(payload);assert.equal(result.stats.failures,0);assert.equal(workerRouteSegmentsClear(payload,result),true)});
test('worker routing handles dense high-count diagrams without recursive traversal',()=>{const nodes=Array.from({length:20000},(_,i)=>({id:`n${i}`,x:(i%200)*180,y:Math.floor(i/200)*120,width:100,height:60})),edges=Array.from({length:40000},(_,i)=>({id:`e${i}`,sourceNodeId:`n${i%nodes.length}`,targetNodeId:`n${(i+201)%nodes.length}`}));const result=routeDiagramPayload({diagramId:'massive',nodes,edges,options:{cellSize:240,padding:12}});assert.equal(result.edges.length,40000);assert.ok(result.stats.failures>=0)});
test('worker layout keeps large presentation sets non-overlapping',()=>{const nodes=Array.from({length:100000},(_,i)=>({id:`n${i}`,x:0,y:0,width:100+(i%3)*20,height:60+(i%2)*20}));const result=layoutDiagramPayload({diagramId:'massive',nodes,options:{maxColumns:400,gapX:40,gapY:40}});assert.equal(result.nodes.length,100000);assert.equal(layoutHasOverlaps(result),false)});
