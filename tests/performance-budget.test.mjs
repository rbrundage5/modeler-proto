import test from 'node:test';import assert from 'node:assert/strict';import {performanceBudget,withinBudget} from '../public/src/performance-budget.js';test('massive model performance budgets are explicit',()=>{assert.equal(performanceBudget('interactiveTaskMs'),50);assert.equal(withinBudget('repositoryDomRows',199),true);assert.equal(withinBudget('repositoryDomRows',201),false)});
