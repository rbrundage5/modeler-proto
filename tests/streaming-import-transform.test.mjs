import test from 'node:test';import assert from 'node:assert/strict';import {transformAppSource,renderTransformContract} from '../public/src/app-source-transform.js';
const source="function renderCanvas(){}function diagramHeaderLabel(){}function drawSelectionBounds(svg,d){}async function createAt(){}function routeAll(){}$('workbookInput').onchange=async e=>{const f=e.target.files[0];if(!f)return;const before=JSON.stringify(project);try{await importWorkbook(f,project,log,{strict:true});const issues=validate(project),errors=issues.filter(x=>x.severity==='error');if(errors.length&&!confirm(`Import has ${errors.length} errors. Keep staged import?`)){project=JSON.parse(before);render();return}commitLocal({type:'bulk-import',project:structuredClone(project)},`Imported ${f.name}`)}catch(err){project=JSON.parse(before);log(err.message,'error')}}";
test('live import transform removes whole-project import snapshots',()=>{const transformed=transformAppSource(source);assert.match(transformed,/importWorkbookStreaming/);assert.doesNotMatch(transformed,/const before=JSON\.stringify\(project\)/);assert.doesNotMatch(transformed,/type:'bulk-import',project:structuredClone\(project\)/);const contract=renderTransformContract();assert.equal(contract.workerWorkbookParsing,true);assert.equal(contract.preImportProjectSnapshot,false)});
