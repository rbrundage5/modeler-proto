import test from 'node:test';import assert from 'node:assert/strict';import {transformAppSource,renderTransformContract} from '../public/src/app-source-transform.js';
test('app transform replaces full-array marquee and multi-selection scans',()=>{const source=`function renderCanvas(){const d=activeDiagram();for(const n of d.nodes)drawNode(svg,d,n)}\nfunction diagramHeaderLabel(){}\nfunction attachCanvasSelection(svg,d){for(const id of nodesInSelectionBox(d.nodes,m.start,m.current,{intersect:ev.altKey}))selectedNodeIds.add(id)}\nfunction drawSelectionBounds(svg,d){if(marquee){}const nodes=d.nodes.filter(n=>selectedNodeIds.has(n.id));if(nodes.length>1){}}\nasync function createAt(){}`;const out=transformAppSource(source);assert.match(out,/indexedNodesInSelectionBox\(d/);assert.match(out,/indexedSelectionBounds\(d,selectedNodeIds\)/);assert.doesNotMatch(out,/nodesInSelectionBox\(d\.nodes/);assert.doesNotMatch(out,/d\.nodes\.filter\(n=>selectedNodeIds/);assert.equal(renderTransformContract().marqueeIndexed,true)});
