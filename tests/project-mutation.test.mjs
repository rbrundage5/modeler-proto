import test from 'node:test';import assert from 'node:assert/strict';import {buildProjectIndex,indexedElement,indexedRelationship,indexedChildren} from '../public/src/model-index.js';import {addElement,moveElement,removeElement,addRelationship,removeRelationship} from '../public/src/project-mutation.js';
test('canonical mutations update arrays and indexes together',()=>{const p={root:{id:'root'},elements:[],relationships:[],diagrams:[]};buildProjectIndex(p);const e={id:'e',ownerId:'root'};addElement(p,e);assert.equal(indexedElement(p,'e'),e);moveElement(p,e,'pkg');assert.deepEqual(indexedChildren(p,'pkg'),[e]);const r={id:'r',sourceId:'e',targetId:'root'};addRelationship(p,r);assert.equal(indexedRelationship(p,'r'),r);removeRelationship(p,r);assert.equal(indexedRelationship(p,'r'),null);removeElement(p,e);assert.equal(indexedElement(p,'e'),null)});
