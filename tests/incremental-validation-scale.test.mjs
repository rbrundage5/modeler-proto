import test from 'node:test';import assert from 'node:assert/strict';import {buildProjectIndex} from '../public/src/model-index.js';import {validateDirtyProject} from '../public/src/incremental-validation.js';
test('dirty validation stays bounded in a million-record repository',()=>{const count=1_000_000,project={root:{id:'root',name:'Root',kind:'Model'},elements:Array.from({length:count},(_,i)=>({id:`e${i}`,name:`E${i}`,kind:'Block',ownerId:'root'})),relationships:[],diagrams:[]};buildProjectIndex(project);project.elements[777777].name='';const started=performance.now(),result=validateDirtyProject(project,{all:false,elements:['e777777'],relationships:[],diagrams:[],owners:[]}),elapsed=performance.now()-started;assert.equal(result.counts.elements,1);assert.ok(result.issues.some(issue=>issue.code==='NAME_REQUIRED'));assert.ok(elapsed<3000)});
