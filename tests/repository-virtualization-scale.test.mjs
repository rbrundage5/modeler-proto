import test from 'node:test';import assert from 'node:assert/strict';import {buildProjectIndex} from '../public/src/model-index.js';import {RepositoryViewport} from '../public/src/repository-viewport.js';

test('million-element repository renders only a bounded viewport window',()=>{const count=1_000_000,project={root:{id:'root',name:'Root',kind:'Model'},elements:Array.from({length:count},(_,i)=>({id:`e${i}`,name:`Element ${String(i).padStart(7,'0')}`,kind:'Block',ownerId:'root'})),relationships:[],diagrams:[]};buildProjectIndex(project);const viewport=new RepositoryViewport(project,{rowHeight:28,overscan:8,maxRows:120});const view=viewport.window({scrollTop:27_000_000,viewportHeight:700});assert.equal(view.totalRows,count+1);assert.ok(view.rendered<=120);assert.ok(view.rendered>0);assert.equal(view.rows.length,view.rendered)});

test('collapsed subtrees are excluded without recursive DOM construction',()=>{const project={root:{id:'root',name:'Root',kind:'Model'},elements:[{id:'a',name:'A',kind:'Package',ownerId:'root'},{id:'a1',name:'A1',kind:'Block',ownerId:'a'},{id:'b',name:'B',kind:'Package',ownerId:'root'}],relationships:[],diagrams:[]};buildProjectIndex(project);const viewport=new RepositoryViewport(project,{collapsed:new Set(['a'])});const view=viewport.window({viewportHeight:500});assert.deepEqual(view.rows.map(row=>row.id),['root','a','b']);assert.equal(view.totalRows,3)});

test('selected row reveal computes scroll position without rendering preceding rows',()=>{const project={root:{id:'root',name:'Root',kind:'Model'},elements:Array.from({length:10000},(_,i)=>({id:`e${i}`,name:`E${String(i).padStart(5,'0')}`,kind:'Block',ownerId:'root'})),relationships:[],diagrams:[]};buildProjectIndex(project);const viewport=new RepositoryViewport(project,{rowHeight:30});const top=viewport.reveal('e9000',{scrollTop:0,viewportHeight:600});assert.ok(top>250000)});
