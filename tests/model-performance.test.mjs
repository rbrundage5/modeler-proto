import test from 'node:test';import assert from 'node:assert/strict';import {buildProjectIndex} from '../public/src/model-index.js';import {performanceDiagnostics} from '../public/src/model-performance.js';test('performance diagnostics distinguish foundations from remaining integration',()=>{const p={root:{id:'root'},elements:[],relationships:[],diagrams:[]};buildProjectIndex(p);const d=performanceDiagnostics(p);assert.equal(d.indexedLookup,true);assert.ok(d.remaining.includes('integrate repository virtualization'))});
