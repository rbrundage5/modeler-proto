import test from 'node:test';import assert from 'node:assert/strict';import {transformAppSource,renderTransformContract} from '../public/src/app-source-transform.js';
test('live commit path uses incremental coordinator with strict fallback',()=>{const source="function renderCanvas(){}function diagramHeaderLabel(){}function routeAll(){}function commitLocal(op,msg){synchronizeSemanticModel(project);normalizeIBDProject(project);refreshQualifiedNames(project);project.revision=(project.revision||0)+1;save();render();collab.publish(op);log(msg,'ok')}function drawSelectionBounds(svg,d){}async function createAt(){}$('workbookInput').onchange=async e=>{const f=e.target.files[0];if(!f)return;const before=JSON.stringify(project);try{await importWorkbook(f,project,log,{strict:true});const issues=validate(project),errors=issues.filter(x=>x.severity==='error');if(errors.length&&!confirm(`Import has ${errors.length} errors. Keep staged import?`)){project=JSON.parse(before);render();return}commitLocal({type:'bulk-import',project:structuredClone(project)},`Imported ${f.name}`)}catch(err){project=JSON.parse(before);log(err.message,'error')}}";const out=transformAppSource(source);assert.match(out,/processIncrementalCommit\(project,op\)/);assert.match(out,/if\(processing\.mode==='full-required'\)/);assert.equal(renderTransformContract().incrementalInteractiveCommit,true)});
