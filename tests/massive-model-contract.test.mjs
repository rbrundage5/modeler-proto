import test from 'node:test';import assert from 'node:assert/strict';import {MASSIVE_MODEL_CONTRACT as c} from '../public/src/massive-model-contract.js';test('massive model contract includes one-million-record qualification without hard cutoff',()=>{assert.ok(c.semanticRecordQualification.includes(1000000));assert.equal(c.arbitraryHardLimit,false);assert.equal(c.correctnessMayDegradeForScale,false)});
