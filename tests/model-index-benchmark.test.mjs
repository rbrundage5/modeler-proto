import test from 'node:test';import assert from 'node:assert/strict';import {benchmarkIndexedLookups} from '../public/src/model-index-benchmark.js';test('benchmark exercises indexed lookup paths',()=>{let t=0;const p={root:{id:'root'},elements:Array.from({length:1000},(_,i)=>({id:`e${i}`})),relationships:Array.from({length:1000},(_,i)=>({id:`r${i}`})),diagrams:[]},r=benchmarkIndexedLookups(p,{iterations:5000,clock:()=>t++});assert.equal(r.hits,10000);assert.equal(r.durationMs,1)});
