import test from 'node:test';import assert from 'node:assert/strict';import {buildProjectIndex} from '../public/src/model-index.js';import {updateElement} from '../public/src/project-mutation.js';import {normalizeDirtyProject} from '../public/src/incremental-normalization.js';

test('rename refreshes only affected qualified-name subtree',()=>{const project={root:{id:'root',name:'Root'},elements:[{id:'a',name:'A',ownerId:'root'},{id:'b',name:'B',ownerId:'a'},{id:'x',name:'X',ownerId:'root'}],relationships:[],diagrams:[]};buildProjectIndex(project);updateElement(project,project.elements[0],{name:'Renamed'});const result=normalizeDirtyProject(project);assert.equal(result.mode,'incremental');assert.equal(project.elements[0].qualifiedNameString,'Root::Renamed');assert.equal(project.elements[1].qualifiedNameString,'Root::Renamed::B');assert.ok(result.qualifiedNameCount<4)});
