import test from 'node:test';import assert from 'node:assert/strict';import {buildProjectIndex} from '../public/src/model-index.js';import {assertIndexIntegrity,indexIntegrityIssues} from '../public/src/index-integrity.js';test('index integrity detects unindexed direct mutations',()=>{const p={root:{id:'root'},elements:[],relationships:[],diagrams:[]};buildProjectIndex(p);assert.equal(assertIndexIntegrity(p),true);p.elements.push({id:'unindexed'});assert.deepEqual(indexIntegrityIssues(p),['ELEMENT_INDEX_COUNT_MISMATCH'])});
