export function layoutDiagramPayload(payload){const nodes=(payload?.nodes||[]).map(node=>({...node})),options=payload?.options||{},gapX=Math.max(24,options.gapX||80),gapY=Math.max(24,options.gapY||60),maxColumns=Math.max(1,options.maxColumns||Math.ceil(Math.sqrt(Math.max(1,nodes.length))));let x=options.originX||80,y=options.originY||80,rowHeight=0,column=0;for(const node of nodes){const width=Math.max(20,Number(node.width)||120),height=Math.max(20,Number(node.height)||70);node.x=x;node.y=y;rowHeight=Math.max(rowHeight,height);column++;if(column>=maxColumns){column=0;x=options.originX||80;y+=rowHeight+gapY;rowHeight=0}else x+=width+gapX}return{diagramId:payload?.diagramId||null,nodes,stats:{nodes:nodes.length,columns:maxColumns}}}
export function layoutHasOverlaps(result){const nodes=result?.nodes||[];for(let i=0;i<nodes.length;i++)for(let j=i+1;j<nodes.length;j++){const a=nodes[i],b=nodes[j];if(a.x<a.x+b.width&&a.x+a.width>b.x&&a.y<a.y+b.height&&a.y+a.height>b.y)return true}return false}
