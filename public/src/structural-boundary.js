import {attachPortPresentation,inferPerimeterAttachment,synchronizeIBDPresentations} from './ibd-engine.js';import {findElement} from './model.js';
export function movePortOnBoundary(project,diagram,portNode,ownerNode,point){const port=findElement(project,portNode?.elementId),owner=findElement(project,port?.ownerId);if(!['ProxyPort','FullPort'].includes(port?.kind))throw Error('Presentation does not reference a supported port kind.');const graphicalOwner=ownerNode.isContextBoundary?findElement(project,diagram.contextId):findElement(project,ownerNode.elementId);if(owner?.id!==graphicalOwner?.id&&!['PartProperty','ReferenceProperty'].includes(graphicalOwner?.kind))throw Error('Port semantic owner does not match the selected boundary context.');const attachment=inferPerimeterAttachment(ownerNode,{x:point.x-portNode.width/2,y:point.y-portNode.height/2,width:portNode.width,height:portNode.height});return attachPortPresentation(portNode,ownerNode,{...attachment,endpointPath:portNode.endpointPath})}
export function resizeStructuralOwner(project,diagram,ownerNode,size){ownerNode.width=Math.max(80,size.width);ownerNode.height=Math.max(60,size.height);synchronizeIBDPresentations(project,diagram);return ownerNode}
export function portBoundaryOperation(diagramId,portNode){return{type:'set-port-placement',diagramId,nodeId:portNode.id,boundaryOwnerNodeId:portNode.boundaryOwnerNodeId,side:portNode.portSide,offset:portNode.perimeterOffset,endpointPath:[...(portNode.endpointPath||[])]}}
