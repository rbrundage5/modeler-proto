import {findElement} from './model.js';import {validTypeKinds} from './semantic-editor.js';
export const REFERENCE_PROPERTY_SCHEMA=Object.freeze({editable:['name','externalId','documentation','typeRef','multiplicity','visibility','defaultValue','isReadOnly','isOrdered','isUnique'],readOnly:['id','ownerId','qualifiedName','semanticRole','composition'],multiline:['documentation'],referenceFields:{typeRef:'compatible classifier selector'},commitPolicy:{externalId:'blur-or-confirm',name:'blur',documentation:'blur'}});
export function validateMultiplicity(value='1'){return /^(?:\d+|\*)(?:\.\.(?:\d+|\*))?$/.test(String(value).trim())}
export function validateReferenceProperty(project,element){const issues=[],owner=findElement(project,element.ownerId),type=findElement(project,element.typeRef);if(!['Block','AssociationBlock','SiteContext'].includes(owner?.kind))issues.push({code:'REFERENCE_OWNER_INVALID',field:'ownerId',message:'Choose a Block, AssociationBlock, or SiteContext owner.'});if(!type||!validTypeKinds('ReferenceProperty').includes(type.kind))issues.push({code:'REFERENCE_TYPE_INVALID',field:'typeRef',message:'Choose a compatible classifier using its stable identity.'});if(!validateMultiplicity(element.multiplicity))issues.push({code:'REFERENCE_MULTIPLICITY_INVALID',field:'multiplicity',message:'Use a multiplicity such as 1, 0..1, 1..*, or *.'});if(['composite','shared'].includes(element.aggregation||element.composition))issues.push({code:'REFERENCE_AGGREGATION_INVALID',field:'aggregation',message:'A ReferenceProperty is non-owning; use no aggregation.'});return issues}
