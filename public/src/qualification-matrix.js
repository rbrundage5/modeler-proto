import {SUPPORTED_TYPE_INVENTORY} from './supported-type-inventory.js';
export const QUALIFICATION_STATES=Object.freeze(['qualified','automated-unverified','manual-only','blocked','failed','unsupported']);
const complete=SUPPORTED_TYPE_INVENTORY.filter(item=>item.supportStatus==='complete');
export const QUALIFICATION_MATRIX=Object.freeze(complete.flatMap(item=>item.diagramTypes.map(diagramType=>Object.freeze({
 semanticType:item.canonicalType,diagramType,creationMethod:item.creationWorkflow,renderer:item.rendererKey,propertyEditor:item.propertySchema,supportedRelationships:item.relationshipRoles,persistencePath:item.serialization,importReimportPath:item.importMapping,undoRedoPath:item.undoRedo,deterministicTests:item.testCoverage,browserTests:item.canonicalType==='Requirement'?['browser-tests/structural-requirements.spec.mjs','browser-tests/visual-accessibility.spec.mjs']:item.canonicalType==='Lifeline'?['browser-tests/behavior-workflows.spec.mjs']:item.canonicalType==='ReferenceProperty'?['browser-tests/structural-requirements.spec.mjs']:[],manualVisualStatus:item.canonicalType==='Requirement'?'passed-representative-review':'not-executed',qualificationStatus:['Requirement','Lifeline','ReferenceProperty'].includes(item.canonicalType)?'automated-unverified':'blocked',knownLimitation:item.canonicalType==='Requirement'?'Representative rendering reviewed; complete editing matrix still requires human acceptance.':item.canonicalType==='Lifeline'?'Open-end message workflow automated; complete visual and resize review remains.':item.canonicalType==='ReferenceProperty'?'Visible IBD presentation automated; full connector and property editing review remains.':'No maintained browser workflow evidence was added for this complete inventory row.',evidenceReferences:[...item.testCoverage]
}))));
export function qualificationSummary(matrix=QUALIFICATION_MATRIX){return Object.fromEntries(QUALIFICATION_STATES.map(state=>[state,matrix.filter(item=>item.qualificationStatus===state).length]))}
