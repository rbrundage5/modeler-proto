import {modelScale} from './model-scale.js';
export function performanceDiagnostics(project){const scale=modelScale(project);return{...scale,indexedLookup:true,incrementalMutationFoundation:true,dirtySetFoundation:true,operationHistoryFoundation:true,repositoryVirtualizationFoundation:true,diagramSpatialIndexFoundation:true,cooperativeChunkFoundation:true,remaining:['integrate incremental normalizers','replace snapshot undo in editor','integrate repository virtualization','integrate diagram viewport virtualization','chunk IndexedDB records','worker streaming import and validation']}}
