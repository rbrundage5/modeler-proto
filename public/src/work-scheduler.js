export class WorkScheduler{constructor({budgetMs=8,clock=()=>performance.now(),yieldControl=()=>new Promise(r=>setTimeout(r,0))}={}){this.budgetMs=budgetMs;this.clock=clock;this.yieldControl=yieldControl}async run(iterator,{signal=null,onProgress=null}={}){let processed=0,start=this.clock();for(const item of iterator){if(signal?.aborted)throw new DOMException('Operation aborted','AbortError');await item();processed++;if(this.clock()-start>=this.budgetMs){onProgress?.(processed);await this.yieldControl();start=this.clock()}}return processed}}
