const clone=v=>JSON.parse(JSON.stringify(v));
export function normalizeRelationshipPresentation(edge,relationship,diagram){edge.relationshipId=relationship.id;edge.sourceNodeId=edge.sourceNodeId||'';edge.targetNodeId=edge.targetNodeId||'';edge.sourceId=relationship.sourceId;edge.targetId=relationship.targetId;edge.routingMode=edge.routingMode||'auto';edge.waypoints=(edge.waypoints||edge.points?.slice(1,-1)||[]).map((p,i)=>({id:p.id||`waypoint-${edge.id}-${i}`,x:Number(p.x),y:Number(p.y),order:i}));edge.labelPresentation=edge.labelPresentation||{id:`label-${edge.id}`,x:edge.labelX??null,y:edge.labelY??null,offsetX:edge.labelOffsetX||0,offsetY:edge.labelOffsetY||0};edge.resolutionStatus=edge.sourceNodeId&&edge.targetNodeId?'resolved':'unresolved';edge.diagramId=diagram.id;return edge}
export function validateRelationshipPresentation(project,diagram,edge){const relationship=(project.relationships||[]).find(r=>r.id===edge.relationshipId),issues=[];if(!relationship)return[{code:'RELATIONSHIP_UNRESOLVED',message:'Resolve the semantic relationship identity.'}];for(const side of['source','target']){if(!(project.elements||[]).some(e=>e.id===relationship[`${side}Id`]))issues.push({code:`${side.toUpperCase()}_ENDPOINT_UNRESOLVED`,message:`Resolve the ${side} semantic identity.`});if(!diagram.nodes.some(n=>n.id===edge[`${side}NodeId`]))issues.push({code:`${side.toUpperCase()}_PRESENTATION_UNRESOLVED`,message:`Resolve the ${side} presentation identity.`})}if(edge.waypoints?.some(p=>!p.id||!Number.isFinite(p.x)||!Number.isFinite(p.y)))issues.push({code:'WAYPOINT_CORRUPT',message:'Waypoints require stable IDs and finite coordinates.'});return issues}
export const snapshotRelationshipPresentation=(relationship,edge)=>clone({relationship,edge});
