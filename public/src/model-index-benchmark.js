import {buildProjectIndex,indexedElement,indexedRelationship} from './model-index.js';export function benchmarkIndexedLookups(project,{iterations=10000,clock=()=>performance.now()}={}){buildProjectIndex(project);const elements=project.elements||[],relationships=project.relationships||[],start=clock();let hits=0;for(let i=0;i<iterations;i++){if(elements.length&&indexedElement(project,elements[(i*7919)%elements.length].id))hits++;if(relationships.length&&indexedRelationship(project,relationships[(i*3571)%relationships.length].id))hits++}return{iterations,hits,durationMs:clock()-start}}
