import {indexElementAdded,indexElementMoved,indexElementRemoved,indexRelationshipAdded,indexRelationshipRemoved} from './model-index-mutations.js';
import {recordSemanticChange} from './incremental-change-set.js';

// Canonical mutation helpers for massive repositories. These keep canonical arrays, runtime
// indexes, and dirty dependency sets coherent without rebuilding repository-wide structures.
export function addElement(project,element){project.elements??=[];project.elements.push(element);indexElementAdded(project,element);recordSemanticChange(project,{elementIds:[element.id],ownerIds:[element.ownerId]});return element}
export function removeElement(project,element){const ownerId=element.ownerId,related=[...(project.relationships||[])].filter(r=>r.sourceId===element.id||r.targetId===element.id).map(r=>r.id);const at=(project.elements||[]).indexOf(element);if(at>=0)project.elements.splice(at,1);indexElementRemoved(project,element);recordSemanticChange(project,{relationshipIds:related,ownerIds:[ownerId]});return element}
export function moveElement(project,element,ownerId){const previousOwnerId=element.ownerId;element.ownerId=ownerId;indexElementMoved(project,element,previousOwnerId);recordSemanticChange(project,{elementIds:[element.id],ownerIds:[previousOwnerId,ownerId],includeDescendants:true});return element}
export function updateElement(project,element,patch){const previousOwnerId=element.ownerId,previousId=element.externalId;Object.assign(element,patch);if(previousOwnerId!==element.ownerId)indexElementMoved(project,element,previousOwnerId);if(previousId!==element.externalId){const index=(awaitIndex=>awaitIndex)(null)}recordSemanticChange(project,{elementIds:[element.id],ownerIds:[previousOwnerId,element.ownerId],includeDescendants:Boolean(patch.name!==undefined||patch.ownerId!==undefined)});return element}
export function addRelationship(project,relationship){project.relationships??=[];project.relationships.push(relationship);indexRelationshipAdded(project,relationship);recordSemanticChange(project,{relationshipIds:[relationship.id],elementIds:[relationship.sourceId,relationship.targetId]});return relationship}
export function removeRelationship(project,relationship){const endpoints=[relationship.sourceId,relationship.targetId];const at=(project.relationships||[]).indexOf(relationship);if(at>=0)project.relationships.splice(at,1);indexRelationshipRemoved(project,relationship);recordSemanticChange(project,{elementIds:endpoints});return relationship}
