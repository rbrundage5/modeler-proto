import {endpointHandles} from './relationship-endpoint-handles.js';import {addWaypoint,moveWaypoint,removeWaypoint,resetRelationshipLabel,resetRouting,labelAnchorForPoint,labelPointForAnchor} from './relationship-geometry.js';
import {presentExistingRelationship} from './relationship-persistence.js';
import {deleteSemanticRelationship} from './relationship-deletion.js';
import {DIAGRAM_TYPES,uid,createProject,allElements,findElement,findRelationship,qualifiedName,defaultElement,defaultRelationship,relationshipStyle,normalizeProject,refreshQualifiedNames} from './model.js';
import {isTypedFeature,supportsDirection,supportsDefault,supportsConjugation,typeOptions,validTypeKinds,setMultiplicity,semanticIssues} from './semantic-editor.js';
import {sysmlIcon,sysmlPaletteIcon} from './sysml-icons.js';
import {allowed,RELATIONSHIPS,DIAGRAMS,ELEMENTS,endpointAllowed} from './sysml-profile.js';import {supportsCompartments,ensureCompartmentState,visibleCompartments,formatFeature,minimumNodeHeight,compartmentAddKind,COMPARTMENT_DEFINITIONS} from './notation.js';import {importWorkbook} from './importer.js';import {applyValidationQuickFix,validate,validationQuickFix} from './validator.js';import {CollaborationClient} from './collaboration.js';
import {centerOf,boundaryPoint as connectorBoundaryPoint,orthogonalRoute,pathData,nearestSegmentIndex,midpointAlong} from './connector-engine.js';
import {routeDiagramRelationships} from './professional-diagram-layout.js';
import {synchronizeSemanticModel} from './semantic-core.js';
import {RESIZE_HANDLES,resizePresentation,presentationMinimum} from './presentation-layout.js';
import {isIBD,ibdContext,availableIBDProperties,availableNestedProperties,availableIBDPorts,createIBDPropertyPresentation,createNestedPropertyPresentation,createIBDPortPresentation,attachPortPresentation,synchronizeIBDPresentations,configureIBDConnector,endpointDescriptor,resolveIBDEndpoint,validIBDConnector,applyConnectorEndpoints,createItemFlow,itemFlowsForConnector,normalizeIBDProject,presentationDescendants,visibleIBDNodes,inferPerimeterAttachment,perimeterPosition} from './ibd-engine.js';
import {enumeratePropertyPaths,formatPropertyPath,normalizePropertyPath,propertiesOfClassifier,resolvePropertyPath,validatePropertyPath} from './property-path.js';
import {comparePorts,effectivePortFlows} from './interface-compatibility.js';
import {collaborationLink,collaborationSettings,loadCollaborationSettings,saveCollaborationSettings} from './collaboration-settings.js';
import {REQUIREMENT_TYPES,canMoveRequirement,moveRequirement,requirementBreadcrumb,requirementChildren,requirementPolicy} from './requirements.js';
import {applyOperation} from './operations.js';
import {foundationIssues,reorderOwnedFoundation} from './bdd-foundations.js';
import {createBddRelationship,reconnectBddRelationship,bddRelationshipIssues,E01S5_RELATIONSHIPS} from './bdd-relationships.js';
import {reconnectConnectorEnd} from './connector-ends.js';
import {ibdManifestEntry} from './e01i-ibd-manifest.js';
import {ConflictResolutionDialog} from './collaboration-conflict-ui.js';
import {withManualValue} from './collaboration-conflicts.js';
import {CollaborationPresenceView,presenceColor,throttleAwareness} from './collaboration-presence.js';
import {WorkspacePanels} from './workspace-panels.js';
import {diagramPanPosition,isDiagramPanGesture} from './diagram-pan.js';
import {canShowElementOnDiagram,defaultPresentationSize,diagramPointFromClient,edgeAutoPanDelta,findPresentation,showElementOnDiagram} from './diagram-presentations.js';
import {resolvePresentation} from './presentation-compatibility.js';
import {presentationCapabilities} from './presentation-capabilities.js';
import {lifelineGeometry,messageGeometry,messagePathData,moveMessageOccurrence,nextMessageOrder,resizeExecutionSpecification,resizeLifelineTimeline,sequenceLifelineAt} from './sequence-interactions.js';
import {nodesInSelectionBox,updateSelection} from './diagram-interactions.js';
import {rendererDescriptor} from './renderer-registry.js';
import {inventoryEntry,capabilitySummary,supportAction} from './supported-type-inventory.js';
import {selectedNotationDescriptor} from './selected-element-notation.js';
import {constraintDisplay,validateBindingEndpoints} from './parametric-notation.js';
import {classifierHeader,propertyNotation,relationshipEndLabels,requirementDisplay,traceabilityDirection} from './notation-contracts.js';
import {behaviorRelationshipLabel,INTERACTION_OPERATORS,MESSAGE_KINDS,validLifelineRepresentation,validateBehaviorConnection,resolveActivityEndpoint,reconnectActivityFlow,validateActivityReference} from './behavior-model.js';
const $=id=>document.getElementById(id);let project=normalizeProject(load()||createProject()),selected={type:null,id:null,nodeId:null,edgeId:null},tool=null,toolRecordKind=null,sourceId=null,sourceNodeId=null,zoom=1,drag=null,resize=null,undoHistory=[],future=[],view='model',locks=[],connectionState='Local',panState=null,spacePan=false,navigationBack=[],navigationForward=[],treeClipboard=null,treeContextMenu=null,draggedTreeElementId=null,selectedNodeIds=new Set(),marquee=null,edgeEdit=null,openMessageAnchor=null;
const initialCollaboration=collaborationSettings({search:location.search,stored:loadCollaborationSettings(localStorage)});
const workspacePanels=new WorkspacePanels({workspace:document.querySelector('.workspace'),menu:$('panelMenu')});$('resetPanelLayout').onclick=()=>workspacePanels.reset();
let collaborators=[],pendingCollaborationChanges=0;
function renderCollaborationPresence(){const status=$('presenceStatus');status.textContent=`${collaborators.length} users${pendingCollaborationChanges?` · ${pendingCollaborationChanges} syncing`:''}`;status.title=collaborators.map(user=>{const focus=user.awareness?.selectedId?` — editing ${findElement(project,user.awareness.selectedId)?.name||user.awareness.selectedId}`:'';return`${user.name} (${user.role||'editor'})${focus}`}).join('\n')||'No collaborators connected'}
const presenceView=new CollaborationPresenceView({panel:$('presencePanel'),canvasWrap:$('canvasWrap'),getProject:()=>project,getActiveDiagram:()=>activeDiagram(),getSelectedId:()=>selected.id,navigateDiagram:id=>navigateDiagram(id),selectElement:id=>{selected={type:'element',id,nodeId:activeDiagram()?.nodes.find(node=>node.elementId===id)?.id||null,edgeId:null};render()}});
const conflictDialog=new ConflictResolutionDialog($('conflictDialog'));
const collab=new CollaborationClient({getProject:()=>project,onProject:(p,r)=>{project=normalizeProject(p);save();render();log(`Loaded ${r}`,'ok')},onPresence:users=>{collaborators=users;renderCollaborationPresence();presenceView.update(users,{selfClientId:collab.clientId,zoom})},onPending:count=>{pendingCollaborationChanges=count;renderCollaborationPresence()},onStatus:s=>{connectionState=s;const badge=$('connectionStatus');badge.textContent=s;badge.className=`status-badge ${String(s).toLowerCase()}`;updateActionStates()},onLog:log,onConflict:showSemanticConflict,onMeta:m=>{if(m.revision!==undefined)$('revisionStatus').textContent=`r${m.revision}`;if(m.branches)renderBranches(m.branches);if(m.locks){locks=m.locks;renderProperties()}}});
function showSemanticConflict(message){project=normalizeProject(message.project);save();render();conflictDialog.onResolve=({action,reviewer,manualValue})=>{const strategy=action==='assign'?'assign-reviewer':action;try{if(strategy==='keep-mine')project=normalizeProject(applyOperation(project,message.operation));if(strategy==='manual')project=normalizeProject(applyOperation(project,withManualValue(message.operation,manualValue)));message.resolve(strategy,{reviewer,value:manualValue});const descriptions={'keep-mine':'Kept your value','keep-theirs':'Kept the room value',manual:'Applied the manually merged value',defer:'Deferred the conflict','assign-reviewer':`Assigned the conflict to ${reviewer}`};log(`${descriptions[strategy]}.`,'warn');save();render()}catch(error){log(`Conflict resolution failed: ${error.message}`,'error')}};conflictDialog.open(message,{mineLabel:collab.name,reviewers:collaborators})}
const sendAwareness=throttleAwareness(state=>collab.awareness(state));
function load(){try{return JSON.parse(localStorage.getItem('systems-modeler.v6.2'))}catch{return null}}function save(){localStorage.setItem('systems-modeler.v6.2',JSON.stringify(project))}function activeDiagram(){return project.diagrams.find(d=>d.id===project.activeDiagramId)||project.diagrams[0]}
function checkpoint(){undoHistory.push(JSON.stringify(project));if(undoHistory.length>100)undoHistory.shift();future=[]}function commitLocal(op,msg){synchronizeSemanticModel(project);normalizeIBDProject(project);refreshQualifiedNames(project);project.revision=(project.revision||0)+1;save();render();collab.publish(op);log(msg,'ok')}
function log(msg,cls=''){const d=document.createElement('div');d.className=`log-entry ${cls}`;d.textContent=`${new Date().toLocaleTimeString()} ${msg}`;$('log').prepend(d)}
function render(){renderTree();renderPalette();renderDiagramSelect();renderCanvas();renderProperties();presenceView.renderCanvas();$('revisionStatus').textContent=`r${project.revision||0}`;const d=activeDiagram();$('diagramTypeLabel').textContent=d?d.diagramType:'';updateActionStates()}
function renderBranches(bs){const s=$('branchSelect');s.replaceChildren();for(const b of bs){const o=document.createElement('option');o.value=b.id;o.textContent=`${b.name} (r${b.head_revision})`;s.append(o)}s.value=collab.branchId}
function collapsedTreeIds(){project.uiState??={};project.uiState.collapsedTreeIds??=[];return new Set(project.uiState.collapsedTreeIds)}
function setTreeCollapsed(id,collapsed){const ids=collapsedTreeIds();if(collapsed)ids.add(id);else ids.delete(id);project.uiState.collapsedTreeIds=[...ids];save()}
function renderTree(){const t=$('tree');t.replaceChildren();if(view==='diagrams'){for(const d of project.diagrams)treeRow(t,d.name,0,d.id,'diagram',`${d.diagramType}`);return}if(view==='xref'){renderXrefs(t);return}const kids=new Map();for(const e of allElements(project)){const k=e.ownerId||'ROOT';if(!kids.has(k))kids.set(k,[]);kids.get(k).push(e)}const collapsed=collapsedTreeIds();const draw=(e,depth=0)=>{const children=(kids.get(e.id)||[]).sort((a,b)=>a.kind==='Requirement'&&b.kind==='Requirement'?(a.requirementOrder??0)-(b.requirementOrder??0)||a.name.localeCompare(b.name):a.name.localeCompare(b.name));const hasChildren=children.length>0;const isCollapsed=hasChildren&&collapsed.has(e.id);treeRow(t,e.name,depth,e.id,'element',e.kind,{hasChildren,isCollapsed,onToggle:()=>{setTreeCollapsed(e.id,!isCollapsed);renderTree()}});if(!isCollapsed)for(const c of children)draw(c,depth+1)};draw(project.root)}
function treeRow(parent,name,depth,id,type,kind,options={}){const {hasChildren=false,isCollapsed=false,onToggle=null}=options;const r=document.createElement('div');r.className=`tree-row ${selected.id===id?'selected':''}`;r.style.paddingLeft=`${6+depth*16}px`;r.tabIndex=0;r.dataset.id=id;r.dataset.type=type;r.dataset.kind=kind;const disclosure=hasChildren?`<button type="button" class="tree-disclosure" aria-label="${isCollapsed?'Expand':'Collapse'} ${esc(name)}" aria-expanded="${!isCollapsed}">${isCollapsed?'▸':'▾'}</button>`:'<span class="tree-disclosure-spacer"></span>';r.innerHTML=`${disclosure}<span class="tree-kind-icon">${icon(kind)}</span><span class="tree-name">${esc(name)}</span><span class="badge">${esc(kind)}</span>`;const toggle=r.querySelector('.tree-disclosure');if(toggle)toggle.onclick=ev=>{ev.stopPropagation();onToggle?.()};r.onclick=()=>selectTreeItem(id,type);r.ondblclick=ev=>{if(ev.target.closest('.tree-disclosure'))return;if(type==='diagram')navigateDiagram(id);else if(type==='element')openChildFor(id)};r.oncontextmenu=ev=>{ev.preventDefault();selectTreeItem(id,type,false);showTreeContextMenu(ev.clientX,ev.clientY,id,type)};if(type==='element'&&id!==project.root.id){r.draggable=true;r.ondragstart=ev=>{draggedTreeElementId=id;const element=findElement(project,id),payload={semanticElementIds:[id],semanticElementId:id,semanticElementType:element.kind,semanticOwnerId:element.ownerId,source:'containment'};ev.dataTransfer.effectAllowed='copyMove';try{ev.dataTransfer.setData('application/x-sysml-containment+json',JSON.stringify(payload));ev.dataTransfer.setData('text/plain',id);ev.dataTransfer.setData('text/x-model-element-id',id)}catch{}const ghost=document.createElement('div');ghost.className='containment-drag-ghost';ghost.innerHTML=`${sysmlPaletteIcon(element.kind)}<span><strong>${esc(element.name)}</strong><small>${esc(element.kind)}</small></span>`;document.body.append(ghost);ev.dataTransfer.setDragImage(ghost,18,18);requestAnimationFrame(()=>ghost.remove());r.classList.add('dragging')};r.ondragend=()=>{draggedTreeElementId=null;r.classList.remove('dragging');document.querySelectorAll('.tree-row.drag-target').forEach(x=>x.classList.remove('drag-target'))}}if(type==='element'){r.ondragenter=ev=>{const movingId=draggedTreeElementId;if(movingId&&canMoveElement(movingId,id)){ev.preventDefault();r.classList.add('drag-target')}};r.ondragover=ev=>{const movingId=draggedTreeElementId;if(movingId&&canMoveElement(movingId,id)){ev.preventDefault();ev.dataTransfer.dropEffect='move';r.classList.add('drag-target')}};r.ondragleave=ev=>{if(!r.contains(ev.relatedTarget))r.classList.remove('drag-target')};r.ondrop=ev=>{ev.preventDefault();ev.stopPropagation();r.classList.remove('drag-target');let movingId=draggedTreeElementId;try{movingId=movingId||ev.dataTransfer.getData('text/x-model-element-id')||ev.dataTransfer.getData('text/plain')}catch{}draggedTreeElementId=null;if(movingId)moveElementTo(movingId,id)}}const remoteEditors=type==='element'?collaborators.filter(user=>user.clientId!==collab.clientId&&user.awareness?.selectedId===id):[];if(remoteEditors.length){r.classList.add('remote-active');r.style.setProperty('--remote-presence-color',presenceColor(remoteEditors[0].clientId||remoteEditors[0].name));const indicator=document.createElement('span');indicator.className='tree-presence-indicator';indicator.textContent=remoteEditors.map(user=>String(user.name||'?').slice(0,1).toUpperCase()).join('');indicator.title=remoteEditors.map(user=>`${user.name} is ${user.awareness?.typing?'typing in':'editing'} ${user.awareness?.propertyName||name}`).join('\n');r.append(indicator)}parent.append(r)}
function renderXrefs(t){if(selected.type!=='element'){t.textContent='Select an element, then open References.';return}const e=findElement(project,selected.id);treeRow(t,e.name,0,e.id,'element',e.kind);for(const r of project.relationships.filter(x=>x.sourceId===e.id||x.targetId===e.id)){const other=findElement(project,r.sourceId===e.id?r.targetId:r.sourceId);treeRow(t,`${r.kind}: ${other?.name||'missing'}`,1,other?.id||r.id,other?'element':'relationship',r.kind)}for(const d of project.diagrams.filter(d=>d.nodes.some(n=>n.elementId===e.id)))treeRow(t,`Shown on ${d.name}`,1,d.id,'diagram',d.diagramType)}
function icon(k){return sysmlIcon(k)}
function selectTreeItem(id,type,rerender=true){selected={type,id,nodeId:null,edgeId:null};if(type==='diagram'){project.activeDiagramId=id;view='model'}collab.awareness({selectedId:type==='element'?id:null,diagramId:type==='diagram'?id:project.activeDiagramId});if(rerender)render();else{renderTree();renderProperties();updateActionStates()}}
function selectedPackageId(){let element=selected.type==='element'?findElement(project,selected.id):null;while(element&&element.kind!=='Package')element=findElement(project,element.ownerId);return element?.id||null}
function publishCurrentAwareness(extra={}){sendAwareness({diagramId:activeDiagram()?.id||null,packageId:selectedPackageId(),selectedId:selected.type==='element'?selected.id:null,presentationId:selected.nodeId||null,...extra})}
function closeTreeContextMenu(){treeContextMenu?.remove();treeContextMenu=null}
function contextButton(label,action,{disabled=false,danger=false,shortcut=''}={}){const b=document.createElement('button');b.type='button';b.className=danger?'context-danger':'';b.disabled=disabled;b.innerHTML=`<span>${esc(label)}</span>${shortcut?`<kbd>${esc(shortcut)}</kbd>`:''}`;b.onclick=()=>{closeTreeContextMenu();action()};return b}
function contextDivider(){const d=document.createElement('div');d.className='tree-context-divider';return d}
function showTreeContextMenu(x,y,id,type){closeTreeContextMenu();const menu=document.createElement('div');menu.className='tree-context-menu';menu.setAttribute('role','menu');const element=type==='element'?findElement(project,id):null;if(type==='element'){
 const newMenu=document.createElement('details');newMenu.className='tree-context-submenu';const summary=document.createElement('summary');summary.textContent='New child';newMenu.append(summary);const sub=document.createElement('div');sub.className='tree-context-submenu-panel';for(const kind of ['Package','ModelLibrary','PrimitiveType','Enumeration','Unit','QuantityKind','Signal','Comment','InterfaceBlock','AssociationBlock','ConstraintBlock','InstanceSpecification','Block','Requirement','Activity','StateMachine','Interaction']){const valid=canOwnKind(element,kind);sub.append(contextButton(kind,()=>createOwnedElement(id,kind),{disabled:!valid}))}sub.append(contextButton('Diagram…',()=>createDiagramForOwner(id)));newMenu.append(sub);menu.append(newMenu);
 const diagram=activeDiagram(),presentation=diagram&&findPresentation(project,id,diagram.id),showVerdict=diagram&&canShowElementOnDiagram(project,id,diagram.id);menu.append(contextButton(presentation?'Reveal on Active Diagram':'Show on Active Diagram',()=>presentation?revealPresentation(presentation):showExistingElement(id,viewportDiagramCenter()),{disabled:!diagram||(!presentation&&!showVerdict?.valid)}));menu.append(contextDivider());
 menu.append(contextButton('Rename',()=>beginTreeRename(id,type),{disabled:id===project.root.id,shortcut:'F2'}));menu.append(contextButton('Duplicate',()=>duplicateElement(id),{disabled:id===project.root.id,shortcut:'Ctrl+D'}));menu.append(contextButton('Copy',()=>copyTreeElement(id),{disabled:id===project.root.id,shortcut:'Ctrl+C'}));menu.append(contextButton('Paste',()=>pasteTreeElement(id),{disabled:!treeClipboard||!canPasteInto(id),shortcut:'Ctrl+V'}));menu.append(contextDivider());menu.append(contextButton('Expand all',()=>expandTreeBranch(id),{disabled:!hasOwnedChildren(id)}));menu.append(contextButton('Collapse branch',()=>collapseTreeBranch(id),{disabled:!hasOwnedChildren(id)}));menu.append(contextDivider());menu.append(contextButton('Delete',()=>{selected={type:'element',id,nodeId:null,edgeId:null};deleteSelected()},{disabled:id===project.root.id,danger:true,shortcut:'Delete'}));
 }else if(type==='diagram'){menu.append(contextButton('Open',()=>navigateDiagram(id)));menu.append(contextButton('Rename',()=>beginTreeRename(id,type),{shortcut:'F2'}));menu.append(contextButton('Delete',()=>deleteDiagram(id),{danger:true,shortcut:'Delete'}))}
 document.body.append(menu);treeContextMenu=menu;const rect=menu.getBoundingClientRect();menu.style.left=`${Math.min(x,innerWidth-rect.width-8)}px`;menu.style.top=`${Math.min(y,innerHeight-rect.height-8)}px`;requestAnimationFrame(()=>menu.querySelector('button:not(:disabled),summary')?.focus())}
function beginTreeRename(id,type){closeTreeContextMenu();view=type==='diagram'?'diagrams':'model';renderTree();const row=document.querySelector(`.tree-row[data-id="${CSS.escape(id)}"]`);if(!row)return;const nameEl=row.querySelector('.tree-name'),oldName=nameEl.textContent;const input=document.createElement('input');input.className='tree-inline-rename';input.value=oldName;nameEl.replaceWith(input);input.focus();input.select();let finished=false;const finish=saveName=>{if(finished)return;finished=true;const value=input.value.trim();if(saveName&&value&&value!==oldName){checkpoint();if(type==='diagram'){const d=project.diagrams.find(x=>x.id===id);if(d)d.name=value}else{const e=findElement(project,id);if(e)e.name=value}commitLocal({type:'rename-model-item',id,itemType:type,name:value},`Renamed ${oldName} to ${value}`)}else renderTree()};input.onkeydown=ev=>{if(ev.key==='Enter'){ev.preventDefault();finish(true)}else if(ev.key==='Escape'){ev.preventDefault();finish(false)}};input.onblur=()=>finish(true)}
function canOwnKind(owner,kind){if(!owner)return false;const rules=ELEMENTS[kind]?.ownerKinds;if(rules?.length)return rules.includes(owner.kind);return ['Model','Package','ModelLibrary','Profile'].includes(owner.kind)}
function createOwnedElement(ownerId,kind){const owner=findElement(project,ownerId);if(!canOwnKind(owner,kind)){log(`${owner?.kind||'Selection'} cannot own ${kind}.`,'warn');return}checkpoint();const e=defaultElement(kind,ownerId);applySpecialDefaults(e);project.elements.push(e);selected={type:'element',id:e.id,nodeId:null,edgeId:null};commitLocal({type:'create-element',element:structuredClone(e)},`Created ${kind}`);setTimeout(()=>beginTreeRename(e.id,'element'),0)}
function createDiagramForOwner(ownerId){selected={type:'element',id:ownerId,nodeId:null,edgeId:null};createDiagram()}
function descendantIds(id){const result=new Set(),stack=[id];while(stack.length){const current=stack.pop();for(const e of project.elements.filter(x=>x.ownerId===current))if(!result.has(e.id)){result.add(e.id);stack.push(e.id)}}return result}
function hasOwnedChildren(id){return project.elements.some(e=>e.ownerId===id)}
function canMoveElement(elementId,targetId){if(!elementId||elementId===targetId||elementId===project.root.id)return false;const moving=findElement(project,elementId),target=findElement(project,targetId);if(!moving||!target)return false;if(descendantIds(elementId).has(targetId))return false;const rules=ELEMENTS[moving.kind]?.ownerKinds;if(rules?.length)return rules.includes(target.kind);return ['Model','Package','ModelLibrary','Profile'].includes(target.kind)}
function moveElementTo(elementId,targetId){if(!canMoveElement(elementId,targetId)){log('That containment move is not valid for this SysML element.','warn');return}const e=findElement(project,elementId),oldOwner=e.ownerId;checkpoint();e.ownerId=targetId;commitLocal({type:'move-element',elementId,targetOwnerId:targetId,expectedOwnerId:oldOwner},`Moved ${e.name}`);selected={type:'element',id:elementId,nodeId:null,edgeId:null}}
function cloneElementSubtree(sourceId,newOwnerId){const source=findElement(project,sourceId);if(!source)return null;const sourceIds=new Set([sourceId,...descendantIds(sourceId)]),map=new Map();for(const oldId of sourceIds)map.set(oldId,uid('el'));const originals=[source,...project.elements.filter(e=>sourceIds.has(e.id)&&e.id!==sourceId)];const clones=originals.map((e,index)=>{const c=structuredClone(e);c.id=map.get(e.id);c.externalId=uid('EXT').toUpperCase();c.ownerId=e.id===sourceId?newOwnerId:(map.get(e.ownerId)||newOwnerId);if(index===0)c.name=`${e.name} Copy`;return c});project.elements.push(...clones);for(const rel of project.relationships.filter(r=>sourceIds.has(r.sourceId)&&sourceIds.has(r.targetId))){const c=structuredClone(rel);c.id=uid('rel');c.externalId=uid('REL').toUpperCase();c.sourceId=map.get(rel.sourceId);c.targetId=map.get(rel.targetId);project.relationships.push(c)}return clones[0]}
function duplicateElement(id){const e=findElement(project,id);if(!e||id===project.root.id)return;checkpoint();const copy=cloneElementSubtree(id,e.ownerId);if(copy){selected={type:'element',id:copy.id,nodeId:null,edgeId:null};commitLocal({type:'duplicate-subtree',sourceId:id,newRootId:copy.id},`Duplicated ${e.name}`)}}
function copyTreeElement(id){const e=findElement(project,id);if(!e)return;treeClipboard={sourceId:id,mode:'copy'};log(`Copied ${e.name}.`,'ok');updateActionStates()}
function canPasteInto(ownerId){return Boolean(treeClipboard&&canMoveClone(treeClipboard.sourceId,ownerId))}
function canMoveClone(sourceId,ownerId){const source=findElement(project,sourceId),owner=findElement(project,ownerId);if(!source||!owner)return false;const rules=ELEMENTS[source.kind]?.ownerKinds;if(rules?.length)return rules.includes(owner.kind);return ['Model','Package','ModelLibrary','Profile'].includes(owner.kind)}
function pasteTreeElement(ownerId){if(!canPasteInto(ownerId)){log('The copied element cannot be owned by that target.','warn');return}checkpoint();const copy=cloneElementSubtree(treeClipboard.sourceId,ownerId);if(copy){selected={type:'element',id:copy.id,nodeId:null,edgeId:null};commitLocal({type:'paste-subtree',sourceId:treeClipboard.sourceId,targetOwnerId:ownerId,newRootId:copy.id},`Pasted ${copy.name}`)}}
function collapseTreeBranch(id){const ids=collapsedTreeIds();ids.add(id);for(const childId of descendantIds(id))if(hasOwnedChildren(childId))ids.add(childId);project.uiState.collapsedTreeIds=[...ids];save();renderTree()}
function expandTreeBranch(id){const ids=collapsedTreeIds();ids.delete(id);for(const childId of descendantIds(id))ids.delete(childId);project.uiState.collapsedTreeIds=[...ids];save();renderTree()}
function deleteDiagram(id){const d=project.diagrams.find(x=>x.id===id);if(!d||!confirm(`Delete diagram "${d.name}"? The semantic model elements will remain.`))return;checkpoint();project.diagrams=project.diagrams.filter(x=>x.id!==id);if(project.activeDiagramId===id)project.activeDiagramId=project.diagrams[0]?.id||null;selected={type:null,id:null,nodeId:null,edgeId:null};commitLocal({type:'delete-diagram',diagramId:id},`Deleted diagram ${d.name}`)}

function renderPalette(){const p=$('palette');p.replaceChildren();const diagram=activeDiagram(),a=allowed(diagram?.diagramType),elementTools=diagram?.diagramType==='Sequence Diagram'?[...a.elements,'ExecutionSpecification','InteractionOperand']:a.elements,relationshipTools=diagram?.diagramType==='Sequence Diagram'?[...a.relationships,'LostMessage','FoundMessage']:isIBD(diagram)?a.relationships.filter(item=>item!=='ItemFlow'):a.relationships,eligibleElements=elementTools.filter(kind=>supportAction(kind,'element','create').allowed),eligibleRelationships=relationshipTools.filter(kind=>supportAction(['LostMessage','FoundMessage'].includes(kind)?'Message':kind,'relationship','create').allowed);for(const [title,items] of [['Elements',eligibleElements],['Relationships',eligibleRelationships]]){const g=document.createElement('div');g.className='palette-group';g.innerHTML=`<h4>${title}</h4>`;for(const x of items){const b=document.createElement('button'),recordKind=title==='Relationships'?'relationship':'element',manifest=isIBD(diagram)?ibdManifestEntry(x):null,classification=manifest?.classification||(diagram?.diagramType==='Use Case Diagram'?(['Actor','UseCase','Block','Package','Comment','Association','Generalization','Include','Extend','Dependency'].includes(x)?'UML':'SysML 1.6'):''),inventory=inventoryEntry(['LostMessage','FoundMessage'].includes(x)?'Message':x,recordKind),summary=capabilitySummary(inventory),support=summary.status,paletteIcon=['LostMessage','FoundMessage'].includes(x)?sysmlPaletteIcon('Message'):sysmlPaletteIcon(x),display=x==='LostMessage'?'Lost Message':x==='FoundMessage'?'Found Message':diagram?.diagramType==='Use Case Diagram'&&x==='Block'?'Subject / System Boundary':x,detailsId=`capability-${diagram?.id||'none'}-${recordKind}-${x}`.replace(/[^a-zA-Z0-9_-]/g,'-'),available=summary.available.length?summary.available:['No user workflow is currently qualified.'],unavailable=summary.unavailable.length?summary.unavailable:['No unavailable capability is recorded for the supported scope.'],description=`${summary.label} support. Available: ${available.join('; ')}. Unavailable: ${unavailable.join('; ')}. Supported diagrams: ${summary.diagramTypes.join(', ')||'None'}.`;b.className=`palette-tool support-${support} ${tool===x?'active':''}`;b.dataset.support=support;b.dataset.tool=x;b.dataset.recordKind=recordKind;if(classification)b.dataset.classification=classification;b.setAttribute('aria-pressed',String(tool===x));b.setAttribute('aria-describedby',detailsId);b.setAttribute('aria-label',`${display}${classification?` — ${classification}`:''}. ${description}`);b.title=`${display}${classification?` — ${classification}`:''}. ${description}`;b.innerHTML=`${paletteIcon}<span class="palette-tool-copy"><span class="palette-tool-name">${esc(display)}</span>${classification?`<span class="palette-classification">${esc(classification)}</span>`:''}<span class="support-label">${esc(summary.label)}</span></span><span id="${detailsId}" class="capability-popover" role="tooltip"><strong>${esc(summary.label)} support</strong><span><b>Available:</b> ${esc(available.join(' • '))}</span><span><b>Unavailable:</b> ${esc(unavailable.join(' • '))}</span><span><b>Supported diagrams:</b> ${esc(summary.diagramTypes.join(' • ')||'None')}</span><span><b>Semantics:</b> ${summary.semanticCreation?'manual creation supported':'manual creation unavailable'}; ${summary.editable?'editing supported':'inspection only'}.</span><span><b>Presentation:</b> ${summary.presentationCreation?'creation supported':'creation unavailable'}; ${summary.containmentDragDrop?'drag-and-drop supported':'drag-and-drop unavailable'}.</span><span><b>Lifecycle:</b> ${summary.importReimport?'import/reimport supported':'import unavailable'}; ${summary.saveReload?'save/reload supported':'save/reload unavailable'}; ${summary.undoRedo?'undo/redo supported':'undo/redo unavailable'}.</span></span>`;b.onclick=()=>{if(!inventory?.semanticCreation&&recordKind==='element'){log(`${display} cannot be created manually: ${unavailable.join('; ')}.`,'warn');return}tool=x;toolRecordKind=recordKind;sourceId=null;sourceNodeId=null;openMessageAnchor=null;renderPalette()};g.append(b)}p.append(g)}}
function renderDiagramSelect(){const s=$('diagramSelect');s.replaceChildren();for(const d of project.diagrams){const o=document.createElement('option');o.value=d.id;o.textContent=`${d.diagramType} — ${d.name}`;s.append(o)}s.value=project.activeDiagramId||'';$('diagramTitle').textContent=activeDiagram()?.name||'No diagram'}
function renderCanvas(){const svg=$('canvas');svg.replaceChildren();defs(svg);svg.style.transform=`scale(${zoom})`;const d=activeDiagram();if(!d)return;drawDiagramFrame(svg,d);if(isIBD(d))synchronizeIBDPresentations(project,d);if(d.diagramType==='Sequence Diagram')renderSequence(svg,d);else{const visibleNodes=isIBD(d)?visibleIBDNodes(d):d.nodes,visibleIds=new Set(visibleNodes.map(n=>n.id)),visibleEdges=d.edges.filter(e=>visibleIds.has(e.sourceNodeId)&&visibleIds.has(e.targetNodeId));for(const e of visibleEdges)drawEdge(svg,d,e);for(const n of visibleNodes)drawNode(svg,d,n);for(const e of visibleEdges)drawEdgeMarker(svg,d,e);for(const e of visibleEdges)drawEdgeEditOverlay(svg,d,e)}drawSelectionBounds(svg,d);attachCanvasSelection(svg,d);attachEdgeCanvasEvents(svg,d)}
function diagramHeaderLabel(d){if(d.headerText?.trim())return d.headerText.trim();const abbreviation=DIAGRAMS[d.diagramType]?.abbreviation||'diagram',owner=d.ownerId?qualifiedName(project,d.ownerId):project.name;return`${abbreviation} [${owner}] ${d.name}`}
function drawDiagramFrame(svg,d){const selectedFrame=selected.type==='diagram'&&selected.id===d.id,frame=el('rect',{class:`diagram-frame ${selectedFrame?'selected':''}`,x:8,y:8,width:3184,height:2184}),header=el('g',{class:`diagram-frame-header ${selectedFrame?'selected':''}`,role:'button','aria-label':`Select diagram header: ${diagramHeaderLabel(d)}`,tabindex:0}),selectFrame=ev=>{ev.stopPropagation();selectedNodeIds.clear();selected={type:'diagram',id:d.id,nodeId:null,edgeId:null};renderCanvas();renderProperties()};frame.onclick=ev=>{if(!tool||resolvePresentation({semanticType:tool,diagramType:d.diagramType}).placementMode!=='CONTEXTUAL')return;const contextNode=d.nodes.find(node=>node.isContextBoundary);createAt(svg,d,ev,contextNode)};header.append(el('path',{class:'diagram-frame-tab',d:'M8 8 H520 L542 30 V42 H8 Z'}));text(header,diagramHeaderLabel(d),20,29,'start','diagram-frame-title');header.onclick=selectFrame;header.ondblclick=ev=>{selectFrame(ev);$('properties').querySelector('input')?.focus()};header.onkeydown=ev=>{if(ev.key==='Enter'||ev.key===' '){ev.preventDefault();selectFrame(ev)}};svg.append(frame,header)}
function attachCanvasSelection(svg,d){svg.onpointerdown=ev=>{if(ev.target!==svg||tool||spacePan)return;ev.preventDefault();const start=point(svg,ev);marquee={start,current:start,additive:ev.ctrlKey||ev.metaKey||ev.shiftKey};svg.setPointerCapture(ev.pointerId)};svg.onpointermove=ev=>{if(!marquee)return;marquee.current=point(svg,ev);renderCanvas()};svg.onpointerup=ev=>{if(!marquee)return;const m=marquee;marquee=null;const x1=Math.min(m.start.x,m.current.x),y1=Math.min(m.start.y,m.current.y),x2=Math.max(m.start.x,m.current.x),y2=Math.max(m.start.y,m.current.y);if(!m.additive)selectedNodeIds.clear();if(Math.abs(x2-x1)<4&&Math.abs(y2-y1)<4){selectedNodeIds.clear();selected={type:null,id:null,nodeId:null,edgeId:null}}else{for(const id of nodesInSelectionBox(d.nodes,m.start,m.current,{intersect:ev.altKey}))selectedNodeIds.add(id);const first=d.nodes.find(n=>selectedNodeIds.has(n.id));selected=first?{type:'element',id:first.elementId,nodeId:first.id,edgeId:null}:{type:null,id:null,nodeId:null,edgeId:null}}renderCanvas();renderProperties()};svg.onclick=ev=>{if(ev.target!==svg||marquee)return;if(!tool)return;if(toolRecordKind==='relationship'&&isRelationshipTool(d,tool)){if(tool==='FoundMessage'&&!openMessageAnchor){openMessageAnchor=point(svg,ev);log('Found Message open endpoint placed; select its target Lifeline.','ok');return}if(tool==='LostMessage'&&sourceNodeId){createOpenMessage(d,point(svg,ev),'lost');return}log('Select a source and target element.','warn');return}createAt(svg,d,ev)}}
function drawSelectionBounds(svg,d){if(marquee){const x=Math.min(marquee.start.x,marquee.current.x),y=Math.min(marquee.start.y,marquee.current.y),w=Math.abs(marquee.current.x-marquee.start.x),h=Math.abs(marquee.current.y-marquee.start.y);svg.append(el('rect',{class:'marquee-selection',x,y,width:w,height:h}))}const nodes=d.nodes.filter(n=>selectedNodeIds.has(n.id));if(nodes.length>1){const x=Math.min(...nodes.map(n=>n.x)),y=Math.min(...nodes.map(n=>n.y)),r=Math.max(...nodes.map(n=>n.x+n.width)),b=Math.max(...nodes.map(n=>n.y+n.height));svg.append(el('rect',{class:'multi-selection-bounds',x:x-5,y:y-5,width:r-x+10,height:b-y+10}))}}
async function createAt(svg,d,ev,targetNode=null){const pt=point(svg,ev),creationTool=tool,ctx=findElement(project,d.contextId),targetElement=targetNode&&findElement(project,targetNode.elementId),ownerId=(['InputPin','OutputPin'].includes(creationTool)||creationTool==='InteractionOperand')&&targetElement?targetElement.id:ownerFor(creationTool,ctx,d),element=defaultElement(creationTool,ownerId);applySpecialDefaults(element);if(['PartProperty','ReferenceProperty','ValueProperty','FlowProperty','ConstraintProperty','ProxyPort','FullPort'].includes(creationTool)){const candidates=allElements(project).filter(candidate=>candidate.id!==element.id&&validTypeKinds(creationTool).includes(candidate.kind)),typeId=await guidedReferenceDialog({title:`Create ${creationTool}`,label:`${creationTool} type`,elements:candidates,guidance:referenceGuidance(creationTool)});if(!typeId){log(`${creationTool} creation cancelled; no semantic record was created.`,'ok');return}const type=findElement(project,typeId);if(!type||!validTypeKinds(creationTool).includes(type.kind)){log(`${creationTool} creation rejected because the selected stable type is no longer compatible.`,'error');return}element.typeRef=type.id;if(['PartProperty','ReferenceProperty'].includes(creationTool)){element.definitionId=type.id;element.semanticRole='Usage';element.composition=creationTool==='PartProperty'?'composite':'none';element.aggregation=creationTool==='PartProperty'?'composite':'none'}if(['ProxyPort','FullPort'].includes(creationTool)){element.direction='unspecified';element.isConjugated=false;element.multiplicity='1'}}if(creationTool==='CallBehaviorAction'){const id=await guidedReferenceDialog({title:'Create Call Behavior Action',label:'Called Behavior',elements:allElements(project).filter(item=>['Activity','Behavior','Interaction','StateMachine'].includes(item.kind)),guidance:'Select an existing Activity or Behavior. Create one in the owning behavior package when no candidate exists.'});if(!id)return log('Call Behavior Action creation cancelled; no semantic record was created.','ok');element.referencedBehaviorId=id}if(creationTool==='CallOperationAction'){const id=await guidedReferenceDialog({title:'Create Call Operation Action',label:'Called Operation',elements:allElements(project).filter(item=>item.kind==='Operation'),guidance:'Select an existing Operation owned by a compatible classifier.'});if(!id)return log('Call Operation Action creation cancelled; no semantic record was created.','ok');element.referencedOperationId=id}if(creationTool==='SendSignalAction'){const id=await guidedReferenceDialog({title:'Create Send Signal Action',label:'Signal',elements:allElements(project).filter(item=>item.kind==='Signal'),guidance:'Select an existing Signal. Create a Signal in a model package, then refresh.'});if(!id)return log('Send Signal Action creation cancelled; no semantic record was created.','ok');element.signalId=id}if(creationTool==='AcceptEventAction'){const id=await guidedReferenceDialog({title:'Create Accept Event Action',label:'Event or Trigger',elements:allElements(project).filter(item=>['Event','Trigger','Signal'].includes(item.kind)),guidance:'Select an existing Event, Trigger, or Signal by stable semantic identity.'});if(!id)return log('Accept Event Action creation cancelled; no semantic record was created.','ok');element.eventId=id}if(['ObjectNode','CentralBufferNode','DataStoreNode'].includes(creationTool)){const id=await guidedReferenceDialog({title:`Create ${creationTool}`,label:'Object type',elements:allElements(project).filter(item=>['Block','ValueType','DataType','PrimitiveType','Enumeration','Signal'].includes(item.kind)),guidance:'Select the classifier carried by this object node.'});if(!id)return log(`${creationTool} creation cancelled; no semantic record was created.`,'ok');element.typeRef=id;element.carriedTypeId=id}if(creationTool==='ActivityParameterNode'){const parameters=allElements(project).filter(item=>item.kind==='Parameter'&&item.ownerId===ctx?.id),id=await guidedReferenceDialog({title:'Create Activity Parameter Node',label:'Activity Parameter',elements:parameters,guidance:'Select a Parameter owned by this Activity. Create it from the Activity property panel, then refresh.'});if(!id)return log('Activity Parameter Node creation cancelled; no semantic record was created.','ok');const parameter=findElement(project,id);element.parameterId=id;element.typeRef=parameter.typeRef||'';element.carriedTypeId=element.typeRef;element.direction=parameter.direction||'in'}if(['InputPin','OutputPin'].includes(creationTool)){if(!targetElement||!['Action','CallBehaviorAction','CallOperationAction','SendSignalAction','AcceptEventAction'].includes(targetElement.kind))return log(`${creationTool} must be placed on the boundary of an Action.`,'error');const id=await guidedReferenceDialog({title:`Create ${creationTool}`,label:'Pin type',elements:allElements(project).filter(item=>['Block','ValueType','DataType','PrimitiveType','Enumeration','Signal'].includes(item.kind)),guidance:'Select an existing compatible classifier. Create the type in a model package, then refresh.'});if(!id)return log(`${creationTool} creation cancelled; no semantic record was created.`,'ok');element.typeRef=id;element.carriedTypeId=id}const activityReferenceIssue=validateActivityReference(project,{...element,id:element.id});if(activityReferenceIssue)return log(activityReferenceIssue,'error');project.elements.push(element);const placementTarget=targetNode||(['ProxyPort','FullPort'].includes(creationTool)&&isIBD(d)?d.nodes.find(node=>node.isContextBoundary):null),verdict=canShowElementOnDiagram(project,element.id,d.id,{point:pt,targetNode:placementTarget});project.elements.pop();if(!verdict.valid){log(verdict.message,'warn');return}checkpoint();project.elements.push(element);const result=showElementOnDiagram(project,element.id,d.id,pt,{source:'palette',targetNode:placementTarget});if(!result.created){project.elements.pop();undoHistory.pop();log(result.message,'warn');return}selected={type:'element',id:element.id,nodeId:result.node.id,edgeId:null};tool=null;toolRecordKind=null;commitLocal({type:'create-element',element:structuredClone(element),diagramId:d.id,node:structuredClone(result.node)},`Created ${creationTool}`);renderPalette()}
function ownerFor(kind,ctx,d){if(['PartProperty','ReferenceProperty','ValueProperty','FlowProperty','ConstraintProperty','ProxyPort','FullPort'].includes(kind)&&ctx?.kind==='Block')return ctx.id;if(['Action','CallBehaviorAction','CallOperationAction','SendSignalAction','AcceptEventAction','ObjectNode','CentralBufferNode','DataStoreNode','ActivityParameterNode','ActivityPartition','InitialNode','ActivityFinalNode','FlowFinalNode','DecisionNode','MergeNode','ForkNode','JoinNode'].includes(kind)&&ctx?.kind==='Activity')return ctx.id;if(['State','InitialPseudostate','FinalState'].includes(kind)&&ctx?.kind==='StateMachine')return ctx.id;if(kind==='Lifeline'&&ctx?.kind==='Interaction')return ctx.id;return d.ownerId||project.root.id}
function applySpecialDefaults(e){if(e.kind==='Actor'){e.name='Actor';e.visibility='public';e.isAbstract=false}if(e.kind==='UseCase'){e.name='Use Case';e.subjectId='';e.extensionPoints=[]}if(e.kind==='Block'&&activeDiagram()?.diagramType==='Use Case Diagram')e.name='System';if(e.kind==='Requirement'){e.name='Requirement';e.requirementId='REQ-';e.requirementText='Enter requirement text'}if(e.kind==='State')Object.assign(e,{entry:'',doActivity:'',exit:''});if(e.kind==='Action')e.actionLanguage='';if(e.kind==='Lifeline')e.representedElementId=''}
function drawNode(svg,d,n){const e=findElement(project,n.elementId);if(!e)return;const descriptor=rendererDescriptor(e.kind,d.diagramType);if(!descriptor.supported){drawDiagnosticPresentation(svg,d,n,e,descriptor);return}const g=el('g',{'data-presentation-id':n.id,'data-semantic-id':e.id,'data-semantic-kind':e.kind,class:`node ${n.isContextBoundary?'ibd-context ':''}${['PartProperty','ReferenceProperty'].includes(e.kind)?'ibd-property ':''}${['ProxyPort','FullPort'].includes(e.kind)?'ibd-port ':''}${n.inheritedPresentation?'inherited ':''}${(selected.nodeId===n.id||selectedNodeIds.has(n.id))?'selected':''}`,transform:`translate(${n.x} ${n.y})`});const w=n.width,h=n.height;
if(['ProxyPort','FullPort'].includes(e.kind)){g.append(el('rect',{class:'port-shape',x:0,y:0,width:w,height:h,fill:e.kind==='FullPort'?'#111':'#fff'}));text(g,propertyNotation(project,e),w/2,-4,'middle','port-label')}
else if(d.diagramType==='Use Case Diagram'&&e.kind==='Block')drawUseCaseSubject(g,e,w,h);else if(e.kind==='SendSignalAction'){g.append(el('polygon',{class:'shape',points:`1,1 ${w*.72},1 ${w-1},${h/2} ${w*.72},${h-1} 1,${h-1}`}));text(g,e.name,w*.45,h/2+4,'middle')}else if(e.kind==='AcceptEventAction'){g.append(el('polygon',{class:'shape',points:`1,1 ${w-1},1 ${w-1},${h-1} 1,${h-1} ${w*.22},${h/2}`}));text(g,e.name,w*.58,h/2+4,'middle')}else if(e.kind==='DataStoreNode'){g.append(el('rect',{class:'shape',x:1,y:1,width:w-2,height:h-2}));g.append(el('line',{x1:1,y1:12,x2:w-1,y2:12,stroke:'#111'}));text(g,'«datastore»',w/2,28,'middle','stereo');text(g,e.name,w/2,48,'middle')}else if(e.kind==='CentralBufferNode'){g.append(el('rect',{class:'shape',x:1,y:1,width:w-2,height:h-2}));text(g,'«centralBuffer»',w/2,24,'middle','stereo');text(g,e.name,w/2,46,'middle')}else if(e.kind==='ObjectNode'){g.append(el('rect',{class:'shape',x:1,y:1,width:w-2,height:h-2}));const type=findElement(project,e.typeRef||e.carriedTypeId);text(g,`${e.name}${type?` : ${type.name}`:''}`,w/2,h/2+4,'middle')}else if(e.kind==='ActivityParameterNode'){g.append(el('rect',{class:'shape',x:1,y:1,width:w-2,height:h-2}));const parameter=findElement(project,e.parameterId);text(g,`${parameter?.name||e.name} [${e.direction||'in'}]`,w/2,h/2+4,'middle')}else if(e.kind==='Actor')drawActor(g,e,w);else if(e.kind==='UseCase'){g.append(el('ellipse',{class:'shape',cx:w/2,cy:h/2,rx:w/2-2,ry:h/2-2}));text(g,e.name,w/2,h/2+4,'middle')}
else if(['InitialNode','InitialPseudostate','JunctionPseudostate'].includes(e.kind))g.append(el('circle',{cx:w/2,cy:h/2,r:Math.min(w,h)/2-2,fill:'#111'}));else if(['ActivityFinalNode','FinalState'].includes(e.kind)){g.append(el('circle',{class:'shape',cx:w/2,cy:h/2,r:Math.min(w,h)/2-2}));g.append(el('circle',{cx:w/2,cy:h/2,r:Math.min(w,h)/2-7,fill:'#111'}))}
else if(e.kind==='FlowFinalNode'){g.append(el('circle',{class:'shape',cx:w/2,cy:h/2,r:Math.min(w,h)/2-2}));g.append(el('path',{d:`M${w*.28} ${h*.28}L${w*.72} ${h*.72}M${w*.72} ${h*.28}L${w*.28} ${h*.72}`,stroke:'#111','stroke-width':2}))}
else if(['DecisionNode','MergeNode','ChoicePseudostate'].includes(e.kind))g.append(el('polygon',{class:'shape',points:`${w/2},1 ${w-1},${h/2} ${w/2},${h-1} 1,${h/2}`}));else if(['ForkNode','JoinNode','StateFork','StateJoin'].includes(e.kind))g.append(el('rect',e.orientation==='vertical'?{x:w/2-4,y:0,width:8,height:h,fill:'#111'}:{x:0,y:h/2-4,width:w,height:8,fill:'#111'}));else if(['InputPin','OutputPin'].includes(e.kind)){g.append(el('rect',{class:'shape',x:1,y:1,width:w-2,height:h-2}));text(g,e.name||'',w/2,-4,'middle','port-label')}else if(e.kind==='ActivityPartition'){g.append(el('rect',{class:'shape',x:1,y:1,width:w-2,height:h-2}));if(e.orientation==='horizontal'){g.append(el('line',{x1:1,y1:28,x2:w-1,y2:28,stroke:'#111'}));text(g,e.name,w/2,19,'middle')}else{g.append(el('line',{x1:34,y1:1,x2:34,y2:h-1,stroke:'#111'}));const label=el('text',{x:18,y:h/2,'text-anchor':'middle',transform:`rotate(-90 18 ${h/2})`});label.textContent=e.name;g.append(label)}}else if(['ShallowHistory','DeepHistory'].includes(e.kind)){g.append(el('circle',{class:'shape',cx:w/2,cy:h/2,r:Math.min(w,h)/2-2}));text(g,e.kind==='DeepHistory'?'H*':'H',w/2,h/2+5,'middle','history-label')}else if(e.kind==='Package')drawPackage(g,e,w,h);else if(e.kind==='Comment')drawComment(g,e,w,h);else drawBox(g,e,n,w,h);
attachNodeEvents(g,d,n,e);svg.append(g);const capabilities=presentationCapabilities(resolvePresentation({semanticType:e.kind,diagramType:d.diagramType}).presentationType);if(selected.nodeId===n.id&&selectedNodeIds.size<=1&&capabilities.resizable&&!n.boundary){for(const [handle,hx,hy,cursor] of RESIZE_HANDLES){const size=handle.length===1?9:11,rh=el('rect',{class:`resize-handle resize-${handle}`,x:n.x+n.width*hx-size/2,y:n.y+n.height*hy-size/2,width:size,height:size,rx:2,'data-handle':handle,style:`cursor:${cursor}`});rh.onpointerdown=ev=>{ev.stopPropagation();checkpoint();resize={node:n,handle,sx:ev.clientX,sy:ev.clientY,start:{x:n.x,y:n.y,width:n.width,height:n.height}};rh.setPointerCapture(ev.pointerId)};rh.onpointermove=ev=>{if(!resize||resize.node!==n||resize.handle!==handle)return;const deltaX=(ev.clientX-resize.sx)/zoom,deltaY=(ev.clientY-resize.sy)/zoom,min=presentationMinimum(e,{contentHeight:minimumNodeHeight(project,e),isContextBoundary:Boolean(n.isContextBoundary)});Object.assign(n,resizePresentation(resize.start,handle,deltaX,deltaY,min));if(isIBD(d))synchronizeIBDPresentations(project,d);renderCanvas()};rh.onpointerup=()=>{if(!resize||resize.node!==n)return;const before=resize.start;resize=null;commitLocal({type:'replace-project',project:structuredClone(project),expectedValue:before},'Resized presentation')};svg.append(rh)}}}
function drawDiagnosticPresentation(svg,d,n,e,descriptor){const g=el('g',{'data-presentation-id':n.id,'data-semantic-id':e.id,'data-semantic-kind':e.kind,class:`node diagnostic-presentation ${selected.nodeId===n.id?'selected':''}`,transform:`translate(${n.x} ${n.y})`}),w=n.width||190,h=n.height||90;g.append(el('rect',{class:'shape diagnostic-shape',x:1,y:1,width:w-2,height:h-2,rx:4,'stroke-dasharray':'7 4'}));text(g,'Unsupported presentation',w/2,24,'middle','diagnostic-title');text(g,e.kind||'Unknown',w/2,46,'middle','element-name');wrap(g,descriptor.diagnostic||'Imported semantic record preserved for inspection.',8,64,w-16,13);attachNodeEvents(g,d,n,e);svg.append(g);const key=`${d.diagramType}:${e.kind}`;if(!missingRendererDiagnostics.has(key)){missingRendererDiagnostics.add(key);log(descriptor.diagnostic||`Unsupported presentation ${e.kind}.`,'warn')}}
function drawPackage(g,e,w,h){const tabWidth=Math.min(w*.52,90);g.append(el('path',{class:'shape',d:`M1 18H${tabWidth}V1H${tabWidth+24}L${tabWidth+36} 18H${w-1}V${h-1}H1Z`}));text(g,e.name,w/2,42,'middle','element-name')}
function drawComment(g,e,w,h){g.append(el('path',{class:'shape comment-shape',d:`M1 1H${w-22}L${w-1} 22V${h-1}H1Z`}));g.append(el('path',{d:`M${w-22} 1V22H${w-1}`,fill:'none',stroke:'#263746'}));wrap(g,e.body||e.documentation||e.name,8,20,w-18,14)}
function drawUseCaseSubject(g,e,w,h){g.append(el('rect',{class:'shape use-case-subject',x:1,y:1,width:w-2,height:h-2,fill:'none'}));text(g,`«subject» ${e.name}`,8,20,'start','element-name')}
function drawActor(g,e,w){g.append(el('circle',{class:'shape',cx:w/2,cy:15,r:10}));for(const a of [[w/2,25,w/2,58],[w/2-20,36,w/2+20,36],[w/2,58,w/2-18,86],[w/2,58,w/2+18,86]])g.append(el('line',{x1:a[0],y1:a[1],x2:a[2],y2:a[3],stroke:'#111'}));text(g,e.name,w/2,105,'middle')}
function drawBox(g,e,n,w,h){g.append(el('rect',{class:'shape',x:1,y:1,width:w-2,height:h-2,rx:['State','Action','CallBehaviorAction','CallOperationAction','SendSignalAction','AcceptEventAction'].includes(e.kind)?10:0}));const clipId=`clip-${String(e.id).replace(/[^a-zA-Z0-9_-]/g,'')}-${Math.round(w)}-${Math.round(h)}`;const cp=el('clipPath',{id:clipId});cp.append(el('rect',{x:3,y:3,width:Math.max(0,w-6),height:Math.max(0,h-6)}));g.append(cp);const content=el('g',{'clip-path':`url(#${clipId})`});g.append(content);let y=17;const header=classifierHeader(e);if(header.keyword){text(content,`«${header.keyword}»`,w/2,y,'middle','stereo');y+=16}const displayName=['PartProperty','ReferenceProperty'].includes(e.kind)?formatFeature(project,e):header.isAbstract?`{abstract} ${header.name}`:header.name;text(content,displayName,w/2,y,'middle','element-name');y+=9;content.append(el('line',{x1:1,y1:y,x2:w-1,y2:y,stroke:'#111'}));if(e.kind==='Requirement'){const requirement=requirementDisplay(e);text(content,`id = "${requirement.requirementId}"`,7,y+18);wrap(content,`text = "${requirement.text}"`,7,y+35,w-14,14)}else if(e.kind==='ConstraintProperty'){const constraint=constraintDisplay(project,e);if(constraint.typeName)text(content,`: ${constraint.typeName}`,7,y+18);if(constraint.expression)wrap(content,`{ ${constraint.expression} }`,7,y+35,w-14,14)}else if(e.kind==='TestCase'){const notation=selectedNotationDescriptor(project,e,activeDiagram()?.diagramType),rows=[e.verificationCaseId&&`id = \"${e.verificationCaseId}\"`,e.verificationObjective&&`objective = ${e.verificationObjective}`,e.verificationMethod&&`method = ${e.verificationMethod}`,e.acceptanceCriteria&&`criteria = ${e.acceptanceCriteria}`,e.verificationLevel&&`level = ${e.verificationLevel}`,e.plannedEnvironment&&`environment = ${e.plannedEnvironment}`,e.plannedStatus&&`planning = ${e.plannedStatus}`].filter(Boolean);let cy=y+18;for(const row of rows){if(cy>h-8)break;wrap(content,row,7,cy,w-14,14);cy+=Math.max(16,Math.ceil(row.length/Math.max(1,Math.floor((w-14)/6)))*14)}if(!notation.supported)text(content,'Diagnostic: unsupported context',7,cy)}else if(e.kind==='ValueType'){const details=[e.quantityKindRef&&`quantityKind=${findElement(project,e.quantityKindRef)?.name||e.quantityKindRef}`,e.unitRef&&`unit=${findElement(project,e.unitRef)?.name||e.unitRef}`].filter(Boolean).join(', ');if(details)wrap(content,details,7,y+18,w-14,14)}else if(e.kind==='State'){let cy=y+18;for(const [k,v] of [['entry',e.entry],['do',e.doActivity],['exit',e.exit]])if(v){text(content,`${k} / ${v}`,7,cy);cy+=16}}else if(supportsCompartments(e))drawCompartments(content,e,n,w,h,y)}
function drawCompartments(g,e,n,w,h,y){let cy=y;const bottom=h-5,visibility={...e.compartmentVisibility,...n.presentationOptions?.compartmentVisibility};for(const compartment of visibleCompartments(project,e,visibility)){if(cy+23>bottom)break;cy+=19;g.append(el('line',{x1:1,y1:cy-14,x2:w-1,y2:cy-14,stroke:'#777'}));text(g,compartment.label,7,cy,'start','compartment-title');const rows=compartment.rows;let shown=0;for(const item of rows){if(cy+16>bottom)break;cy+=16;text(g,formatFeature(project,item),9,cy,'start','compartment-row');shown++}if(shown<rows.length&&cy+16<=bottom){cy+=16;text(g,`… ${rows.length-shown} more`,9,cy,'start','compartment-overflow')}}}
function attachNodeEvents(g,d,n,e){g.onpointerdown=ev=>{ev.stopPropagation();const additive=ev.ctrlKey||ev.metaKey||ev.shiftKey;if(tool&&toolRecordKind==='relationship'&&isRelationshipTool(d,tool)){selected={type:'element',id:e.id,nodeId:n.id,edgeId:null};relClick(d,e,n);return}if(tool&&(allowed(d.diagramType).elements.includes(tool)||resolvePresentation({semanticType:tool,diagramType:d.diagramType}).placementMode==='CONTEXTUAL')){const placementMode=resolvePresentation({semanticType:tool,diagramType:d.diagramType}).placementMode;if(placementMode==='CONTEXTUAL'){createAt($('canvas'),d,ev,n);return}if(placementMode==='DIRECT'){createAt($('canvas'),d,ev);return}}selectedNodeIds=updateSelection(selectedNodeIds,n.id,{additive,toggle:true});selected={type:'element',id:e.id,nodeId:n.id,edgeId:null};const moving=d.nodes.filter(x=>selectedNodeIds.has(x.id)),withChildren=(isIBD(d)||e.kind==='CombinedFragment')?[...new Set(moving.flatMap(x=>[x,...presentationDescendants(d,x.id)]))]:moving;drag={node:n,sx:ev.clientX,sy:ev.clientY,starts:withChildren.map(x=>({node:x,x:x.x,y:x.y}))};g.setPointerCapture(ev.pointerId);renderCanvas();renderProperties()};g.onpointermove=ev=>{if(!drag||drag.node!==n)return;const dx=(ev.clientX-drag.sx)/zoom,rawDy=(ev.clientY-drag.sy)/zoom,dy=e.kind==='Lifeline'?0:rawDy;for(const item of drag.starts){item.node.x=item.x+(e.kind==='ExecutionSpecification'?0:dx);item.node.y=item.y+dy;if(item.node.boundary)snapBoundary(item.node,d)}renderCanvas()};g.onpointerup=()=>{if(!drag)return;const before=drag.starts.map(x=>({id:x.node.id,x:x.x,y:x.y}));const completed=drag;drag=null;if(before.length===1)commitLocal({type:'move-node',diagramId:d.id,nodeId:n.id,x:n.x,y:n.y,expectedValue:{x:before[0].x,y:before[0].y}},e.kind==='Lifeline'?'Moved Lifeline':'Moved symbol');else commitLocal({type:'batch-operation',operations:completed.starts.map((item,index)=>({type:'move-node',diagramId:d.id,nodeId:item.node.id,x:item.node.x,y:item.node.y,expectedValue:{x:before[index].x,y:before[index].y}}))},`Moved ${before.length} symbols`)};g.ondblclick=()=>openChildFor(e.id)}
function snapBoundary(n,d){
 const center={x:n.x+n.width/2,y:n.y+n.height/2};
 if(isIBD(d)){const owners=d.nodes.filter(node=>node.id!==n.id&&(node.isContextBoundary||['PartProperty','ReferenceProperty'].includes(findElement(project,node.elementId)?.kind))),owner=owners.filter(node=>center.x>=node.x&&center.x<=node.x+node.width&&center.y>=node.y&&center.y<=node.y+node.height).at(-1)||owners.find(node=>node.id===n.boundaryOwnerNodeId)||owners[0];if(owner)attachPortPresentation(n,owner,{endpointPath:owner.isContextBoundary?[n.elementId]:[owner.elementId,n.elementId]});return}
 const owner=d.nodes.find(node=>node.id===n.boundaryOwnerNodeId||node.id===n.parentPresentationId);if(!owner)return;const attachment=inferPerimeterAttachment(owner,n);n.boundaryOwnerNodeId=owner.id;n.parentPresentationId=owner.id;n.portSide=attachment.side;n.perimeterOffset=attachment.offset;Object.assign(n,perimeterPosition(owner,attachment.side,attachment.offset,n))
}
function isRelationshipTool(d,tool){return allowed(d.diagramType).relationships.includes(tool)||d.diagramType==='Sequence Diagram'&&['LostMessage','FoundMessage'].includes(tool)}
function createOpenMessage(d,anchor,sort,lifeline=e=>e){const node=sort==='lost'?d.nodes.find(item=>item.id===sourceNodeId):lifeline,semantic=findElement(project,node?.elementId);if(!semantic||semantic.kind!=='Lifeline'){log(`${sort==='lost'?'Lost':'Found'} Message requires a Lifeline endpoint.`,'error');return}checkpoint();const r=defaultRelationship('Message',sort==='lost'?semantic.id:'',sort==='found'?semantic.id:'',d.contextId||d.ownerId);r.messageSort=sort;r.sequenceOrder=nextMessageOrder(project,d);r.name=sort==='lost'?'lost message':'found message';project.relationships.push(r);const edge={id:uid('edge'),relationshipId:r.id,sourceId:r.sourceId,targetId:r.targetId,sourceNodeId:sort==='lost'?node.id:null,targetNodeId:sort==='found'?node.id:null,occurrenceY:anchor.y,[sort==='lost'?'lostAnchor':'foundAnchor']:{x:anchor.x,y:anchor.y},points:[]};d.edges.push(edge);sourceId=null;sourceNodeId=null;openMessageAnchor=null;tool=null;commitLocal({type:'create-relationship',relationship:structuredClone(r),diagramId:d.id,edge:structuredClone(edge)},`Created ${sort} Message`);renderPalette()}
function relClick(d,e,n){if(tool==='FoundMessage'&&openMessageAnchor){createOpenMessage(d,openMessageAnchor,'found',n);return}if(!sourceNodeId){sourceId=e.id;sourceNodeId=n.id;selected.nodeId=n.id;log(`Relationship source: ${e.name}`);return}if(sourceNodeId===n.id)return;const sourceNode=d.nodes.find(node=>node.id===sourceNodeId),s=findElement(project,sourceNode?.elementId||sourceId),rule=RELATIONSHIPS[tool];if(isIBD(d)&&['Connector','DelegationConnector'].includes(tool)){const connectorKind=tool==='DelegationConnector'?'delegation':'assembly';const r=defaultRelationship(tool,s?.id||'',e?.id||'',d.contextId);try{configureIBDConnector(project,d,r,sourceNode,n,connectorKind)}catch(error){sourceId=null;sourceNodeId=null;log(error.message,'error');return}checkpoint();project.relationships.push(r);const edge={id:uid('edge'),relationshipId:r.id,sourceId:r.sourceId,targetId:r.targetId,sourceNodeId:sourceNode.id,targetNodeId:n.id,sourceEndpointPath:[...r.sourceEndpointPath],targetEndpointPath:[...r.targetEndpointPath],points:orthogonalRoute(sourceNode,n),autoRouted:true};d.edges.push(edge);sourceId=null;sourceNodeId=null;commitLocal({type:'create-relationship',relationship:structuredClone(r),diagramId:d.id,edge:structuredClone(edge)},`Created ${connectorKind} connector`);return}if(d.diagramType==='Activity Diagram'&&['ControlFlow','ObjectFlow'].includes(tool)){try{const sourceResolved=resolveActivityEndpoint(project,d,sourceNode,'Source endpoint'),targetResolved=resolveActivityEndpoint(project,d,n,'Target endpoint');if(sourceResolved.semanticId!==s.id||targetResolved.semanticId!==e.id)throw new Error('Activity endpoint semantic identity changed during selection.')}catch(error){sourceId=null;sourceNodeId=null;log(error.message,'error');return}}let behaviorVerdict=validateBehaviorConnection(project,tool,s.id,e.id);if(d.diagramType==='Use Case Diagram'&&tool==='Association'&&!((s.kind==='Actor'&&e.kind==='UseCase')||(s.kind==='UseCase'&&e.kind==='Actor')))behaviorVerdict={valid:false,message:'Use Case communication Association must connect one Actor and one Use Case.'};const bindingVerdict=tool==='BindingConnector'?validateBindingEndpoints(project,s.id,e.id):{valid:true};if(!bindingVerdict.valid){log(bindingVerdict.message,'error');sourceId=null;sourceNodeId=null;return}if(!behaviorVerdict.valid){log(behaviorVerdict.message,'error');sourceId=null;sourceNodeId=null;return}if(rule&&!endpointOk(rule.source,s.kind)||rule&&!endpointOk(rule.target,e.kind)){log(`${tool} is invalid between ${s.kind} and ${e.kind}. Compatible source: ${formatEndpointKinds(rule?.source)}. Compatible target: ${formatEndpointKinds(rule?.target)}.`,'error');sourceId=null;sourceNodeId=null;return}checkpoint();let r;try{r=E01S5_RELATIONSHIPS.includes(tool)?createBddRelationship(project,tool,s.id,e.id,{ownerId:d.contextId||d.ownerId},{allowRequiredFieldEditing:true}):defaultRelationship(tool,s.id,e.id,d.contextId||d.ownerId);if(!E01S5_RELATIONSHIPS.includes(tool))project.relationships.push(r)}catch(error){undoHistory.pop();sourceId=null;sourceNodeId=null;log(error.message,'error');return}if(tool==='Message')r.sequenceOrder=nextMessageOrder(project,d);const edge={id:uid('edge'),relationshipId:r.id,sourceId:s.id,targetId:e.id,sourceNodeId:sourceNode?.id,targetNodeId:n.id,points:sourceNode?orthogonalRoute(sourceNode,n):[],autoRouted:true,...(tool==='Message'?{occurrenceY:100+r.sequenceOrder*50}:{})};d.edges.push(edge);sourceId=null;sourceNodeId=null;const createdTool=tool;tool=null;toolRecordKind=null;commitLocal({type:'create-relationship',relationship:structuredClone(r),diagramId:d.id,edge:structuredClone(edge)},`Created ${createdTool}`);renderPalette()}function endpointOk(rule,k){return rule==='*'||rule?.includes(k)}function formatEndpointKinds(rule){return rule==='*'?'any displayed element':(rule||[]).map(kind=>kind.replace(/([a-z])([A-Z])/g,'$1 $2')).join(', ')||'none'}
function edgeGeometry(d,edge,r){const s=d.nodes.find(n=>n.id===edge.sourceNodeId)||d.nodes.find(n=>n.elementId===r.sourceId),t=d.nodes.find(n=>n.id===edge.targetNodeId)||d.nodes.find(n=>n.elementId===r.targetId);if(!s||!t)return null;if(!edge.points?.length)edge.points=orthogonalRoute(s,t);const raw=edge.points,first=raw[0]||centerOf(t),last=raw.at(-1)||centerOf(s),a=connectorBoundaryPoint(s,first),b=connectorBoundaryPoint(t,last),all=[a,...raw,b];if(edge.labelPosition&&!edge.labelAnchor)edge.labelAnchor=labelAnchorForPoint(all,edge.labelPosition);if(edge.labelAnchor)edge.labelPosition=labelPointForAnchor(all,edge.labelAnchor);return{s,t,a,b,pts:raw,all}}
function findDropNode(d,p,excludeId){return [...d.nodes].reverse().find(n=>n.id!==excludeId&&p.x>=n.x-8&&p.x<=n.x+n.width+8&&p.y>=n.y-8&&p.y<=n.y+n.height+8)}
function startEdgeDrag(ev,svg,d,edge,mode,index=null){ev.stopPropagation();ev.preventDefault();if(!['source','target'].includes(mode))checkpoint();edgeEdit={edge,mode,index,pointerId:ev.pointerId,before:structuredClone(edge)};svg.setPointerCapture(ev.pointerId)}
function keyboardTargets(d,edge,r,mode){const current=edge[`${mode}NodeId`],rule=RELATIONSHIPS[r.kind];return d.nodes.filter(node=>node.id!==current).filter(node=>{const source=mode==='source'?node:d.nodes.find(n=>n.id===edge.sourceNodeId),target=mode==='target'?node:d.nodes.find(n=>n.id===edge.targetNodeId),sourceElement=findElement(project,source?.elementId),targetElement=findElement(project,target?.elementId),kind=r.connectorKind||(r.kind==='DelegationConnector'?'delegation':'assembly');if(!sourceElement||!targetElement)return false;if(isIBD(d)&&['Connector','DelegationConnector'].includes(r.kind))return validIBDConnector(project,d,source,target,kind);return(!rule||endpointOk(rule.source,sourceElement.kind)&&endpointOk(rule.target,targetElement.kind))&&!bddRelationshipIssues(project,{...r,sourceId:sourceElement.id,targetId:targetElement.id}).length}).sort((a,b)=>a.id.localeCompare(b.id))}
function confirmKeyboardReconnect(d,edge,r,mode,node){const beforeRelationship=structuredClone(r),beforeEdge=structuredClone(edge),source=mode==='source'?node:d.nodes.find(n=>n.id===edge.sourceNodeId),target=mode==='target'?node:d.nodes.find(n=>n.id===edge.targetNodeId);if(!source||!target){log('The selected endpoint target is no longer available.','error');return}checkpoint();try{if(E01S5_RELATIONSHIPS.includes(r.kind))reconnectBddRelationship(project,r.id,mode,node.elementId);else if(d.diagramType==='Activity Diagram'&&['ControlFlow','ObjectFlow'].includes(r.kind))reconnectActivityFlow(project,d,r,mode,node);else{r.sourceId=source.elementId;r.targetId=target.elementId;if(isIBD(d)&&['Connector','DelegationConnector'].includes(r.kind))configureIBDConnector(project,d,r,source,target,r.connectorKind||(r.kind==='DelegationConnector'?'delegation':'assembly'))}edge.sourceNodeId=source.id;edge.targetNodeId=target.id;edge.sourceId=source.elementId;edge.targetId=target.elementId;if(isIBD(d)&&['Connector','DelegationConnector'].includes(r.kind)){edge.sourceEndpointPath=[...r.sourceEndpointPath];edge.targetEndpointPath=[...r.targetEndpointPath]}edge.points=orthogonalRoute(source,target);const descriptor=endpointDescriptor(project,d,node);commitLocal({type:'reconnect-connector',diagramId:d.id,edgeId:edge.id,relationshipId:r.id,end:mode,descriptor:{...descriptor,roleId:node.elementId,presentationId:node.id}},`Reconnected ${r.kind} ${mode} to ${findElement(project,node.elementId)?.name||node.elementId}`);renderCanvas();renderProperties()}catch(error){Object.assign(r,beforeRelationship);Object.assign(edge,beforeEdge);undoHistory.pop();log(error.message,'error')}}
function drawEdgeHandles(svg,d,edge,r,g){if(selected.edgeId!==edge.id)return;g.pts.forEach((pt,i)=>{const h=el('circle',{class:'edge-bendpoint',cx:pt.x,cy:pt.y,r:5});h.onpointerdown=ev=>startEdgeDrag(ev,svg,d,edge,'bend',i);svg.append(h)});for(const [index,[mode,pt]] of [['source',g.a],['target',g.b]].entries()){const descriptor=endpointHandles(edge)[index],h=el('rect',{class:'edge-endpoint',x:pt.x-6,y:pt.y-6,width:12,height:12,rx:2,role:'button',tabindex:0,'aria-label':descriptor.ariaLabel,'aria-keyshortcuts':'Enter Space ArrowRight ArrowLeft Escape','data-endpoint-side':mode,'data-semantic-id':descriptor.semanticId});h.onpointerdown=ev=>startEdgeDrag(ev,svg,d,edge,mode);let state=null;h.onkeydown=ev=>{if(!state&&(ev.key==='Enter'||ev.key===' ')){ev.preventDefault();const targets=keyboardTargets(d,edge,r,mode);if(!targets.length){log(`No compatible ${mode} targets. Resolve endpoint typing and nested paths.`,'error');return}state={targets,index:0};h.setAttribute('aria-expanded','true');log(`Compatible target 1 of ${targets.length}: ${findElement(project,targets[0].elementId)?.name||targets[0].elementId}. Use arrow keys, Enter to confirm, or Escape to cancel.`);return}if(!state)return;if(ev.key==='Escape'){ev.preventDefault();state=null;h.removeAttribute('aria-expanded');log('Endpoint reconnection cancelled.');return}if(['ArrowRight','ArrowDown','ArrowLeft','ArrowUp','Tab'].includes(ev.key)){ev.preventDefault();const delta=['ArrowLeft','ArrowUp'].includes(ev.key)?-1:1;state.index=(state.index+delta+state.targets.length)%state.targets.length;const target=state.targets[state.index];log(`Compatible target ${state.index+1} of ${state.targets.length}: ${findElement(project,target.elementId)?.name||target.elementId}.`);return}if(ev.key==='Enter'||ev.key===' '){ev.preventDefault();const target=state.targets[state.index];state=null;h.removeAttribute('aria-expanded');confirmKeyboardReconnect(d,edge,r,mode,target)}};svg.append(h)}}
function attachEdgeCanvasEvents(svg,d){const priorMove=svg.onpointermove,priorUp=svg.onpointerup;svg.onpointermove=ev=>{if(edgeEdit){const p=point(svg,ev),edge=edgeEdit.edge;if(edgeEdit.mode==='bend')edge.points[edgeEdit.index]={x:p.x,y:p.y};else if(edgeEdit.mode==='label')edge.labelPosition={x:p.x,y:p.y};else edgeEdit.preview=p;renderCanvas();return}priorMove?.(ev)};svg.onpointerup=ev=>{if(edgeEdit){const edit=edgeEdit,p=point(svg,ev),edge=edit.edge;if(edit.mode==='source'||edit.mode==='target'){const oldNodeId=edit.mode==='source'?edge.sourceNodeId:edge.targetNodeId,drop=findDropNode(d,p,oldNodeId);if(drop){const rel=findRelationship(project,edge.relationshipId),beforeRelationship=structuredClone(rel),nextSource=edit.mode==='source'?drop:d.nodes.find(n=>n.id===edge.sourceNodeId),nextTarget=edit.mode==='target'?drop:d.nodes.find(n=>n.id===edge.targetNodeId),kind=rel.connectorKind||(rel.kind==='DelegationConnector'?'delegation':'assembly');checkpoint();try{if(E01S5_RELATIONSHIPS.includes(rel.kind))reconnectBddRelationship(project,rel.id,edit.mode,drop.elementId);else if(d.diagramType==='Activity Diagram'&&['ControlFlow','ObjectFlow'].includes(rel.kind))reconnectActivityFlow(project,d,rel,edit.mode,drop);else{rel.sourceId=nextSource.elementId;rel.targetId=nextTarget.elementId;if(isIBD(d)&&['Connector','DelegationConnector'].includes(rel.kind))configureIBDConnector(project,d,rel,nextSource,nextTarget,kind)}edge.sourceNodeId=nextSource.id;edge.targetNodeId=nextTarget.id;edge.sourceId=nextSource.elementId;edge.targetId=nextTarget.elementId;if(isIBD(d)&&['Connector','DelegationConnector'].includes(rel.kind)){edge.sourceEndpointPath=[...rel.sourceEndpointPath];edge.targetEndpointPath=[...rel.targetEndpointPath]}edge.points=orthogonalRoute(nextSource,nextTarget);commitLocal({type:'reconnect-connector',diagramId:d.id,edgeId:edge.id,relationshipId:rel.id,end:edit.mode,descriptor:{...endpointDescriptor(project,d,drop),roleId:drop.elementId,presentationId:drop.id}},`Reconnected ${rel.kind} ${edit.mode}`)}catch(error){Object.assign(rel,beforeRelationship);undoHistory.pop();Object.assign(edge,edit.before);log(error.message,'error')}}else Object.assign(edge,edit.before)}edgeEdit=null;renderCanvas();renderProperties();return}priorUp?.(ev)}}
function drawEdge(svg,d,edge){const r=findRelationship(project,edge.relationshipId);if(!r)return;const g=edgeGeometry(d,edge,r);if(!g)return;const style=relationshipStyle(r.kind),p=el('path',{'data-presentation-id':edge.id,'data-semantic-id':r.id,'data-semantic-kind':r.kind,class:`edge edge-hit-target ${style.dashed?'dashed':''} ${selected.edgeId===edge.id?'selected':''}`,d:pathData(g.all)});p.onclick=ev=>{ev.stopPropagation();selectedNodeIds.clear();selected={type:'relationship',id:r.id,nodeId:null,edgeId:edge.id};renderCanvas();renderProperties()};p.ondblclick=ev=>{ev.stopPropagation();const pt=point(svg,ev),idx=nearestSegmentIndex(g.all,pt);const waypoint={id:uid('waypoint'),x:pt.x,y:pt.y};checkpoint();addWaypoint(edge,waypoint);edge.points.splice(Math.max(0,idx),0,pt);commitLocal({type:'add-waypoint',diagramId:d.id,edgeId:edge.id,relationshipId:r.id,waypoint},'Added bendpoint');renderCanvas()};svg.append(p);const ends=relationshipEndLabels(r);if(ends.source)text(svg,ends.source,g.a.x+8,g.a.y-7,'start','edge-end-label');if(ends.target)text(svg,ends.target,g.b.x-8,g.b.y-7,'end','edge-end-label');const mid=edge.labelPosition||midpointAlong(g.all),flows=itemFlowsForConnector(project,r.id),flowLabel=flows.map(flow=>{const names=(flow.conveyedClassifierIds||flow.conveyedIds||[]).map(id=>findElement(project,id)?.name||id).join(', '),arrow=flow.direction==='targetToSource'?'←':flow.direction==='bidirectional'?'↔':'→';return `«itemFlow» ${names} ${arrow}`}).join(' · '),behaviorLabel=behaviorRelationshipLabel(project,r),baseLabel=behaviorLabel||(r.stereotype?`«${r.stereotype}» ${r.name||''}`:r.name),label=[baseLabel,flowLabel].filter(Boolean).join(' ');if(label){const labelGroup=el('g',{class:'edge-label-group'});const labelText=el('text',{class:'edge-label',x:mid.x,y:mid.y-5,'text-anchor':'middle'});labelText.textContent=label;labelGroup.append(labelText);labelGroup.onpointerdown=ev=>{ev.stopPropagation();const start=point(svg,ev),before=edge.labelPosition?{...edge.labelPosition}:null;edgeEdit={edge,mode:'label',start,beforeLabel:before,before:structuredClone(edge)};svg.setPointerCapture(ev.pointerId)};svg.append(labelGroup)}}
function drawEdgeEditOverlay(svg,d,edge){if(selected.edgeId!==edge.id)return;const r=findRelationship(project,edge.relationshipId),g=r&&edgeGeometry(d,edge,r);if(g)drawEdgeHandles(svg,d,edge,r,g)}
function drawEdgeMarker(svg,d,edge){const r=findRelationship(project,edge.relationshipId);if(!r)return;const g=edgeGeometry(d,edge,r),style=relationshipStyle(r.kind);if(!g||style.marker==='none')return;const prev=g.all.at(-2)||g.a,dx=g.b.x-prev.x,dy=g.b.y-prev.y,len=Math.hypot(dx,dy)||1,start={x:g.b.x-dx/len*22,y:g.b.y-dy/len*22};svg.append(el('line',{class:`edge marker-overlay ${style.dashed?'dashed':''}`,x1:start.x,y1:start.y,x2:g.b.x,y2:g.b.y,'marker-end':`url(#${style.marker})`}))}
const missingRendererDiagnostics=new Set();
function renderSequence(svg,d){
 const nodes=d.nodes.map(n=>({n,e:findElement(project,n.elementId)})).filter(item=>item.e);
 for(const {n,e} of nodes){const resolution=resolvePresentation({semanticType:e.kind,diagramType:d.diagramType});if(e.kind==='ExecutionSpecification'){drawExecutionSpecification(svg,d,n,e);continue}if(e.kind==='InteractionOperand'){drawInteractionOperand(svg,d,n,e);continue}if(resolution.placementMode!=='DIRECT'){const key=`${d.diagramType}:${e.kind}`;if(!missingRendererDiagnostics.has(key)){missingRendererDiagnostics.add(key);log(`Skipped contextual presentation: diagram = ${d.diagramType}, semanticType = ${e.kind}, presentationType = ${resolution.presentationType||'N/A'}.`,'warn')}continue}if(e.kind==='Lifeline')drawSequenceLifeline(svg,d,n,e);else if(e.kind==='CombinedFragment')drawCombinedFragment(svg,d,n,e);else if(e.kind==='InteractionUse')drawInteractionUse(svg,d,n,e);else if(e.kind==='Comment')drawNode(svg,d,n);else{const key=`${d.diagramType}:${e.kind}`;if(!missingRendererDiagnostics.has(key)){missingRendererDiagnostics.add(key);log(`Missing renderer: diagram = ${d.diagramType}, semanticType = ${e.kind}, presentationType = ${resolution.presentationType}.`,'error')}}}
 for(const edge of d.edges)drawSequenceMessage(svg,d,edge)
}
function drawSequenceLifeline(svg,d,n,e){const geometry=lifelineGeometry(n),g=el('g',{'data-presentation-id':n.id,'data-semantic-id':e.id,'data-semantic-kind':e.kind,class:`node ${(selected.nodeId===n.id||selectedNodeIds.has(n.id))?'selected':''}`,transform:`translate(${n.x} ${n.y})`});g.append(el('rect',{class:'shape',x:0,y:0,width:n.width,height:geometry.headHeight}));const represented=findElement(project,e.representedElementId),label=represented?`${e.name} : ${represented.name}`:e.name;text(g,label,n.width/2,25,'middle');g.append(el('line',{class:'lifeline',x1:n.width/2,y1:geometry.headHeight,x2:n.width/2,y2:geometry.timelineEndY-n.y}));attachNodeEvents(g,d,n,e);svg.append(g);if(selected.nodeId===n.id){const handle=el('rect',{class:'sequence-timeline-handle',x:geometry.centerX-6,y:geometry.timelineEndY-6,width:12,height:12,rx:2});handle.onpointerdown=ev=>{ev.stopPropagation();const before=n.timelineEndY??geometry.timelineEndY;handle.setPointerCapture(ev.pointerId);handle.onpointermove=move=>{resizeLifelineTimeline(n,point(svg,move).y);renderCanvas()};handle.onpointerup=()=>{commitLocal({type:'resize-lifeline-timeline',diagramId:d.id,nodeId:n.id,timelineEndY:n.timelineEndY,expectedValue:before},'Resized Lifeline timeline');renderProperties()}};svg.append(handle)}}
function drawSequenceMessage(svg,d,edge){const relationship=findRelationship(project,edge.relationshipId),geometry=relationship&&messageGeometry(d,edge,relationship);if(!geometry)return;const path=messagePathData(geometry),reply=relationship.messageSort==='reply',marker=['asynchronous','reply','create'].includes(relationship.messageSort)?'open':'messageFilled',selectedMessage=selected.edgeId===edge.id;const hit=el('path',{'data-presentation-id':edge.id,'data-semantic-id':relationship.id,'data-semantic-kind':'Message',class:'sequence-message-hit',d:path}),line=el('path',{class:`edge sequence-message ${reply?'dashed':''} ${selectedMessage?'selected':''}`,d:path,'marker-end':`url(#${marker})`});const select=ev=>{ev.stopPropagation();selectedNodeIds.clear();selected={type:'relationship',id:relationship.id,nodeId:null,edgeId:edge.id};renderCanvas();renderProperties()};hit.onclick=select;line.onclick=select;let startY=null,before=null;hit.onpointerdown=ev=>{ev.stopPropagation();startY=point(svg,ev).y;before=edge.occurrenceY??geometry.y;hit.setPointerCapture(ev.pointerId)};hit.onpointermove=ev=>{if(startY==null)return;moveMessageOccurrence(edge,Number(before)+point(svg,ev).y-startY);renderCanvas()};hit.onpointerup=()=>{if(startY==null)return;startY=null;commitLocal({type:'move-message-occurrence',diagramId:d.id,edgeId:edge.id,relationshipId:relationship.id,occurrenceY:edge.occurrenceY,sequenceOrder:edge.occurrenceY,expectedValue:before},'Moved Message occurrence')};svg.append(hit,line);const labelPoint=geometry.self?geometry.path[1]:{x:(geometry.path[0].x+geometry.path.at(-1).x)/2,y:geometry.y};const label=el('text',{class:'edge-label sequence-message-label',x:labelPoint.x+(edge.labelOffsetX||0),y:labelPoint.y-6+(edge.labelOffsetY||0),'text-anchor':'middle'});label.textContent=relationship.name||'message';label.onclick=select;label.ondblclick=ev=>{ev.stopPropagation();const value=prompt('Message name',relationship.name||'');if(value!=null)edit(relationship,'name',value,'relationship')};svg.append(label);if(selectedMessage){const lifelineEnds=relationship.messageSort==='lost'?[['source',geometry.path[0]]]:relationship.messageSort==='found'?[['target',geometry.path.at(-1)]]:[['source',geometry.path[0]],['target',geometry.path.at(-1)]];for(const [end,pt] of lifelineEnds){const handle=el('circle',{class:'edge-endpoint sequence-endpoint',cx:pt.x,cy:pt.y,r:6});handle.onpointerdown=ev=>{ev.stopPropagation();const oldNodeId=edge[`${end}NodeId`],oldElementId=relationship[`${end}Id`];handle.setPointerCapture(ev.pointerId);handle.onpointerup=up=>{const target=sequenceLifelineAt(project,d,point(svg,up));if(!target){log('Message endpoint must be dropped on a Lifeline timeline.','warn');return}edge[`${end}NodeId`]=target.id;edge[`${end}Id`]=target.elementId;relationship[`${end}Id`]=target.elementId;commitLocal({type:'reconnect-message',diagramId:d.id,edgeId:edge.id,relationshipId:relationship.id,end,nodeId:target.id,elementId:target.elementId,expectedValue:{nodeId:oldNodeId,elementId:oldElementId}},`Reconnected Message ${end}`);renderCanvas();renderProperties()}};svg.append(handle)}if(['lost','found'].includes(relationship.messageSort)){const key=relationship.messageSort==='lost'?'lostAnchor':'foundAnchor',pt=edge[key]||geometry.path[relationship.messageSort==='lost'?'at':'at']?.(-1)||geometry.path[0],open=el('circle',{class:'message-open-end',cx:pt.x,cy:pt.y,r:6,fill:'#fff',stroke:'#263746','stroke-width':2});open.onpointerdown=ev=>{ev.stopPropagation();const before=structuredClone(edge[key]);open.setPointerCapture(ev.pointerId);open.onpointermove=move=>{edge[key]=point(svg,move);edge.occurrenceY=edge[key].y;renderCanvas()};open.onpointerup=()=>commitLocal({type:'move-open-message-anchor',diagramId:d.id,edgeId:edge.id,anchorKey:key,anchor:structuredClone(edge[key]),expectedValue:before},`Moved ${relationship.messageSort} Message open endpoint`)};svg.append(open)}}}
function drawExecutionSpecification(svg,d,n,e){const lifeline=d.nodes.find(item=>item.id===n.parentPresentationId),x=lifeline?lifeline.x+lifeline.width/2-6:n.x;n.x=x;n.width=12;n.height=Math.max(35,n.height||100);const g=el('g',{'data-presentation-id':n.id,'data-semantic-id':e.id,'data-semantic-kind':e.kind,class:`node execution-specification ${selected.nodeId===n.id?'selected':''}`,transform:`translate(${n.x} ${n.y})`});g.append(el('rect',{class:'activation shape',x:0,y:0,width:n.width,height:n.height}));attachNodeEvents(g,d,n,e);svg.append(g);if(selected.nodeId===n.id){const handle=el('rect',{class:'sequence-timeline-handle',x:n.x-1,y:n.y+n.height-6,width:14,height:12,rx:2});handle.onpointerdown=ev=>{ev.stopPropagation();const before=n.height,start=point(svg,ev).y;handle.setPointerCapture(ev.pointerId);handle.onpointermove=move=>{resizeExecutionSpecification(n,before+point(svg,move).y-start);renderCanvas()};handle.onpointerup=()=>commitLocal({type:'resize-execution-specification',diagramId:d.id,nodeId:n.id,height:n.height,expectedValue:before},'Resized Execution Specification')};svg.append(handle)}}
function drawCombinedFragment(svg,d,n,e){const g=el('g',{'data-presentation-id':n.id,'data-semantic-id':e.id,'data-semantic-kind':e.kind,class:`node sequence-fragment ${(selected.nodeId===n.id||selectedNodeIds.has(n.id))?'selected':''}`,transform:`translate(${n.x} ${n.y})`}),label=e.fragmentOperator||'alt';g.append(el('rect',{class:'shape',x:0,y:0,width:n.width,height:n.height}));g.append(el('path',{d:'M0 0H68L82 14 68 28H0Z',fill:'#fff',stroke:'#263746'}));text(g,label,8,19,'start','fragment-operator');text(g,e.name||'',88,19,'start','fragment-name');attachNodeEvents(g,d,n,e);svg.append(g);if(selected.nodeId===n.id){const handle=el('rect',{class:'resize-handle resize-se',x:n.x+n.width-6,y:n.y+n.height-6,width:12,height:12,rx:2});handle.onpointerdown=ev=>{ev.stopPropagation();const start=point(svg,ev),before={width:n.width,height:n.height};handle.setPointerCapture(ev.pointerId);handle.onpointermove=move=>{const now=point(svg,move);n.width=Math.max(180,before.width+now.x-start.x);n.height=Math.max(100,before.height+now.y-start.y);renderCanvas()};handle.onpointerup=()=>commitLocal({type:'resize-node',diagramId:d.id,nodeId:n.id,width:n.width,height:n.height,expectedValue:before},'Resized Combined Fragment')};svg.append(handle)}}
function drawInteractionOperand(svg,d,n,e){const parent=d.nodes.find(item=>item.id===n.parentPresentationId);if(parent){n.x=parent.x;n.width=parent.width}const g=el('g',{'data-presentation-id':n.id,'data-semantic-id':e.id,'data-semantic-kind':e.kind,class:`node interaction-operand ${selected.nodeId===n.id?'selected':''}`,transform:`translate(${n.x} ${n.y})`});g.append(el('line',{x1:0,y1:0,x2:n.width,y2:0,stroke:'#263746','stroke-dasharray':'7 4'}));text(g,`[${e.guard||'else'}]`,8,18,'start','fragment-guard');attachNodeEvents(g,d,n,e);svg.append(g)}
function drawInteractionUse(svg,d,n,e){const g=el('g',{class:`node interaction-use ${(selected.nodeId===n.id||selectedNodeIds.has(n.id))?'selected':''}`,transform:`translate(${n.x} ${n.y})`});g.append(el('rect',{class:'shape',x:0,y:0,width:n.width,height:n.height}));g.append(el('path',{d:'M0 0H44L56 12 44 24H0Z',fill:'#fff',stroke:'#263746'}));text(g,'ref',8,17,'start','interaction-ref');text(g,e.name,n.width/2,n.height/2+5,'middle','element-name');attachNodeEvents(g,d,n,e);svg.append(g)}
function center(n){return centerOf(n)}function routeAll(){const d=activeDiagram();if(!d)return;checkpoint();const result=routeDiagramRelationships(project,d);if(!result.changed){undoHistory.pop();log(result.reason||'Relationship routing was not changed.','warn');renderCanvas();return}commitLocal({type:'replace-project',project:structuredClone(project)},'Routed connectors')}
function relationshipEndpointCandidates(relationship,side){const rule=RELATIONSHIPS[relationship.kind]?.[side],otherId=relationship[`${side==='source'?'target':'source'}Id`],compatible=allElements(project).filter(element=>element.id!==otherId&&endpointAllowed(rule,element.kind));if(['ControlFlow','ObjectFlow'].includes(relationship.kind)){const diagram=activeDiagram();return compatible.filter(element=>diagram?.nodes.some(node=>node.elementId===element.id)&&validateBehaviorConnection(project,relationship.kind,side==='source'?element.id:otherId,side==='target'?element.id:otherId).valid)}if(!['Connector','DelegationConnector'].includes(relationship.kind))return compatible;const diagram=activeDiagram(),edge=diagram?.edges.find(item=>item.relationshipId===relationship.id),otherSide=side==='source'?'target':'source',otherNode=diagram?.nodes.find(item=>item.id===edge?.[`${otherSide}NodeId`]);if(!diagram||!edge||!otherNode)return[];return compatible.filter(element=>{const node=diagram.nodes.find(item=>item.elementId===element.id);if(!node)return false;const source=side==='source'?node:otherNode,target=side==='target'?node:otherNode;return validIBDConnector(project,diagram,source,target,relationship.connectorKind||'assembly')})}
function endpointGuidance(relationship){return relationship.kind==='DelegationConnector'?referenceGuidance('DelegationConnector'):'Select a compatible displayed semantic endpoint. The ConnectorEnd stores the role, port, and nested path by stable ID.'}
function renderProperties(){const p=$('properties');p.replaceChildren();if(!selected.id){p.textContent='Select an element, relationship, or diagram.';return}const obj=selected.type==='relationship'?findRelationship(project,selected.id):selected.type==='diagram'?project.diagrams.find(d=>d.id===selected.id):findElement(project,selected.id);if(!obj)return;p.append(section('Identity',[field('Name',obj.name||'',v=>edit(obj,'name',v,selected.type),'blur'),field('Kind',obj.kind||obj.diagramType||''),field('External ID',obj.externalId||obj.id,v=>edit(obj,'externalId',v,selected.type),'blur'),field('Qualified Name',selected.type==='element'?qualifiedName(project,obj.id):'')]));if(selected.type==='diagram')p.append(section('Diagram Header',[field('Header text',obj.headerText||diagramHeaderLabel(obj),v=>edit(obj,'headerText',v,'diagram'),'blur'),button('Use generated header',()=>edit(obj,'headerText','','diagram'))]));if(selected.type==='element'){p.append(section('Ownership',[guidedReferenceField('Owner',obj.ownerId,allElements(project).filter(e=>e.id!==obj.id&&canMoveElement(obj.id,e.id)),v=>edit(obj,'ownerId',v),{guidance:'Select a compatible semantic owner. The qualified path and stable ID distinguish duplicate names.',emptyGuidance:'No compatible owner exists. Create the required Model, Package, classifier, or owning feature first.'})]));p.append(section('Documentation',[field('Documentation',obj.documentation||'',v=>edit(obj,'documentation',v),'blur',true)]));if(obj.kind==='Actor')p.append(section('Actor',[selectField('Visibility',obj.visibility||'public',[['public','public'],['private','private'],['protected','protected'],['package','package']],v=>edit(obj,'visibility',v)),checkField('Abstract',Boolean(obj.isAbstract),v=>edit(obj,'isAbstract',v)),field('Applied stereotypes',(obj.appliedStereotypes||[]).join(', '),v=>edit(obj,'appliedStereotypes',v.split(',').map(x=>x.trim()).filter(Boolean)),'blur')]));if(obj.kind==='UseCase')p.append(section('Use Case',[guidedReferenceField('Subject',obj.subjectId||'',allElements(project).filter(item=>['Block'].includes(item.kind)),v=>edit(obj,'subjectId',v),{guidance:'Select the existing Block that is the subject providing this Use Case.',emptyGuidance:'No subject Block exists. Create a Subject / System Boundary from this palette or a Block in the model, then refresh.'}),button('+ Extension Point',()=>addOwnedFeature(obj,'ExtensionPoint')),...allElements(project).filter(item=>item.kind==='ExtensionPoint'&&item.ownerId===obj.id).map(item=>field('Extension Point',item.name,value=>edit(item,'name',value),'blur'))]));if(obj.kind==='Requirement')for(const requirementSection of requirementEditor(obj))p.append(requirementSection);if(obj.kind==='TestCase')p.append(section('Verification Planning',[field('Verification Case ID',obj.verificationCaseId||'',v=>edit(obj,'verificationCaseId',v),'blur'),field('Objective',obj.verificationObjective||'',v=>edit(obj,'verificationObjective',v),'blur',true),selectField('Method',obj.verificationMethod||'Test',[['Test','Test'],['Analysis','Analysis'],['Inspection','Inspection'],['Demonstration','Demonstration']],v=>edit(obj,'verificationMethod',v)),field('Acceptance criteria',obj.acceptanceCriteria||'',v=>edit(obj,'acceptanceCriteria',v),'blur',true),selectField('Level',obj.verificationLevel||'System',[['Component','Component'],['Subsystem','Subsystem'],['System','System'],['Integration','Integration'],['Acceptance','Acceptance']],v=>edit(obj,'verificationLevel',v)),field('Planned environment',obj.plannedEnvironment||'',v=>edit(obj,'plannedEnvironment',v),'blur'),field('Responsible role',obj.responsibleRole||'',v=>edit(obj,'responsibleRole',v),'blur'),selectField('Planning status',obj.plannedStatus||'Draft',[['Draft','Draft'],['Planned','Planned'],['Ready','Ready'],['Deferred','Deferred'],['Cancelled','Cancelled']],v=>edit(obj,'plannedStatus',v)),field('Execution status',obj.executionStatus||'Unavailable') ]));if(isTypedFeature(obj))p.append(semanticFeatureEditor(obj));if(obj.kind==='ValueType')p.append(valueTypeEditor(obj));if(obj.kind==='Comment')p.append(commentEditor(obj));if(obj.kind==='ConstraintBlock')p.append(section('Constraint Definition',[field('Constraint expression',obj.constraintExpression||'',v=>edit(obj,'constraintExpression',v),'blur',true)]));if(obj.kind==='Reception')p.append(section('Reception',[selectField('Signal',obj.signalRef||'',allElements(project).filter(e=>e.kind==='Signal').map(e=>[e.id,qualifiedName(project,e.id)]),v=>edit(obj,'signalRef',v))]));if(obj.kind==='InstanceSpecification')p.append(section('Instance',[selectField('Classifier',obj.classifierIds?.[0]||'',allElements(project).filter(e=>['Block','AssociationBlock','InterfaceBlock','ConstraintBlock'].includes(e.kind)).map(e=>[e.id,qualifiedName(project,e.id)]),v=>edit(obj,'classifierIds',v?[v]:[]))]));if(obj.kind==='Slot')p.append(section('Slot',[selectField('Defining feature',obj.definingFeatureId||'',allElements(project).filter(e=>['ValueProperty','PartProperty','ReferenceProperty','FlowProperty','ConstraintProperty'].includes(e.kind)).map(e=>[e.id,qualifiedName(project,e.id)]),v=>edit(obj,'definingFeatureId',v)),field('Value',obj.valueSpecification?.value||'',v=>edit(obj,'valueSpecification',{kind:'LiteralString',value:v}),'blur')]));if(obj.kind==='Operation')p.append(section('Operation',[button('+ Parameter',()=>addOwnedFeature(obj,'Parameter'))]));if(['Block','InterfaceBlock','AssociationBlock'].includes(obj.kind))p.append(section('Signals',[button('+ Reception',()=>addOwnedFeature(obj,'Reception'))]));if(obj.kind==='CallBehaviorAction')p.append(section('Invocation',[guidedReferenceField('Referenced behavior',obj.referencedBehaviorId||'',allElements(project).filter(item=>['Behavior','Activity','Interaction','StateMachine'].includes(item.kind)),v=>edit(obj,'referencedBehaviorId',v),{guidance:'Select an existing Activity or Behavior by stable semantic identity.',emptyGuidance:'No compatible Behavior exists. Create an Activity in the owning behavior package, then refresh.'})]));if(obj.kind==='Activity')p.append(section('Activity parameters',[button('+ Parameter',()=>addOwnedFeature(obj,'Parameter')),...allElements(project).filter(item=>item.kind==='Parameter'&&item.ownerId===obj.id).map(item=>field('Parameter',item.name,value=>edit(item,'name',value),'blur'))]));if(obj.kind==='CallOperationAction')p.append(section('Invocation',[guidedReferenceField('Referenced operation',obj.referencedOperationId||'',allElements(project).filter(item=>item.kind==='Operation'),v=>edit(obj,'referencedOperationId',v),{guidance:'Select an existing semantic Operation by stable ID.',emptyGuidance:'No Operation exists. Create an Operation on a Block or InterfaceBlock, then refresh.'})]));if(obj.kind==='SendSignalAction')p.append(section('Signal',[guidedReferenceField('Signal',obj.signalId||'',allElements(project).filter(item=>item.kind==='Signal'),v=>edit(obj,'signalId',v),{guidance:'Select an existing semantic Signal by stable ID.',emptyGuidance:'No Signal exists. Create one from a BDD or model package, then refresh.'})]));if(obj.kind==='AcceptEventAction')p.append(section('Event',[guidedReferenceField('Event or Trigger',obj.eventId||'',allElements(project).filter(item=>['Event','Trigger','Signal'].includes(item.kind)),v=>edit(obj,'eventId',v),{guidance:'Select an existing Event, Trigger, or Signal by stable ID.',emptyGuidance:'No supported event exists. Create an Event, Trigger, or Signal, then refresh.'})]));if(obj.kind==='ActivityParameterNode')p.append(section('Activity Parameter',[guidedReferenceField('Parameter',obj.parameterId||'',allElements(project).filter(item=>item.kind==='Parameter'&&item.ownerId===activeDiagram()?.contextId),v=>edit(obj,'parameterId',v),{guidance:'Select a Parameter owned by this Activity.',emptyGuidance:'This Activity has no Parameter. Select the Activity in containment and use + Parameter, then refresh.'}),selectField('Direction',obj.direction||'in',[['in','in'],['out','out'],['inout','inout']],v=>edit(obj,'direction',v))]));if(['InputPin','OutputPin','ObjectNode','CentralBufferNode','DataStoreNode'].includes(obj.kind))p.append(section('Object Flow',[guidedReferenceField('Type',obj.typeRef||obj.carriedTypeId||'',allElements(project).filter(item=>['Block','ValueType','DataType','PrimitiveType','Enumeration','Signal'].includes(item.kind)),v=>{edit(obj,'typeRef',v);obj.carriedTypeId=v},{guidance:'Select a compatible classifier; the full stable ID is stored.',emptyGuidance:'No compatible classifier exists. Create a Block, ValueType, DataType, PrimitiveType, Enumeration, or Signal, then refresh.'}),...(['InputPin','OutputPin'].includes(obj.kind)?[field('Lower',obj.lower??1,v=>edit(obj,'lower',Number(v)),'blur'),field('Upper',obj.upper??1,v=>edit(obj,'upper',v==='*'?'*':Number(v)),'blur'),checkField('Ordered',Boolean(obj.isOrdered),v=>edit(obj,'isOrdered',v)),checkField('Unique',obj.isUnique!==false,v=>edit(obj,'isUnique',v))]:[])]));if(['Action','CallBehaviorAction','CallOperationAction','SendSignalAction','AcceptEventAction','ObjectNode','CentralBufferNode','DataStoreNode','ActivityParameterNode'].includes(obj.kind))p.append(section('Partition membership',[guidedReferenceField('Activity Partition',obj.partitionId||'',allElements(project).filter(item=>item.kind==='ActivityPartition'&&item.ownerId===activeDiagram()?.contextId),v=>edit(obj,'partitionId',v),{guidance:'Explicitly assign this node to an Activity Partition. Geometry never changes membership.',emptyGuidance:'No Activity Partition exists in this Activity. Create one from the palette, then refresh.'})]));if(obj.kind==='ActivityPartition')p.append(section('Partition responsibility',[guidedReferenceField('Represented element',obj.representedElementId||'',allElements(project).filter(item=>item.id!==obj.id),v=>edit(obj,'representedElementId',v),{guidance:'Optionally select the classifier, Actor, part, or organization represented by this partition.',emptyGuidance:'No compatible represented semantic element exists.'})]));if(['ForkNode','JoinNode','ActivityPartition'].includes(obj.kind))p.append(section('Activity notation',[selectField('Orientation',obj.orientation||'horizontal',[['horizontal','horizontal'],['vertical','vertical']],v=>edit(obj,'orientation',v))]));if(obj.kind==='Lifeline')p.append(section('Lifeline',[field('Selector',obj.selector||'',v=>edit(obj,'selector',v),'blur'),selectField('Represents',obj.representedElementId||'',allElements(project).filter(item=>item.id!==obj.id&&validLifelineRepresentation(project,item.id)).map(item=>[item.id,item.name]),v=>edit(obj,'representedElementId',v))]));if(obj.kind==='CombinedFragment')p.append(section('Interaction frame',[selectField('Operator',obj.fragmentOperator||'alt',INTERACTION_OPERATORS.map(operator=>[operator,operator]),v=>edit(obj,'fragmentOperator',v)),field('Guard',obj.guard||'',v=>edit(obj,'guard',v),'blur')]));if(obj.kind==='State')p.append(section('State Behavior',[field('Entry',obj.entry||'',v=>edit(obj,'entry',v),'blur'),field('Do',obj.doActivity||'',v=>edit(obj,'doActivity',v),'blur'),field('Exit',obj.exit||'',v=>edit(obj,'exit',v),'blur')]));if(supportsCompartments(obj))p.append(compartmentEditor(obj,selected.nodeId?activeDiagram()?.nodes.find(node=>node.id===selected.nodeId):null));p.append(section('Navigation',[button('Create/Open Child Diagram',()=>createOrOpenChild(obj)),button('Show References',()=>{view='xref';$('xrefTab').click()})]));if(selected.nodeId){const node=activeDiagram()?.nodes.find(item=>item.id===selected.nodeId);if(node)p.append(section('Diagram Presentation',[field('X',node.x,value=>editPresentationGeometry(node,'x',value),'blur'),field('Y',node.y,value=>editPresentationGeometry(node,'y',value),'blur'),field('Width',node.width,value=>editPresentationGeometry(node,'width',value),'blur'),field('Height',node.height,value=>editPresentationGeometry(node,'height',value),'blur'),field('Presentation ID',node.id)]))}}else if(selected.type==='relationship'){p.append(section('Relationship',[guidedReferenceField('Source',obj.sourceId,relationshipEndpointCandidates(obj,'source'),v=>editRelationshipEndpoint(obj,'source',v),{guidance:endpointGuidance(obj),emptyGuidance:`${endpointGuidance(obj)} No compatible source is currently presented.`}),guidedReferenceField('Target',obj.targetId,relationshipEndpointCandidates(obj,'target'),v=>editRelationshipEndpoint(obj,'target',v),{guidance:endpointGuidance(obj),emptyGuidance:`${endpointGuidance(obj)} No compatible target is currently presented.`}),...(traceabilityDirection(obj.kind)?[field('Semantic direction',`${traceabilityDirection(obj.kind)[0]} → ${traceabilityDirection(obj.kind)[1]}`)]:[]),...((['Connector','DelegationConnector','BindingConnector'].includes(obj.kind))?[field('Source endpoint path',(obj.sourceEndpointPath||obj.sourcePropertyPath||[]).map(step=>step.propertyId||step).join(' / ')),field('Target endpoint path',(obj.targetEndpointPath||obj.targetPropertyPath||[]).map(step=>step.propertyId||step).join(' / '))]:[]),...((obj.kind==='Message')?[selectField('Message type',obj.messageSort||'synchronous',MESSAGE_KINDS.map(kind=>[kind,kind]),v=>edit(obj,'messageSort',v,'relationship'))]:[]),field('Source role',obj.sourceRole||'',v=>edit(obj,'sourceRole',v,'relationship'),'blur'),field('Target role',obj.targetRole||'',v=>edit(obj,'targetRole',v,'relationship'),'blur'),field('Source multiplicity',obj.sourceMultiplicity||'1',v=>edit(obj,'sourceMultiplicity',v,'relationship'),'blur'),field('Target multiplicity',obj.targetMultiplicity||'1',v=>edit(obj,'targetMultiplicity',v,'relationship'),'blur'),selectField('Source aggregation',obj.sourceAggregation||'none',[['none','none'],['shared','shared'],['composite','composite']],v=>edit(obj,'sourceAggregation',v,'relationship')),selectField('Target aggregation',obj.targetAggregation||'none',[['none','none'],['shared','shared'],['composite','composite']],v=>edit(obj,'targetAggregation',v,'relationship')),checkField('Source navigable',obj.sourceNavigable!==false,v=>edit(obj,'sourceNavigable',v,'relationship')),checkField('Target navigable',obj.targetNavigable!==false,v=>edit(obj,'targetNavigable',v,'relationship')),checkField('Source end owned',Boolean(obj.sourceEndOwned),v=>edit(obj,'sourceEndOwned',v,'relationship')),checkField('Target end owned',obj.targetEndOwned!==false,v=>edit(obj,'targetEndOwned',v,'relationship')),...((obj.kind==='Message')?[selectField('Operation',obj.operationRef||'',allElements(project).filter(item=>item.kind==='Operation').map(item=>[item.id,item.name]),v=>edit(obj,'operationRef',v,'relationship')),selectField('Signal',obj.signalRef||'',allElements(project).filter(item=>item.kind==='Signal').map(item=>[item.id,item.name]),v=>edit(obj,'signalRef',v,'relationship')),field('Sequence order',obj.sequenceOrder??0,v=>edit(obj,'sequenceOrder',Number(v),'relationship'),'blur')]:[]),...((obj.kind==='Transition')?[selectField('Trigger',obj.triggerIds?.[0]||'',allElements(project).filter(item=>['Trigger','Event','Signal'].includes(item.kind)).map(item=>[item.id,item.name]),v=>edit(obj,'triggerIds',v?[v]:[],'relationship'))]:[]),...((obj.kind==='Extend')?[guidedReferenceField('Extension point',obj.extensionPointId||'',allElements(project).filter(item=>item.kind==='ExtensionPoint'&&item.ownerId===obj.targetId),v=>edit(obj,'extensionPointId',v,'relationship'),{guidance:'Select an Extension Point owned by the extended target Use Case. The stable semantic ID is stored.',emptyGuidance:'The target Use Case has no Extension Point. Select it and use + Extension Point in its property panel, then refresh.'})]:[]),...((obj.kind==='ObjectFlow')?[selectField('Carried type',obj.carriedTypeId||'',allElements(project).filter(item=>['Block','ValueType','Signal','DataType'].includes(item.kind)).map(item=>[item.id,item.name]),v=>edit(obj,'carriedTypeId',v,'relationship'))]:[]),...(['InformationFlow','ItemFlow'].includes(obj.kind)?[selectField('Conveyed item',obj.conveyedIds?.[0]||'',allElements(project).filter(e=>['Block','AssociationBlock','InterfaceBlock','ValueType','DataType','PrimitiveType','Enumeration','Signal','ValueProperty','FlowProperty'].includes(e.kind)).map(e=>[e.id,qualifiedName(project,e.id)]),v=>edit(obj,'conveyedIds',v?[v]:[],'relationship')),selectField('Realizing Association',obj.realizingRelationshipId||'',project.relationships.filter(r=>['Association','AssociationBlock','Aggregation','Composition'].includes(r.kind)).map(r=>[r.id,r.name||r.kind]),v=>edit(obj,'realizingRelationshipId',v,'relationship'))]:[]),field('Guard',obj.guard||'',v=>edit(obj,'guard',v,'relationship'),'blur'),field('Effect',obj.effect||'',v=>edit(obj,'effect',v,'relationship'),'blur'),...(['Connector','DelegationConnector'].includes(obj.kind)?[selectField('Connector kind',obj.connectorKind||'assembly',[['assembly','assembly'],['delegation','delegation']],v=>setConnectorKind(obj,v)),guidedReferenceField('AssociationBlock connector type',obj.connectorTypeRef||'',allElements(project).filter(item=>item.kind==='AssociationBlock'),v=>edit(obj,'connectorTypeRef',v,'relationship'),{guidance:'Optionally select an existing AssociationBlock. The Connector reuses that stable semantic identity; no duplicate AssociationBlock is created.',emptyGuidance:'No AssociationBlock exists. Create one from the BDD palette or containment tree, then refresh this panel.'}),...connectorItemFlowEditor(obj)]:[])]));p.append(section('Relationship Presentation',[button('Present on active diagram',presentSelectedRelationship),button('Add waypoint',addRelationshipWaypoint),button('Move last waypoint',moveLastRelationshipWaypoint),button('Delete last waypoint',deleteLastRelationshipWaypoint),button('Reset label position',resetSelectedRelationshipLabel),button('Reset routing',resetSelectedRelationshipRouting),button('Delete relationship semantics…',openSemanticRelationshipDeletion)]))}}

function requirementEditor(obj){const policy=requirementPolicy(project),options=values=>values.map(value=>[value,value]);return[
  section('Hierarchy',[field('Breadcrumb',requirementBreadcrumb(project,obj.id)),selectField('Parent',obj.ownerId,allElements(project).filter(owner=>canMoveRequirement(project,obj.id,owner.id)).map(owner=>[owner.id,qualifiedName(project,owner.id)]),ownerId=>moveRequirementFromEditor(obj,ownerId)),requirementChildrenView(obj)]),
  section('Requirement Text',[field('Requirement ID',obj.requirementId,v=>edit(obj,'requirementId',v),'blur'),selectField('Type',obj.requirementType,options(REQUIREMENT_TYPES),v=>edit(obj,'requirementType',v)),field('Text',obj.requirementText,v=>edit(obj,'requirementText',v),'blur',true)]),
  section('Source',[field('Source URI',obj.sourceUri,v=>edit(obj,'sourceUri',v),'blur'),field('Source document',obj.sourceDocument,v=>edit(obj,'sourceDocument',v),'blur'),field('Source section',obj.sourceSection,v=>edit(obj,'sourceSection',v),'blur'),field('Source revision',obj.sourceRevision,v=>edit(obj,'sourceRevision',v),'blur')]),
  section('Lifecycle',[selectField('Status',obj.lifecycleStatus,options(policy.statuses),v=>edit(obj,'lifecycleStatus',v)),selectField('Priority',obj.priority,options(policy.priorities),v=>edit(obj,'priority',v)),field('Risk',obj.risk,v=>edit(obj,'risk',v),'blur'),field('Maturity',obj.maturity,v=>edit(obj,'maturity',v),'blur'),field('Responsible role',obj.responsibleRole,v=>edit(obj,'responsibleRole',v),'blur'),field('Rationale',obj.rationale,v=>edit(obj,'rationale',v),'blur',true)]),
  section('Verification',[selectField('Method',obj.verificationMethod,options(policy.verificationMethods),v=>edit(obj,'verificationMethod',v)),field('Status',obj.verificationStatus,v=>edit(obj,'verificationStatus',v),'blur')]),
  section('Approval',[field('Approver',obj.approver,v=>edit(obj,'approver',v),'blur'),dateField('Approval date',obj.approvalDate,v=>edit(obj,'approvalDate',v)),field('Created',obj.createdDate),field('Modified',obj.modifiedDate),checkField('Suspect',obj.suspect,v=>edit(obj,'suspect',v))])
]}
function moveRequirementFromEditor(obj,ownerId,index=null){const oldOwner=obj.ownerId;checkpoint();moveRequirement(project,obj.id,ownerId,index);commitLocal({type:'move-element',elementId:obj.id,targetOwnerId:ownerId,index,expectedOwnerId:oldOwner},`Moved requirement ${obj.name}`)}
function requirementChildrenView(obj){const root=document.createElement('div');root.className='requirement-children';const heading=document.createElement('strong');heading.textContent='Child Requirements';root.append(heading);const children=requirementChildren(project,obj.id);for(const [index,child] of children.entries()){const row=document.createElement('div'),open=button(`${child.requirementId||'—'} ${child.name}`,()=>{selected={type:'element',id:child.id,nodeId:null,edgeId:null};render()}),up=button('↑',()=>moveRequirementFromEditor(child,obj.id,index-1)),down=button('↓',()=>moveRequirementFromEditor(child,obj.id,index+1));up.disabled=index===0;down.disabled=index===children.length-1;row.append(open,up,down);root.append(row)}const add=button('Create Child Requirement',()=>createOwnedElement(obj.id,'Requirement'));root.append(add);return root}
function setConnectorKind(connector,kind){const d=activeDiagram(),edge=d?.edges.find(item=>item.relationshipId===connector.id),source=d?.nodes.find(item=>item.id===edge?.sourceNodeId),target=d?.nodes.find(item=>item.id===edge?.targetNodeId),before=structuredClone(connector);if(!d||!source||!target)return log('Connector presentations are required to change connector classification.','error');checkpoint();try{configureIBDConnector(project,d,connector,source,target,kind);commitLocal({type:'replace-project',project:structuredClone(project)},`Changed connector kind to ${kind}`)}catch(error){Object.assign(connector,before);undoHistory.pop();log(error.message,'error');renderProperties()}}
function connectorItemFlowEditor(connector){let conveyedId='',direction='sourceToTarget';return[guidedReferenceField('Conveyed item','',allElements(project).filter(item=>['Block','InterfaceBlock','ValueType','DataType','PrimitiveType','Enumeration','Signal','ValueProperty','FlowProperty'].includes(item.kind)),value=>{conveyedId=value},{guidance:referenceGuidance('ItemFlow'),emptyGuidance:'No compatible conveyed classifier or property exists. Create one in the model or Common Library, then refresh the properties panel.'}),selectField('Item Flow direction',direction,[['sourceToTarget','source to target'],['targetToSource','target to source'],['bidirectional','bidirectional']],value=>{direction=value}),button('Add Item Flow',()=>{if(!conveyedId)return log('Select a conveyed semantic item.','error');try{checkpoint();const flow=createItemFlow(project,connector,conveyedId,direction);commitLocal({type:'replace-project',project:structuredClone(project)},`Added ItemFlow ${flow.id}`)}catch(error){undoHistory.pop();log(error.message,'error')}})]}
function editPresentationGeometry(node,key,value){const number=Number(value);if(!Number.isFinite(number))return renderProperties();const before={x:node.x,y:node.y,width:node.width,height:node.height};if(before[key]===number)return;checkpoint();node[key]=number;const diagram=activeDiagram();if(node.boundary&&['x','y'].includes(key))snapBoundary(node,diagram);const operation=['x','y'].includes(key)?{type:'move-node',diagramId:diagram.id,nodeId:node.id,x:node.x,y:node.y,expectedValue:{x:before.x,y:before.y}}:{type:'resize-node',diagramId:diagram.id,nodeId:node.id,width:node.width,height:node.height,expectedValue:{width:before.width,height:before.height}};commitLocal(operation,`Updated presentation ${key}`)}
function edit(obj,k,v,type='element'){const old=obj[k];if(JSON.stringify(old)===JSON.stringify(v))return;checkpoint();obj[k]=v;commitLocal({type:'set-property',targetType:type,targetId:obj.id,property:k,value:v,expectedValue:old},`Updated ${k}`)}
function editRelationshipEndpoint(obj,side,value){const old=obj[`${side}Id`];if(old===value)return;checkpoint();try{if(['ControlFlow','ObjectFlow'].includes(obj.kind)&&activeDiagram()?.diagramType==='Activity Diagram'){const d=activeDiagram(),edge=d.edges.find(item=>item.relationshipId===obj.id),nodes=d.nodes.filter(item=>item.elementId===value),node=nodes.find(item=>item.id===edge?.[`${side}NodeId`])||(nodes.length===1?nodes[0]:null);if(!node||!edge)throw Error(`${side} endpoint requires one displayed Activity presentation.`);reconnectActivityFlow(project,d,obj,side,node);edge[`${side}NodeId`]=node.id;edge[`${side}Id`]=value;edge.points=orthogonalRoute(d.nodes.find(n=>n.id===edge.sourceNodeId),d.nodes.find(n=>n.id===edge.targetNodeId))}else if(['Connector','DelegationConnector'].includes(obj.kind)){const d=activeDiagram(),edge=d?.edges.find(item=>item.relationshipId===obj.id),nodes=d?.nodes.filter(item=>item.elementId===value)||[],node=nodes.find(item=>item.id===edge?.[`${side}NodeId`])||(nodes.length===1?nodes[0]:null),otherSide=side==='source'?'target':'source',otherNode=d?.nodes.find(item=>item.id===edge?.[`${otherSide}NodeId`]),source=side==='source'?node:otherNode,target=side==='target'?node:otherNode;if(!node||!edge||!otherNode)throw Error(nodes.length>1?`${side[0].toUpperCase()+side.slice(1)} endpoint has multiple presentations. Select the intended canvas presentation before reconnecting.`:`${side[0].toUpperCase()+side.slice(1)} endpoint has no compatible displayed IBD presentation.`);const probe=structuredClone(obj);configureIBDConnector(project,d,probe,source,target,obj.connectorKind||'assembly');const descriptor=resolveIBDEndpoint(project,d,node,side);reconnectConnectorEnd(project,obj.id,side,{roleId:descriptor.semanticId,portId:descriptor.kind.endsWith('port')?descriptor.semanticId:'',endpointPath:descriptor.path,propertyPath:descriptor.propertyPath,partWithPortPath:descriptor.propertyPath.map(step=>step.propertyId),presentationId:descriptor.presentationId});edge[`${side}NodeId`]=node.id;edge[`${side}Id`]=value;edge[`${side}EndpointPath`]=[...descriptor.path];edge.points=orthogonalRoute(source,target)}else reconnectBddRelationship(project,obj.id,side,value);commitLocal({type:'set-property',targetType:'relationship',targetId:obj.id,property:`${side}Id`,value,expectedValue:old},`Reconnected ${side} endpoint`)}catch(error){undoHistory.pop();log(error.message,'error');renderProperties()}}

function commentEditor(obj){
  const body=field('Body',obj.body||obj.documentation||'',value=>{const oldBody=obj.body,oldDocumentation=obj.documentation;if(oldBody===value)return;checkpoint();obj.body=value;obj.documentation=value;commitLocal({type:'batch-operation',operations:[{type:'set-property',targetType:'element',targetId:obj.id,property:'body',value,expectedValue:oldBody},{type:'set-property',targetType:'element',targetId:obj.id,property:'documentation',value,expectedValue:oldDocumentation}]},'Updated Comment body')},'blur',true);
  const options=allElements(project).filter(item=>item.id!==obj.id).map(item=>[item.id,qualifiedName(project,item.id)]),annotation=selectField('Add annotated element','',options,value=>{if(!value||(obj.annotatedElementIds||[]).includes(value))return;edit(obj,'annotatedElementIds',[...(obj.annotatedElementIds||[]),value])}),list=document.createElement('div');list.className='annotation-list';for(const id of obj.annotatedElementIds||[]){const row=document.createElement('div'),name=document.createElement('span'),remove=button('Remove',()=>edit(obj,'annotatedElementIds',(obj.annotatedElementIds||[]).filter(value=>value!==id)));name.textContent=qualifiedName(project,id)||id;row.append(name,remove);list.append(row)}const issues=foundationIssues(project,obj);if(issues.length){const alert=document.createElement('div');alert.setAttribute('role','alert');alert.className='semantic-issues';for(const issue of issues){const row=document.createElement('div');row.dataset.field=issue.field;row.textContent=issue.message;alert.append(row)}list.append(alert)}return section('Comment Annotation',[body,annotation,list])
}
function valueTypeEditor(obj){
  const children=[
    selectField('Unit',obj.unitRef||'',allElements(project).filter(e=>e.kind==='Unit').map(e=>[e.id,qualifiedName(project,e.id)]),v=>edit(obj,'unitRef',v)),
    selectField('Quantity kind',obj.quantityKindRef||'',allElements(project).filter(e=>e.kind==='QuantityKind').map(e=>[e.id,qualifiedName(project,e.id)]),v=>edit(obj,'quantityKindRef',v))
  ];
  const issues=semanticIssues(project,obj);if(issues.length){const box=document.createElement('div');box.className='semantic-issues';box.setAttribute('role','alert');for(const issue of issues){const row=document.createElement('div');row.className=`semantic-issue ${issue.severity}`;row.dataset.field=issue.field||'';row.textContent=issue.message;box.append(row)}children.push(box)}
  return section('Value Type Semantics',children)
}
function semanticFeatureEditor(obj){
  const children=[guidedReferenceField('Type',obj.typeRef||'',allElements(project).filter(e=>e.id!==obj.id&&validTypeKinds(obj.kind).includes(e.kind)),v=>edit(obj,'typeRef',v),{guidance:referenceGuidance(obj.kind),emptyGuidance:`${referenceGuidance(obj.kind)} No compatible type currently exists.`})];
  const mult=document.createElement('div');mult.className='multiplicity-editor';
  const lower=compactField('Lower',obj.multiplicityLower??'1'),upper=compactField('Upper',obj.multiplicityUpper??'1');
  const commitMultiplicity=()=>{const before={multiplicity:obj.multiplicity,multiplicityLower:obj.multiplicityLower,multiplicityUpper:obj.multiplicityUpper},result=setMultiplicity(obj,lower.input.value,upper.input.value);if(!result.ok){lower.input.setCustomValidity(result.message);lower.input.reportValidity();Object.assign(obj,before);return}lower.input.setCustomValidity('');checkpoint();commitLocal({type:'replace-project',project:structuredClone(project),expectedValue:before},`Updated multiplicity to ${result.value}`)};
  lower.input.addEventListener('blur',event=>{if(event.relatedTarget!==upper.input)commitMultiplicity()});upper.input.addEventListener('blur',commitMultiplicity);mult.append(lower.root,upper.root);children.push(mult);
  if(supportsDirection(obj))children.push(selectField('Direction',obj.direction||'inout',[['in','in'],['out','out'],['inout','inout'],...(obj.kind==='Parameter'?[['return','return']]:[])],v=>edit(obj,'direction',v)));
  if(supportsDefault(obj))children.push(field('Default value',obj.defaultValue||'',v=>edit(obj,'defaultValue',v),'blur'));
  if(obj.kind==='ValueProperty'||obj.kind==='FlowProperty'){
    children.push(guidedReferenceField('Unit',obj.unitRef||'',allElements(project).filter(e=>e.kind==='Unit'),v=>edit(obj,'unitRef',v),{guidance:'Select an existing Unit from the model library. The stable ID is stored.'}));
    children.push(guidedReferenceField('Quantity kind',obj.quantityKindRef||'',allElements(project).filter(e=>e.kind==='QuantityKind'),v=>edit(obj,'quantityKindRef',v),{guidance:'Select an existing QuantityKind from the model library. The stable ID is stored.'}));
  }
  if(supportsConjugation(obj))children.push(checkField('Conjugated',Boolean(obj.isConjugated),v=>edit(obj,'isConjugated',v)));if(['PartProperty','ReferenceProperty','ValueProperty','FlowProperty','ConstraintProperty'].includes(obj.kind)){const related=allElements(project).filter(item=>item.id!==obj.id&&item.kind===obj.kind);children.push(guidedReferenceField('Redefined property',obj.redefinedPropertyIds?.[0]||'',related,v=>edit(obj,'redefinedPropertyIds',v?[v]:[]),{guidance:'Select a compatible existing property to redefine. The property stable ID is stored and survives rename.',emptyGuidance:'No compatible property is available to redefine in the current model.'}));children.push(guidedReferenceField('Subsetted property',obj.subsettedPropertyIds?.[0]||'',related,v=>edit(obj,'subsettedPropertyIds',v?[v]:[]),{guidance:'Select a compatible existing property to subset. The property stable ID is stored and independently validated.',emptyGuidance:'No compatible property is available to subset in the current model.'}))}
  const issues=semanticIssues(project,obj);if(issues.length){const box=document.createElement('div');box.className='semantic-issues';box.setAttribute('role','alert');for(const issue of issues){const row=document.createElement('div');row.className=`semantic-issue ${issue.severity}`;row.dataset.field=issue.field||'';row.textContent=issue.message;box.append(row)}children.push(box)}
  return section('Semantic Feature',children)
}
function compactField(label,value){const root=document.createElement('label');root.className='field compact-field';const span=document.createElement('span');span.textContent=label;const input=document.createElement('input');input.value=value??'';input.inputMode='numeric';root.append(span,input);return{root,input}}
function checkField(label,value,onchange){const r=document.createElement('label');r.className='check-field';const i=document.createElement('input');i.type='checkbox';i.checked=Boolean(value);i.onchange=e=>onchange(e.target.checked);r.append(i,document.createTextNode(` ${label}`));return r}

function compartmentEditor(e,node=null){
  ensureCompartmentState(e);const s=section('Compartments',[]),diagram=activeDiagram(),presentationVisibility=node?.presentationOptions?.compartmentVisibility||{};
  const hint=document.createElement('div');hint.className='property-hint';hint.textContent='Compartments are generated from owned semantic elements. Resize the symbol to reveal more rows.';s.append(hint);
  for(const name of ensureCompartmentState(e)){
    const def=COMPARTMENT_DEFINITIONS[name]||{label:name,kinds:[]},row=document.createElement('div');row.className='compartment-control';
    const header=document.createElement('div');header.className='compartment-control-header';const label=document.createElement('label'),cb=document.createElement('input');cb.type='checkbox';cb.checked=presentationVisibility[name]??e.compartmentVisibility[name];cb.onchange=()=>{if(node){const old=presentationVisibility[name];node.presentationOptions=node.presentationOptions||{};node.presentationOptions.compartmentVisibility=node.presentationOptions.compartmentVisibility||{};node.presentationOptions.compartmentVisibility[name]=cb.checked;commitLocal({type:'set-presentation-compartment-visibility',diagramId:diagram.id,nodeId:node.id,name,value:cb.checked,expectedValue:old},`Changed ${def.label} visibility for this presentation`)}else{const old=e.compartmentVisibility[name];e.compartmentVisibility[name]=cb.checked;commitLocal({type:'set-compartment-visibility',elementId:e.id,name,value:cb.checked,expectedValue:old},`Changed ${def.label} default visibility`)}};label.append(cb,document.createTextNode(` ${def.label}`));header.append(label);
    const addKind=compartmentAddKind(name);if(addKind){const add=document.createElement('button');add.type='button';add.className='mini-button';add.textContent=`+ ${addKind}`;add.onclick=()=>addOwnedFeature(e,addKind);header.append(add)}
    const list=document.createElement('div');list.className='compartment-semantic-list';const items=visibleCompartmentItems(e,name);if(!items.length)list.textContent='No owned elements';else for(const item of items){const line=document.createElement('button');line.type='button';line.className='compartment-item';const issues=semanticIssues(project,item);if(issues.some(x=>x.severity==='error'))line.classList.add('has-error');else if(issues.length)line.classList.add('has-warning');line.textContent=formatFeature(project,item);line.title=issues.map(x=>x.message).join('\n');line.onclick=()=>{selected={type:'element',id:item.id,nodeId:activeDiagram()?.nodes.find(n=>n.elementId===item.id)?.id||null,edgeId:null};render()};list.append(line);if(['EnumerationLiteral','FlowProperty'].includes(item.kind)){const order=items.indexOf(item),up=button('Move up',()=>{checkpoint();reorderOwnedFoundation(project,item.id,order-1);commitLocal({type:'replace-project',project:structuredClone(project)},`Reordered ${item.kind}`)}),down=button('Move down',()=>{checkpoint();reorderOwnedFoundation(project,item.id,order+1);commitLocal({type:'replace-project',project:structuredClone(project)},`Reordered ${item.kind}`)});up.className=down.className='mini-button';up.disabled=order===0;down.disabled=order===items.length-1;list.append(up,down)}}
    const legacy=(e.compartments[name]||[]).filter(x=>typeof x==='string');if(legacy.length){const details=document.createElement('details');const summary=document.createElement('summary');summary.textContent='Imported display text';const ta=document.createElement('textarea');ta.value=legacy.join('\n');ta.onblur=()=>{const old=structuredClone(e.compartments[name]||[]),value=ta.value.split('\n').map(x=>x.trim()).filter(Boolean);e.compartments[name]=value;commitLocal({type:'set-compartment',elementId:e.id,name,value,expectedValue:old},`Updated imported ${def.label} text`)};details.append(summary,ta);list.append(details)}
    row.append(header,list);s.append(row)
  }return s
}
function visibleCompartmentItems(e,name){const kinds=COMPARTMENT_DEFINITIONS[name]?.kinds||[];if(name==='providedInterfaces')return (e.providedInterfaceIds||[]).map(id=>findElement(project,id)).filter(Boolean);if(name==='requiredInterfaces')return (e.requiredInterfaceIds||[]).map(id=>findElement(project,id)).filter(Boolean);return project.elements.filter(x=>x.ownerId===e.id&&kinds.includes(x.kind))}
async function addOwnedFeature(owner,kind){const feature=defaultElement(kind,owner.id);feature.name=kind.replace(/Property$/,'').replace(/([a-z])([A-Z])/g,'$1 $2').toLowerCase();if(isTypedFeature(feature)){const candidates=allElements(project).filter(item=>item.id!==feature.id&&validTypeKinds(kind).includes(item.kind)),typeId=await guidedReferenceDialog({title:`Create ${kind}`,label:`${kind} type`,elements:candidates,guidance:referenceGuidance(kind)});if(!typeId){log(`${kind} creation cancelled; no semantic record was created.`,'ok');return}const type=findElement(project,typeId);if(!type||!validTypeKinds(kind).includes(type.kind)){log(`${kind} creation rejected because its selected type is no longer compatible.`,'error');return}feature.typeRef=type.id}checkpoint();project.elements.push(feature);const d=activeDiagram();let node=null;if(d&&d.contextId===owner.id&&allowed(d.diagramType).elements.includes(kind)){node={id:uid('node'),elementId:feature.id,x:owner.kind==='Block'?80:120,y:80,width:widthFor(kind),height:heightFor(kind),rotation:0};if(['ProxyPort','FullPort'].includes(kind))Object.assign(node,{width:18,height:18,boundary:true});d.nodes.push(node)}selected={type:'element',id:feature.id,nodeId:node?.id||null,edgeId:null};commitLocal({type:'create-element',element:structuredClone(feature),diagramId:d?.id,node:node?structuredClone(node):null},`Created ${kind} in ${owner.name}`)}
function section(title,children=[]){const s=document.createElement('div');s.className='section';s.innerHTML=`<h3>${title}</h3>`;for(const c of children)s.append(c);return s}function field(label,value,onchange,event='input',area=false){const r=document.createElement('label');r.className='field';r.innerHTML=`<span>${label}</span>`;const i=document.createElement(area?'textarea':'input');i.value=value??'';if(!onchange)i.readOnly=true;else i.addEventListener(event,e=>onchange(e.target.value));r.append(i);return r}function dateField(label,value,onchange){const r=field(label,String(value||'').slice(0,10),onchange,'change');r.querySelector('input').type='date';return r}function selectField(label,value,opts,onchange){const r=document.createElement('label');r.className='field';r.innerHTML=`<span>${label}</span>`;const s=document.createElement('select');const blank=document.createElement('option');blank.value='';blank.textContent='—';s.append(blank);for(const [v,n] of opts){const o=document.createElement('option');o.value=v;o.textContent=n;s.append(o)}s.value=value||'';s.onchange=e=>onchange(e.target.value);r.append(s);return r}function button(label,fn){const b=document.createElement('button');b.textContent=label;b.onclick=fn;return b}function stableIdShort(id){return String(id||'').length>12?`${String(id).slice(0,8)}…`:String(id||'')}
function semanticOption(element){const owner=element.ownerId?qualifiedName(project,element.ownerId):project.name,qname=qualifiedName(project,element.id)||element.name||element.id,label=`${element.name||element.id} — «${element.kind}» — ${owner||'model'} — ${stableIdShort(element.id)}`;return{id:element.id,label,search:`${element.name||''} ${qname} ${element.kind||''} ${element.stereotype||''} ${element.id||''} ${element.externalId||''}`.toLowerCase(),title:`${qname} — ${element.kind} — ${element.id}`}}
function guidedReferenceField(label,value,elements,onchange,{guidance='',emptyGuidance='',refresh=null}={}){const root=document.createElement('div');root.className='field guided-reference';const id=`guided-${uid('field')}`,caption=document.createElement('span');caption.className='guided-reference-label';caption.textContent=label;const search=document.createElement('input');search.id=id;search.type='search';search.setAttribute('aria-label',`Search ${label}`);search.setAttribute('role','combobox');search.setAttribute('aria-autocomplete','list');search.setAttribute('aria-expanded','true');search.placeholder='Search by name, qualified path, type, or stable ID';const select=document.createElement('select');select.setAttribute('aria-label',label);select.size=Math.min(6,Math.max(2,elements.length+1));const help=document.createElement('div');help.className='property-hint';help.id=`${id}-help`;search.setAttribute('aria-describedby',help.id);const candidates=elements.map(semanticOption);let selected=value||'';const renderOptions=()=>{const term=search.value.trim().toLowerCase(),visible=candidates.filter(item=>!term||item.search.includes(term));select.replaceChildren();const blank=document.createElement('option');blank.value='';blank.textContent=visible.length?'Select a compatible semantic record':'No compatible records';select.append(blank);for(const item of visible){const option=document.createElement('option');option.value=item.id;option.textContent=item.label;option.title=item.title;select.append(option)}select.value=visible.some(item=>item.id===selected)?selected:'';help.textContent=visible.length?guidance:(emptyGuidance||`${guidance} No compatible records currently exist.`)};search.oninput=renderOptions;search.onkeydown=event=>{if(event.key==='ArrowDown'){event.preventDefault();select.focus();select.selectedIndex=Math.min(1,select.options.length-1)}else if(event.key==='Enter'){const matches=[...select.options].filter(option=>option.value);if(matches.length===1){event.preventDefault();select.value=matches[0].value;select.dispatchEvent(new Event('change',{bubbles:true}))}}};select.onchange=()=>{selected=select.value;onchange(selected)};root.append(caption,search,select,help);if(refresh){const action=button('Refresh candidates',()=>refresh());action.type='button';root.append(action)}renderOptions();return root}
function referenceGuidance(kind){return{PartProperty:'Select an existing Block that defines the part’s type. Create the Block in the owning model package from the BDD palette or containment tree.',ReferenceProperty:'Select an existing compatible classifier. Create the classifier in the owning model package before creating this reference.',ValueProperty:'Select an existing ValueType, DataType, PrimitiveType, or Enumeration from the model or Common Library.',FlowProperty:'Select an existing compatible classifier or Signal used by this flow.',ConstraintProperty:'Select an existing ConstraintBlock. Create it from the BDD palette or containment tree.',ProxyPort:'Select an existing compatible InterfaceBlock. If none are available, create an InterfaceBlock from the BDD palette or containment tree.',FullPort:'Select an existing compatible Block, InterfaceBlock, or Signal. Create the type in the owning model package before creating this port.',DelegationConnector:'Select a compatible boundary port and an internal property or port owned within this IBD’s context Block.',ItemFlow:'Select an existing classifier or property that the connector conveys. The reference is stored by stable ID.'}[kind]||'Select a compatible existing semantic record. The visible label is descriptive; the stable ID is stored.'}
function guidedReferenceDialog({title,label,elements,guidance}){return new Promise(resolve=>{const dialog=document.createElement('dialog'),form=document.createElement('form');form.method='dialog';const heading=document.createElement('h2');heading.textContent=title;let selectedId='';const selector=guidedReferenceField(label,'',elements,id=>{selectedId=id;confirm.disabled=!id},{guidance,emptyGuidance:`${guidance} Create the prerequisite in its required owner or package, then reopen or refresh this workflow.`}),actions=document.createElement('div'),cancel=button('Cancel',()=>dialog.close('cancel')),confirm=button('Use selected record',()=>dialog.close('confirm'));cancel.type=confirm.type='button';confirm.disabled=true;actions.append(cancel,confirm);form.append(heading,selector,actions);dialog.append(form);document.body.append(dialog);dialog.addEventListener('close',()=>{const result=dialog.returnValue==='confirm'?selectedId:null;dialog.remove();resolve(result)},{once:true});dialog.showModal();selector.querySelector('input').focus()})}
function createOrOpenChild(e){let type=e.kind==='Block'?'Internal Block Diagram':e.kind==='Activity'?'Activity Diagram':e.kind==='StateMachine'?'State Machine Diagram':e.kind==='Interaction'?'Sequence Diagram':null;if(!type){log('This element type does not own a child diagram.','warn');return}let d=project.diagrams.find(x=>x.contextId===e.id&&x.diagramType===type);if(!d){checkpoint();d={id:uid('diagram'),externalId:uid('DGM').toUpperCase(),name:`${e.name} ${DIAGRAMS[type].abbreviation}`,diagramType:type,ownerId:e.ownerId,contextId:e.id,nodes:[],edges:[],documentation:''};project.diagrams.push(d);normalizeIBDProject(project);commitLocal({type:'create-diagram',diagram:structuredClone(d)},`Created ${type}`)}project.activeDiagramId=d.id;render()}
function openChildFor(id){const d=project.diagrams.find(x=>x.contextId===id);if(d){project.activeDiagramId=d.id;render()}else log('No child diagram exists.','warn')}
function navigateDiagram(id){const current=project.activeDiagramId;if(current&&current!==id){navigationBack.push(current);navigationForward=[]}project.activeDiagramId=id;view='model';render();publishCurrentAwareness()}
function navigationGoBack(){if(!navigationBack.length)return;const current=project.activeDiagramId,id=navigationBack.pop();if(current)navigationForward.push(current);project.activeDiagramId=id;render()}
function navigationGoForward(){if(!navigationForward.length)return;const current=project.activeDiagramId,id=navigationForward.pop();if(current)navigationBack.push(current);project.activeDiagramId=id;render()}
function openChildForSelected(){if(selected.type==='element')createOrOpenChild(findElement(project,selected.id))}

function selectedDiagramNodes(){const d=activeDiagram();return d?d.nodes.filter(n=>selectedNodeIds.has(n.id)):[]}
function commitNodeLayout(message){checkpoint();commitLocal({type:'replace-project',project:structuredClone(project)},message)}
function alignSelected(mode){const nodes=selectedDiagramNodes();if(nodes.length<2){log('Select at least two diagram elements.','warn');return}checkpoint();if(mode==='left'){const v=Math.min(...nodes.map(n=>n.x));nodes.forEach(n=>n.x=v)}else if(mode==='right'){const v=Math.max(...nodes.map(n=>n.x+n.width));nodes.forEach(n=>n.x=v-n.width)}else if(mode==='top'){const v=Math.min(...nodes.map(n=>n.y));nodes.forEach(n=>n.y=v)}else if(mode==='bottom'){const v=Math.max(...nodes.map(n=>n.y+n.height));nodes.forEach(n=>n.y=v-n.height)}else if(mode==='hcenter'){const v=nodes.reduce((a,n)=>a+n.x+n.width/2,0)/nodes.length;nodes.forEach(n=>n.x=v-n.width/2)}else if(mode==='vcenter'){const v=nodes.reduce((a,n)=>a+n.y+n.height/2,0)/nodes.length;nodes.forEach(n=>n.y=v-n.height/2)}commitLocal({type:'replace-project',project:structuredClone(project)},`Aligned ${nodes.length} symbols`)}
function distributeSelected(axis){const nodes=selectedDiagramNodes();if(nodes.length<3){log('Select at least three diagram elements.','warn');return}checkpoint();const sorted=[...nodes].sort((a,b)=>axis==='h'?a.x-b.x:a.y-b.y);if(axis==='h'){const first=sorted[0].x,last=sorted.at(-1).x+sorted.at(-1).width,total=sorted.reduce((a,n)=>a+n.width,0),gap=(last-first-total)/(sorted.length-1);let x=first;for(const n of sorted){n.x=x;x+=n.width+gap}}else{const first=sorted[0].y,last=sorted.at(-1).y+sorted.at(-1).height,total=sorted.reduce((a,n)=>a+n.height,0),gap=(last-first-total)/(sorted.length-1);let y=first;for(const n of sorted){n.y=y;y+=n.height+gap}}commitLocal({type:'replace-project',project:structuredClone(project)},`Distributed ${nodes.length} symbols`)}
function nudgeSelection(dx,dy){const d=activeDiagram(),nodes=selectedDiagramNodes();if(!nodes.length)return;checkpoint();const before=nodes.map(node=>({id:node.id,x:node.x,y:node.y}));nodes.forEach(node=>{node.x+=dx;node.y+=dy;if(node.boundary)snapBoundary(node,d)});commitLocal({type:'batch-operation',operations:nodes.map((node,index)=>({type:'move-node',diagramId:d.id,nodeId:node.id,x:node.x,y:node.y,expectedValue:{x:before[index].x,y:before[index].y}}))},`Moved ${nodes.length} symbol${nodes.length===1?'':'s'}`)}

function activeRelationshipEdge(){const d=activeDiagram();return d?.edges.find(edge=>edge.id===selected.edgeId||edge.relationshipId===selected.id)||null}
function commitRelationshipPresentationOperation(type,mutate,extra={},message=type){const d=activeDiagram(),edge=activeRelationshipEdge();if(!d||!edge)return log('Select a relationship presentation first.','warn');checkpoint();mutate(edge);commitLocal({type,diagramId:d.id,edgeId:edge.id,relationshipId:edge.relationshipId,...extra},message);renderCanvas();renderProperties()}
function addRelationshipWaypoint(){const edge=activeRelationshipEdge(),d=activeDiagram(),r=edge&&findRelationship(project,edge.relationshipId),geometry=r&&edgeGeometry(d,edge,r);if(!geometry)return log('A resolved relationship presentation is required.','warn');const point=midpointAlong(geometry.all),waypoint={id:uid('waypoint'),x:point.x,y:point.y};commitRelationshipPresentationOperation('add-waypoint',item=>{addWaypoint(item,waypoint);item.points=[...(item.waypoints||[]).map(({x,y})=>({x,y}))];item.routingMode='manual'},{waypoint},'Added relationship waypoint')}
function moveLastRelationshipWaypoint(){const edge=activeRelationshipEdge(),waypoint=edge?.waypoints?.at(-1);if(!waypoint)return log('No waypoint is available to move.','warn');const point={x:waypoint.x+20,y:waypoint.y+20};commitRelationshipPresentationOperation('move-waypoint',item=>{moveWaypoint(item,waypoint.id,point);item.points=item.waypoints.map(({x,y})=>({x,y}))},{waypointId:waypoint.id,point},'Moved relationship waypoint')}
function deleteLastRelationshipWaypoint(){const edge=activeRelationshipEdge(),waypoint=edge?.waypoints?.at(-1);if(!waypoint)return log('No waypoint is available to delete.','warn');commitRelationshipPresentationOperation('remove-waypoint',item=>{removeWaypoint(item,waypoint.id);item.points=item.waypoints.map(({x,y})=>({x,y}))},{waypointId:waypoint.id},'Deleted relationship waypoint')}
function resetSelectedRelationshipLabel(){commitRelationshipPresentationOperation('reset-relationship-label',edge=>resetRelationshipLabel(edge),{},'Reset relationship label position')}
function resetSelectedRelationshipRouting(){commitRelationshipPresentationOperation('reset-relationship-routing',edge=>{resetRouting(edge);edge.points=[]},{},'Restored default relationship routing')}
function presentSelectedRelationship(){const d=activeDiagram(),relationship=findRelationship(project,selected.id);if(!d||!relationship)return log('Select a semantic relationship and an active diagram.','warn');try{checkpoint();const result=presentExistingRelationship(project,d.id,relationship.id);if(!result.created){undoHistory.pop();return log('This semantic relationship is already presented on the active diagram.','warn')}selected={type:'relationship',id:relationship.id,nodeId:null,edgeId:result.edge.id};commitLocal({type:'create-relationship-presentation',diagramId:d.id,relationshipId:relationship.id,edge:structuredClone(result.edge)},`Presented ${relationship.name||relationship.kind} on ${d.name}`)}catch(error){log(error.message,'error')}}
function semanticDeleteImpact(relationship){const presentations=project.diagrams.flatMap(d=>d.edges.filter(e=>e.relationshipId===relationship.id).map(e=>({diagram:d,edge:e}))),flows=project.relationships.filter(r=>r.connectorId===relationship.id),ends=relationship.connectorEnds||[];return{presentations,flows,ends}}
function openSemanticRelationshipDeletion(){const relationship=findRelationship(project,selected.id);if(!relationship)return log('Select a semantic relationship first.','warn');const impact=semanticDeleteImpact(relationship),dialog=document.createElement('dialog');dialog.setAttribute('aria-labelledby','relationship-delete-title');dialog.innerHTML=`<form method="dialog"><h2 id="relationship-delete-title">Delete semantic relationship</h2><p><strong>${esc(relationship.name||relationship.kind)}</strong> will be deleted.</p><ul><li>${impact.presentations.length} diagram presentation(s)</li><li>${impact.ends.length} ConnectorEnd record(s)</li><li>${impact.flows.length} associated ItemFlow record(s)</li><li>Endpoint elements will be preserved</li></ul><p class="semantic-delete-status" role="status">Review the impact before confirming.</p><div><button value="cancel">Cancel</button><button value="confirm" class="danger">Delete relationship semantics</button></div></form>`;document.body.append(dialog);const opener=document.activeElement;dialog.addEventListener('close',()=>{if(dialog.returnValue==='confirm'){try{checkpoint();const snapshot=deleteSemanticRelationship(project,relationship.id);commitLocal({type:'delete-relationship',relationshipId:relationship.id,snapshot},'Deleted semantic relationship and affected presentations');selected={type:null,id:null,nodeId:null,edgeId:null}}catch(error){dialog.querySelector('.semantic-delete-status').textContent=`Deletion failed: ${error.message}`;log(`Deletion failed: ${error.message}`,'error');return}}dialog.remove();opener?.focus()});dialog.addEventListener('cancel',event=>{event.preventDefault();dialog.close('cancel')});dialog.showModal();dialog.querySelector('button[value="cancel"]').focus()}
function deleteSelected(){const d=activeDiagram();if(selectedNodeIds.size>1){const ids=[...selectedNodeIds];checkpoint();for(const id of ids)d.nodes=d.nodes.filter(node=>node.id!==id);const removed=new Set(ids);d.edges=d.edges.filter(edge=>!removed.has(edge.sourceNodeId)&&!removed.has(edge.targetNodeId));commitLocal({type:'batch-operation',operations:ids.map(nodeId=>({type:'remove-presentation',diagramId:d.id,nodeId}))},`Removed ${ids.length} diagram presentations`);selectedNodeIds.clear();selected={type:null,id:null,nodeId:null,edgeId:null};return}if(selected.nodeId){checkpoint();d.nodes=d.nodes.filter(x=>x.id!==selected.nodeId);commitLocal({type:'remove-presentation',diagramId:d.id,nodeId:selected.nodeId},'Removed diagram presentation');selectedNodeIds.clear();selected={type:null,id:null,nodeId:null,edgeId:null};return}if(selected.edgeId){checkpoint();d.edges=d.edges.filter(edge=>edge.id!==selected.edgeId);commitLocal({type:'remove-edge-presentation',diagramId:d.id,edgeId:selected.edgeId},'Removed relationship presentation');selected={type:null,id:null,nodeId:null,edgeId:null};return}if(selected.type==='relationship'){if(!confirm('Delete this semantic relationship from the model and every diagram?'))return;checkpoint();project.relationships=project.relationships.filter(x=>x.id!==selected.id);for(const x of project.diagrams)x.edges=x.edges.filter(e=>e.relationshipId!==selected.id);commitLocal({type:'delete-relationship',relationshipId:selected.id},'Deleted semantic relationship');selected={type:null,id:null};return}if(selected.type==='element'&&selected.id!==project.root.id){if(!confirm('Delete this semantic element, its owned elements, relationships, and every presentation?'))return;checkpoint();const id=selected.id;applyDeleteLocal(id);commitLocal({type:'delete-element',elementId:id},'Deleted semantic model element');selected={type:null,id:null}}}
function applyDeleteLocal(id){const ids=new Set([id]);let changed=true;while(changed){changed=false;for(const e of project.elements)if(ids.has(e.ownerId)&&!ids.has(e.id)){ids.add(e.id);changed=true}}project.elements=project.elements.filter(e=>!ids.has(e.id));project.relationships=project.relationships.filter(r=>!ids.has(r.sourceId)&&!ids.has(r.targetId));for(const d of project.diagrams){d.nodes=d.nodes.filter(n=>!ids.has(n.elementId));const valid=new Set(project.relationships.map(r=>r.id));d.edges=d.edges.filter(e=>valid.has(e.relationshipId))}}
function reuse(){const d=activeDiagram(),term=prompt('Search existing element by name or ID','');if(term===null)return;if(isIBD(d)){const ownerNode=d.nodes.find(node=>node.id===selected.nodeId)||d.nodes.find(node=>node.isContextBoundary),propertyCandidates=(ownerNode&&!ownerNode.isContextBoundary?availableNestedProperties(project,d,ownerNode):availableIBDProperties(project,d)).map(item=>item.element),portCandidates=availableIBDPorts(project,d,ownerNode),matches=[...propertyCandidates,...portCandidates].filter(e=>`${e.name} ${e.externalId} ${e.kind}`.toLowerCase().includes(term.toLowerCase()));if(!matches.length){log('No owned or inherited IBD property/port matched.','warn');return}const e=matches[0];try{checkpoint();const n=['PartProperty','ReferenceProperty'].includes(e.kind)?(ownerNode&&!ownerNode.isContextBoundary?createNestedPropertyPresentation(project,d,ownerNode,e.id,{relativeX:24,relativeY:42}):createIBDPropertyPresentation(project,d,e.id,{x:100+d.nodes.length*30,y:100+d.nodes.length*25})):createIBDPortPresentation(project,d,e.id,ownerNode);selected={type:'element',id:e.id,nodeId:n.id,edgeId:null};commitLocal({type:'add-presentation',diagramId:d.id,node:structuredClone(n)},`Placed existing ${e.kind} ${e.name}`)}catch(error){log(error.message,'error')}return}const matches=allElements(project).filter(e=>`${e.name} ${e.externalId} ${e.kind}`.toLowerCase().includes(term.toLowerCase())&&allowed(d.diagramType).elements.includes(e.kind));if(!matches.length){log('No valid reusable element found.','warn');return}const e=matches[0];if(d.nodes.some(n=>n.elementId===e.id)){log('Element is already shown on this diagram.','warn');return}const n={id:uid('node'),elementId:e.id,x:100+d.nodes.length*30,y:100+d.nodes.length*25,width:widthFor(e.kind),height:heightFor(e.kind)};checkpoint();d.nodes.push(n);commitLocal({type:'add-presentation',diagramId:d.id,node:structuredClone(n)},`Reused ${e.name}`)}
function search(){const q=$('searchBox').value.trim().toLowerCase();if(!q)return;const e=allElements(project).find(x=>`${x.name} ${x.externalId} ${x.stereotype} ${x.kind}`.toLowerCase().includes(q));if(e){selected={type:'element',id:e.id,nodeId:null,edgeId:null};view='model';render();return}log('No model element matched.','warn')}
function createDiagram(){const dialog=$('newDiagramDialog'),typeSelect=$('newDiagramType'),nameInput=$('newDiagramName');typeSelect.replaceChildren(...DIAGRAM_TYPES.map(type=>{const option=document.createElement('option');option.value=type;option.textContent=type;return option}));typeSelect.value='Block Definition Diagram';nameInput.value='New Diagram';$('newDiagramError').textContent='';dialog.showModal();queueMicrotask(()=>typeSelect.focus())}
function confirmCreateDiagram(){const type=$('newDiagramType').value,name=$('newDiagramName').value.trim(),owner=selected.type==='element'?selected.id:project.root.id,error=$('newDiagramError');if(!DIAGRAM_TYPES.includes(type)){error.textContent='Select a valid diagram type.';return false}if(!name){error.textContent='Enter a diagram name.';return false}if(type==='Internal Block Diagram'&&findElement(project,owner)?.kind!=='Block'){error.textContent='Select a Block before creating an Internal Block Diagram.';return false}const d={id:uid('diagram'),externalId:uid('DGM').toUpperCase(),name,diagramType:type,ownerId:owner,contextId:owner,nodes:[],edges:[],documentation:'',headerText:''};checkpoint();project.diagrams.push(d);normalizeIBDProject(project);project.activeDiagramId=d.id;selected={type:'diagram',id:d.id,nodeId:null,edgeId:null};commitLocal({type:'create-diagram',diagram:structuredClone(d)},`Created ${type} ${name}`);return true}
function defs(svg){const d=el('defs');d.innerHTML='<marker id="open" markerWidth="12" markerHeight="12" refX="11" refY="6" orient="auto"><path d="M1,1 L11,6 L1,11" fill="none" stroke="#263746"/></marker><marker id="messageFilled" markerWidth="12" markerHeight="12" refX="11" refY="6" orient="auto"><path d="M1,1 L11,6 L1,11 Z" fill="#263746" stroke="#263746"/></marker><marker id="triangle" markerWidth="14" markerHeight="14" refX="13" refY="7" orient="auto"><path d="M1,1 L13,7 L1,13 Z" fill="white" stroke="#263746"/></marker><marker id="diamond" markerWidth="16" markerHeight="12" refX="15" refY="6" orient="auto"><path d="M1,6 L7,1 L15,6 L7,11 Z" fill="white" stroke="#263746"/></marker><marker id="diamondFilled" markerWidth="16" markerHeight="12" refX="15" refY="6" orient="auto"><path d="M1,6 L7,1 L15,6 L7,11 Z" fill="#263746" stroke="#263746"/></marker>';svg.append(d)}function el(n,a={}){const x=document.createElementNS('http://www.w3.org/2000/svg',n);for(const[k,v]of Object.entries(a))if(v!==''&&v!=null)x.setAttribute(k,v);return x}function text(p,v,x,y,anchor='start',cls=''){const t=el('text',{x,y,'text-anchor':anchor,class:cls});t.textContent=v;p.append(t)}function wrap(p,v,x,y,w,h){let line='',row=0;for(const word of String(v).split(/\s+/)){const test=`${line} ${word}`.trim();if(test.length*6>w&&line){text(p,line,x,y+row*h);line=word;row++}else line=test}if(line)text(p,line,x,y+row*h)}function point(svg,e){const r=svg.getBoundingClientRect();return{x:(e.clientX-r.left)/zoom,y:(e.clientY-r.top)/zoom}}function esc(s){return String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}function widthFor(k){return defaultPresentationSize(k).width}function heightFor(k){return defaultPresentationSize(k).height}function download(n,d,t){const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([d],{type:t}));a.download=n;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000)}

function updateActionStates(){
  const connected=connectionState==='Connected';
  const hasSelection=Boolean(selected?.id||selected?.nodeId||selected?.edgeId);
  const hasElement=selected?.type==='element'&&Boolean(selected.id);
  const selectedElement=hasElement?findElement(project,selected.id):null;
  const childCapable=['Block','Activity','StateMachine','Interaction'].includes(selectedElement?.kind);
  const set=(id,disabled,reason='')=>{const b=$(id);if(!b)return;b.disabled=Boolean(disabled);b.title=disabled&&reason?reason:(b.dataset.defaultTitle||b.title||'');if(!b.dataset.defaultTitle)b.dataset.defaultTitle=b.title||''};
  set('undo',!undoHistory.length,'No local change to undo');set('redo',!future.length,'No local change to redo');
  set('deleteSelected',!hasSelection,'Select a presentation, relationship, or model element');
  set('openChild',!childCapable,'Select a Block, Activity, StateMachine, or Interaction');
  set('lockSelected',!connected||!hasElement,!connected?'Connect to a collaboration room first':'Select a model element');
  set('unlockSelected',!connected||!hasElement,!connected?'Connect to a collaboration room first':'Select a model element');
  for(const id of ['commitModel','newBranch','mergeBranch'])set(id,!connected,'Connect to a collaboration room first');
  $('branchSelect').disabled=!connected;
  const d=activeDiagram();set('routeEdges',!d||!d.edges?.length,'The active diagram has no relationships to route');
  set('reuseElement',!d,'Open a diagram first');set('newDiagram',false);set('validateModel',false);
  document.querySelectorAll('.menu-popover button[data-target]').forEach(menuButton=>{const target=$(menuButton.dataset.target);menuButton.disabled=Boolean(target?.disabled);menuButton.title=target?.title||''});
}
function wireMenus(){
  document.querySelectorAll('.menu-popover button[data-target]').forEach(button=>button.addEventListener('click',()=>{const target=$(button.dataset.target);button.closest('details')?.removeAttribute('open');if(target&&!target.disabled)target.click()}));
  document.addEventListener('click',event=>{for(const menu of document.querySelectorAll('.app-menu[open]'))if(!menu.contains(event.target))menu.removeAttribute('open');if(treeContextMenu&&!treeContextMenu.contains(event.target))closeTreeContextMenu()});
}


$('alignLeft').onclick=()=>alignSelected('left');$('alignRight').onclick=()=>alignSelected('right');$('alignTop').onclick=()=>alignSelected('top');$('alignBottom').onclick=()=>alignSelected('bottom');$('alignHCenter').onclick=()=>alignSelected('hcenter');$('alignVCenter').onclick=()=>alignSelected('vcenter');$('distributeH').onclick=()=>distributeSelected('h');$('distributeV').onclick=()=>distributeSelected('v');
$('modelTab').onclick=()=>{view='model';setTab('modelTab');renderTree()};$('diagramTab').onclick=()=>{view='diagrams';setTab('diagramTab');renderTree()};$('xrefTab').onclick=()=>{view='xref';setTab('xrefTab');renderTree()};function setTab(id){for(const x of ['modelTab','diagramTab','xrefTab'])$(x).classList.toggle('active',x===id)}
$('presenceStatus').onclick=()=>{const panel=$('presencePanel'),open=panel.hidden;panel.hidden=!open;$('presenceStatus').setAttribute('aria-expanded',String(open));if(open)presenceView.renderPanel()};
$('diagramSelect').onchange=e=>navigateDiagram(e.target.value);$('newDiagram').onclick=createDiagram;$('createDiagramConfirm').onclick=e=>{e.preventDefault();if(!confirmCreateDiagram())return;$('newDiagramDialog').close();render()};$('newProject').onclick=()=>{if(!confirm('Create a new project?'))return;project=createProject(prompt('Project name','New Systems Model')||'New Systems Model');selected={};save();render()};$('saveProject').onclick=()=>{save();log('Saved locally.','ok')};$('exportProject').onclick=()=>download(`${project.name.replace(/\W+/g,'_')}.sysml.json`,JSON.stringify(project,null,2),'application/json');$('openProject').onclick=()=>$('fileInput').click();$('fileInput').onchange=async e=>{const f=e.target.files[0];if(!f)return;const before=project;try{project=normalizeProject(JSON.parse(await f.text()));save();render();log(`Opened ${f.name}.`,'ok')}catch(error){project=before;log(`Could not open ${f.name}: ${error.message}`,'error')}finally{e.target.value=''}};$('importWorkbook').onclick=()=>$('workbookInput').click();$('workbookInput').onchange=async e=>{const f=e.target.files[0];if(!f)return;const before=JSON.stringify(project);try{await importWorkbook(f,project,log,{strict:true});const issues=validate(project),errors=issues.filter(x=>x.severity==='error');if(errors.length&&!confirm(`Import has ${errors.length} errors. Keep staged import?`)){project=JSON.parse(before);render();return}commitLocal({type:'bulk-import',project:structuredClone(project)},`Imported ${f.name}`)}catch(err){project=JSON.parse(before);log(err.message,'error')}};$('validateModel').onclick=()=>{const issues=validate(project);if(!issues.length)log('SysML validation passed.','ok');else issues.forEach(i=>log(`${i.severity.toUpperCase()} [${i.code}] ${i.message}`,i.severity==='error'?'error':'warn'))};$('undo').onclick=()=>{if(!undoHistory.length)return;future.push(JSON.stringify(project));project=normalizeProject(JSON.parse(undoHistory.pop()));save();render();collab.publish({type:'replace-project',project:structuredClone(project)});log('Undid local change and synchronized the room.','ok')};$('redo').onclick=()=>{if(!future.length)return;undoHistory.push(JSON.stringify(project));project=normalizeProject(JSON.parse(future.pop()));save();render();collab.publish({type:'replace-project',project:structuredClone(project)});log('Redid local change and synchronized the room.','ok')};$('deleteSelected').onclick=deleteSelected;$('reuseElement').onclick=reuse;$('openChild').onclick=openChildForSelected;$('searchButton').onclick=search;$('searchBox').onkeydown=e=>{if(e.key==='Enter')search()};$('routeEdges').onclick=routeAll;$('zoomIn').onclick=()=>{zoom=Math.min(2.5,zoom+.1);renderCanvas()};$('zoomOut').onclick=()=>{zoom=Math.max(.25,zoom-.1);renderCanvas()};$('zoomReset').onclick=()=>{zoom=1;renderCanvas()};$('connectRoom').onclick=()=>{const settings={displayName:$('displayName').value,roomId:$('roomId').value,branchId:$('branchSelect').value};saveCollaborationSettings(localStorage,settings);window.history.replaceState(null,'',collaborationLink(location,settings));collab.setName(settings.displayName.trim()||'Modeler');collab.connect(settings.roomId.trim()||'default',settings.branchId||'main')};$('copyRoomLink').onclick=async()=>{const settings={roomId:$('roomId').value,branchId:$('branchSelect').value};const link=collaborationLink(location,settings);try{await navigator.clipboard.writeText(link);log('Collaboration room link copied.','ok')}catch{prompt('Copy this collaboration room link',link)}};$('commitModel').onclick=()=>{const m=prompt('Commit message','Model update');if(m)collab.commit(m)};$('newBranch').onclick=()=>{const n=prompt('Branch name','feature');if(n)collab.createBranch(n)};$('branchSelect').onchange=e=>collab.switchBranch(e.target.value);$('mergeBranch').onclick=()=>{const from=prompt('Branch ID to merge into current branch','');if(from)collab.send({type:'merge-branch',sourceBranchId:from,targetBranchId:collab.branchId})};$('lockSelected').onclick=()=>selected.id&&collab.lock(selected.id,300000);$('unlockSelected').onclick=()=>selected.id&&collab.unlock(selected.id);$('clearLog').onclick=()=>$('log').replaceChildren();$('help').onclick=()=>alert('Create a diagram, choose a palette tool, and click the canvas. Choose a relationship, then click source and target. Double-click a Block, Activity, StateMachine, or Interaction to open its child diagram.');

function isEditingTarget(target){return target?.matches?.('input,textarea,select,[contenteditable="true"]')}
let typingAwarenessTimer=null;document.addEventListener('focusin',event=>{if(!isEditingTarget(event.target))return;const propertyName=event.target.closest('.field')?.querySelector('span')?.textContent||event.target.dataset.field||event.target.name||'property';publishCurrentAwareness({propertyName,typing:false})});document.addEventListener('input',event=>{if(!isEditingTarget(event.target))return;const propertyName=event.target.closest('.field')?.querySelector('span')?.textContent||event.target.dataset.field||event.target.name||'property';publishCurrentAwareness({propertyName,typing:true});clearTimeout(typingAwarenessTimer);typingAwarenessTimer=setTimeout(()=>publishCurrentAwareness({propertyName,typing:false}),900)});document.addEventListener('focusout',event=>{if(isEditingTarget(event.target))publishCurrentAwareness({propertyName:null,typing:false})});document.addEventListener('pointerup',()=>publishCurrentAwareness());
function viewportDiagramCenter(){const wrap=$('canvasWrap'),rect=$('canvas').getBoundingClientRect();return diagramPointFromClient({clientX:wrap.getBoundingClientRect().left+wrap.clientWidth/2,clientY:wrap.getBoundingClientRect().top+wrap.clientHeight/2},rect,zoom)}
function revealPresentation(node){const wrap=$('canvasWrap');wrap.scrollTo({left:(node.x+node.width/2)*zoom-wrap.clientWidth/2,top:(node.y+node.height/2)*zoom-wrap.clientHeight/2,behavior:'smooth'});selected={type:'element',id:node.elementId,nodeId:node.id,edgeId:null};renderCanvas();renderProperties();log('Element is already displayed on this diagram.','ok')}
function showExistingElement(elementId,position){const diagram=activeDiagram();if(!diagram)return log('No active diagram is available.','warn');const semantic=findElement(project,elementId),support=supportAction(semantic?.kind||'Unknown','element','present');if(!support.allowed)return log(support.message,'warn');const existing=findPresentation(project,elementId,diagram.id);if(existing)return revealPresentation(existing);if(diagram.diagramType==='Sequence Diagram'&&validLifelineRepresentation(project,elementId)){const participant=findElement(project,elementId),lifeline=defaultElement('Lifeline',diagram.contextId||diagram.ownerId);lifeline.name=participant.name;lifeline.representedElementId=participant.id;checkpoint();project.elements.push(lifeline);const shown=showElementOnDiagram(project,lifeline.id,diagram.id,position,{source:'containment-participant'});if(!shown.created){project.elements.pop();return log(shown.message,'warn')}selected={type:'element',id:lifeline.id,nodeId:shown.node.id,edgeId:null};commitLocal({type:'create-element',element:structuredClone(lifeline),diagramId:diagram.id,node:structuredClone(shown.node)},`Presented ${participant.name} as a Lifeline`);return}const verdict=canShowElementOnDiagram(project,elementId,diagram.id,{point:position});if(!verdict.valid)return log(verdict.message,'warn');checkpoint();const result=showElementOnDiagram(project,elementId,diagram.id,position,{source:'containment'});if(!result.created)return log(result.message,'warn');selected={type:'element',id:elementId,nodeId:result.node.id,edgeId:null};commitLocal({type:'add-presentation',diagramId:diagram.id,node:structuredClone(result.node)},`Displayed ${result.element.name} on ${diagram.name}`)}
const canvasWrap=$('canvasWrap');canvasWrap.addEventListener('pointermove',event=>{const rect=$('canvas').getBoundingClientRect();publishCurrentAwareness({cursor:{x:(event.clientX-rect.left)/zoom,y:(event.clientY-rect.top)/zoom}})});canvasWrap.addEventListener('pointerleave',()=>publishCurrentAwareness({cursor:null}));canvasWrap.addEventListener('scroll',()=>publishCurrentAwareness({viewport:{x:canvasWrap.scrollLeft/zoom,y:canvasWrap.scrollTop/zoom,width:canvasWrap.clientWidth/zoom,height:canvasWrap.clientHeight/zoom,zoom}}),{passive:true});
let containmentDragDepth=0;function draggedElementId(event){try{const payload=JSON.parse(event.dataTransfer.getData('application/x-sysml-containment+json')||'null');return payload?.semanticElementIds?.[0]||payload?.semanticElementId||draggedTreeElementId}catch{return draggedTreeElementId}}
canvasWrap.addEventListener('dragenter',event=>{const id=draggedElementId(event);if(!id)return;event.preventDefault();containmentDragDepth++;canvasWrap.classList.add('containment-drop-active')});
canvasWrap.addEventListener('dragover',event=>{const id=draggedElementId(event);if(!id)return;event.preventDefault();const point=diagramPointFromClient(event,$('canvas').getBoundingClientRect(),zoom),verdict=canShowElementOnDiagram(project,id,activeDiagram()?.id,{point});event.dataTransfer.dropEffect=verdict.valid?'copy':'none';canvasWrap.classList.toggle('drop-valid',verdict.valid);canvasWrap.classList.toggle('drop-invalid',!verdict.valid);const delta=edgeAutoPanDelta({x:event.clientX,y:event.clientY},canvasWrap.getBoundingClientRect());if(delta.x||delta.y)canvasWrap.scrollBy(delta.x,delta.y)});
canvasWrap.addEventListener('dragleave',event=>{if(--containmentDragDepth<=0||!canvasWrap.contains(event.relatedTarget)){containmentDragDepth=0;canvasWrap.classList.remove('containment-drop-active','drop-valid','drop-invalid')}});
canvasWrap.addEventListener('drop',event=>{event.preventDefault();event.stopPropagation();containmentDragDepth=0;canvasWrap.classList.remove('containment-drop-active','drop-valid','drop-invalid');const id=draggedElementId(event);draggedTreeElementId=null;if(!id)return;showExistingElement(id,diagramPointFromClient(event,$('canvas').getBoundingClientRect(),zoom))});
$('tree').addEventListener('dragover',event=>{const rect=$('tree').getBoundingClientRect(),margin=38;if(event.clientY<rect.top+margin)$('tree').scrollTop-=8;else if(event.clientY>rect.bottom-margin)$('tree').scrollTop+=8});

function setSelectMode(){tool=null;toolRecordKind=null;sourceId=null;sourceNodeId=null;openMessageAnchor=null;renderPalette();$('canvasWrap').classList.add('select-mode');log('Select mode.','ok')}
document.addEventListener('keydown',e=>{if(['ControlLeft','ControlRight','MetaLeft','MetaRight'].includes(e.code)&&!isEditingTarget(e.target))$('canvasWrap').classList.add('pan-ready');if(e.code==='Space'&&!isEditingTarget(e.target)){spacePan=true;$('canvasWrap').classList.add('pan-ready');e.preventDefault()}if(isEditingTarget(e.target))return;const mod=e.ctrlKey||e.metaKey;if(e.key==='Escape'){closeTreeContextMenu();setSelectMode();selectedNodeIds.clear();selected={type:null,id:null,nodeId:null,edgeId:null};renderCanvas();renderProperties();e.preventDefault()}else if(e.key==='F2'&&selected?.id){beginTreeRename(selected.id,selected.type);e.preventDefault()}else if(mod&&e.key.toLowerCase()==='c'&&selected.type==='element'){copyTreeElement(selected.id);e.preventDefault()}else if(mod&&e.key.toLowerCase()==='v'&&selected.type==='element'){pasteTreeElement(selected.id);e.preventDefault()}else if(mod&&e.key.toLowerCase()==='d'&&selected.type==='element'){duplicateElement(selected.id);e.preventDefault()}else if(e.key==='Backspace'||e.key==='Delete'){if(selected?.id||selected?.nodeId||selected?.edgeId){deleteSelected();e.preventDefault()}}else if(mod&&e.key.toLowerCase()==='f'){e.preventDefault();$('searchBox').focus()}else if((e.altKey)&&e.key==='ArrowLeft'){navigationGoBack();e.preventDefault()}else if((e.altKey)&&e.key==='ArrowRight'){navigationGoForward();e.preventDefault()}else if(['ArrowLeft','ArrowRight','ArrowUp','ArrowDown'].includes(e.key)&&selectedNodeIds.size){const step=e.shiftKey?10:1;const map={ArrowLeft:[-step,0],ArrowRight:[step,0],ArrowUp:[0,-step],ArrowDown:[0,step]};nudgeSelection(...map[e.key]);e.preventDefault()}});
document.addEventListener('keyup',e=>{if(e.code==='Space')spacePan=false;if(['Space','ControlLeft','ControlRight','MetaLeft','MetaRight'].includes(e.code))$('canvasWrap').classList.remove('pan-ready')});
const wrapEl=$('canvasWrap');
wrapEl.addEventListener('wheel',e=>{if(!e.ctrlKey&&!e.metaKey)return;e.preventDefault();const rect=wrapEl.getBoundingClientRect(),cx=e.clientX-rect.left,cy=e.clientY-rect.top,worldX=(wrapEl.scrollLeft+cx)/zoom,worldY=(wrapEl.scrollTop+cy)/zoom,next=Math.max(.25,Math.min(3,zoom*Math.exp(-e.deltaY*.0015)));zoom=next;renderCanvas();wrapEl.scrollLeft=worldX*zoom-cx;wrapEl.scrollTop=worldY*zoom-cy},{passive:false});
let suppressPanClick=false;
wrapEl.addEventListener('pointerdown',e=>{if(!isDiagramPanGesture(e,{spacePan}))return;panState={pointerId:e.pointerId,x:e.clientX,y:e.clientY,left:wrapEl.scrollLeft,top:wrapEl.scrollTop,moved:false};wrapEl.classList.add('panning');wrapEl.setPointerCapture(e.pointerId);e.preventDefault();e.stopPropagation()},true);
wrapEl.addEventListener('pointermove',e=>{if(!panState||panState.pointerId!==e.pointerId)return;const next=diagramPanPosition(panState,e);wrapEl.scrollLeft=next.left;wrapEl.scrollTop=next.top;panState.moved||=next.moved});
function finishDiagramPan(e){if(!panState||e?.pointerId!==undefined&&panState.pointerId!==e.pointerId)return;suppressPanClick=panState.moved;panState=null;wrapEl.classList.remove('panning')}
wrapEl.addEventListener('pointerup',finishDiagramPan);wrapEl.addEventListener('pointercancel',finishDiagramPan);wrapEl.addEventListener('lostpointercapture',finishDiagramPan);wrapEl.addEventListener('click',e=>{if(!suppressPanClick)return;suppressPanClick=false;e.preventDefault();e.stopImmediatePropagation()},true);window.addEventListener('blur',()=>{spacePan=false;wrapEl.classList.remove('pan-ready');finishDiagramPan()});

$('displayName').value=initialCollaboration.displayName;$('roomId').value=initialCollaboration.roomId;if(![...$('branchSelect').options].some(option=>option.value===initialCollaboration.branchId))$('branchSelect').add(new Option(initialCollaboration.branchId,initialCollaboration.branchId));$('branchSelect').value=initialCollaboration.branchId;

wireMenus();render();log('Systems Modeler Collaborative v5 loaded. All visible commands are wired or explicitly disabled when unavailable.','ok');


window.SystemsModelerAPI={
 getProject:()=>project,
 createBlankProject:name=>createProject(name),
 setProject:p=>{project=normalizeProject(p);save();render();},
 validate:()=>validate(project),
 validationQuickFix:issue=>validationQuickFix(project,issue),
 applyValidationQuickFix:issue=>{const before=structuredClone(project);if(!applyValidationQuickFix(project,issue))return false;commitLocal({type:'replace-project',project:structuredClone(project),expectedValue:before},`Applied validation fix ${issue.code}`);return true},
 getSelection:()=>structuredClone(selected),
 selectElement:id=>{selected={type:'element',id,nodeId:null,edgeId:null};render();},
 activeDiagram:()=>activeDiagram(),
 log,
 save,
 render,
 commitOperation:(op,msg)=>{checkpoint();project=normalizeProject(applyOperation(project,op));commitLocal(op,msg);},
 exportDownload:download,
 allElements:()=>allElements(project),
 findElement:id=>findElement(project,id),
 findRelationship:id=>findRelationship(project,id),
 collaboration:collab
};
window.dispatchEvent(new CustomEvent('systems-modeler-ready'));
