import test from 'node:test';import assert from 'node:assert/strict';import {createProject,defaultElement,defaultRelationship} from '../public/src/model.js';import {CONNECTOR_END_SCHEMA,PORT_PROPERTY_SCHEMA,validateStructuralEndpoints} from '../public/src/structural-endpoint-validation.js';
test('structural endpoint schemas distinguish authored selectors from read-only identity',()=>{assert.ok(PORT_PROPERTY_SCHEMA.editable.includes('isConjugated'));assert.ok(PORT_PROPERTY_SCHEMA.multiline.includes('documentation'));assert.ok(PORT_PROPERTY_SCHEMA.readOnly.includes('qualifiedName'));assert.ok(CONNECTOR_END_SCHEMA.selectors.includes('endpointPath'));assert.ok(CONNECTOR_END_SCHEMA.readOnly.includes('connectorId'))});
test('structural validation reports field-specific port, connector-end, and item-flow defects',()=>{const p=createProject('Invalid'),port=Object.assign(defaultElement('ProxyPort',p.root.id),{id:'port',typeRef:'missing',direction:'sideways',isConjugated:'yes',multiplicity:'many'}),connector=Object.assign(defaultRelationship('Connector','port','missing',p.root.id),{id:'connector'}),flow=Object.assign(defaultRelationship('ItemFlow','port','port',p.root.id),{id:'flow',connectorId:'missing',conveyedIds:['missing'],direction:'visual-left'});p.elements.push(port);p.relationships.push(connector,flow);const codes=validateStructuralEndpoints(p).map(x=>x.code);for(const code of ['PORT_OWNER_INVALID','PORT_TYPE_UNRESOLVED','PORT_DIRECTION_INVALID','PORT_CONJUGATION_INVALID','PORT_MULTIPLICITY_INVALID','CONNECTOR_END_UNRESOLVED','ITEM_FLOW_CONNECTOR_INVALID','ITEM_FLOW_CONVEYED_UNRESOLVED','ITEM_FLOW_DIRECTION_INVALID'])assert.ok(codes.includes(code),code)});
