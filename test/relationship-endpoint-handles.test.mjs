import test from'node:test';import assert from'node:assert/strict';import{endpointHandles}from'../public/src/relationship-endpoint-handles.js';test('endpoint handles are stable and keyboard accessible',()=>{const h=endpointHandles({id:'edge',sourceNodeId:'a',targetNodeId:'b',sourceId:'sa',targetId:'sb'});assert.deepEqual(h.map(x=>x.id),['handle-edge-source','handle-edge-target']);assert.ok(h.every(x=>x.tabIndex===0&&x.ariaLabel))});
