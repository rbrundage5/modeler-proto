import test from'node:test';import assert from'node:assert/strict';import{applyRelationshipOperation}from'../public/src/relationship-operations.js';test('granular relationship operation replay is idempotent',()=>{const p={relationships:[{id:'r'}],diagrams:[{id:'d',nodes:[],edges:[{id:'e',relationshipId:'r'}]}]};const op={operationId:'op',type:'add-waypoint',diagramId:'d',edgeId:'e',relationshipId:'r',waypoint:{id:'w',x:1,y:2}};applyRelationshipOperation(p,op);applyRelationshipOperation(p,op);assert.equal(p.diagrams[0].edges[0].waypoints.length,1);assert.deepEqual(p.appliedRelationshipOperationIds,['op'])});
