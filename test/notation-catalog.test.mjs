import test from 'node:test';import assert from 'node:assert/strict';import {DIAGRAMS,ELEMENTS,RELATIONSHIPS} from '../public/src/sysml-profile.js';import {DIAGRAM_NOTATION,ELEMENT_NOTATION,RELATIONSHIP_NOTATION,notationCoverageIssues,notationForElement,notationForRelationship} from '../public/src/notation-catalog.js';
test('catalog covers every supported semantic kind exactly once',()=>{assert.deepEqual(Object.keys(ELEMENT_NOTATION).sort(),Object.keys(ELEMENTS).sort());assert.deepEqual(Object.keys(RELATIONSHIP_NOTATION).sort(),Object.keys(RELATIONSHIPS).sort());assert.deepEqual(Object.keys(DIAGRAM_NOTATION).sort(),Object.keys(DIAGRAMS).sort());assert.deepEqual(notationCoverageIssues(),[])});
test('every enabled palette entry resolves in its diagram context',()=>{for(const [diagramType,definition] of Object.entries(DIAGRAMS)){for(const kind of definition.elements)assert.ok(notationForElement(kind,diagramType),`${kind} on ${diagramType}`);for(const kind of definition.relationships)assert.ok(notationForRelationship(kind,diagramType),`${kind} on ${diagramType}`)}});
test('catalog retains compartments, boundary rules, and minimum geometry',()=>{assert.deepEqual(ELEMENT_NOTATION.Block.compartments,['parts','references','values','flowProperties','ports','operations','constraints']);assert.equal(ELEMENT_NOTATION.ProxyPort.boundaryAttached,true);assert.ok(ELEMENT_NOTATION.Requirement.minWidth>0);assert.ok(ELEMENT_NOTATION.Requirement.minHeight>0)});
test('relationship lookup applies semantic end ownership without mutating catalog',()=>{const composition=notationForRelationship({kind:'Composition',sourceAggregation:'composite'},'Block Definition Diagram');assert.equal(composition.sourceMarker,'diamondFilled');assert.equal(RELATIONSHIP_NOTATION.Composition.sourceMarker,'none')});
