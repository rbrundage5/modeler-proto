import test from 'node:test';import assert from 'node:assert/strict';import {createProject,defaultElement} from '../public/src/model.js';import {createSupportedElement,validateCompletedElement,upsertSupportedElement} from '../public/src/selected-element-support.js';
function fixture(){const project=createProject('References'),owner=Object.assign(defaultElement('Block',project.root.id),{id:'owner'}),type=Object.assign(defaultElement('InterfaceBlock',project.root.id),{id:'type'});project.elements.push(owner,type);return{project,owner,type}}
test('ReferenceProperty is a stable typed non-composite Usage with explicit ownership',()=>{const {project}=fixture(),ref=createSupportedElement(project,'ReferenceProperty',{id:'ref',externalId:'REF-1',ownerId:'owner',typeRef:'type',name:'service'});assert.equal(ref.semanticRole,'Usage');assert.equal(ref.composition,'none');assert.deepEqual(validateCompletedElement(project,ref),[]);ref.name='renamed';assert.equal(ref.id,'ref');assert.throws(()=>createSupportedElement(project,'ReferenceProperty',{ownerId:project.root.id,typeRef:'type'}),/owner/)});
test('ReferenceProperty stable-ID-first reimport updates without duplication',()=>{const {project}=fixture();createSupportedElement(project,'ReferenceProperty',{id:'ref',externalId:'REF-1',ownerId:'owner',typeRef:'type'});const result=upsertSupportedElement(project,{kind:'ReferenceProperty',id:'ref',externalId:'REF-1',ownerId:'owner',typeRef:'type',documentation:'updated'});assert.equal(result.created,false);assert.equal(project.elements.filter(x=>x.kind==='ReferenceProperty').length,1);assert.equal(result.element.documentation,'updated')});
