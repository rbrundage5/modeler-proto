import test from 'node:test';import assert from 'node:assert/strict';import {createPlatformServices} from '../public/src/platform-services.js';
test('browser adapter keeps native file capability disabled',()=>{const service=createPlatformServices({});assert.equal(service.kind,'browser');assert.equal(service.capabilities.nativeFiles,false)});
test('desktop adapter delegates only the narrow declared capabilities',async()=>{const calls=[],host={modelerDesktop:{openProject:async()=>({content:'{}'}),saveProject:async(...args)=>calls.push(['save',...args]),saveProjectAs:async()=>{},exportFile:async()=>{},importFile:async()=>{},setDirty:value=>calls.push(['dirty',value]),onCommand:()=>()=>{},metadata:async()=>({version:'1'})}};const service=createPlatformServices(host);assert.equal(service.kind,'desktop');await service.saveProject('a.json','{}');service.setDirty(true);assert.deepEqual(calls,[['save','{}','a.json'],['dirty',true]]);assert.equal('readFile' in service,false);assert.equal('ipcRenderer' in service,false)});
