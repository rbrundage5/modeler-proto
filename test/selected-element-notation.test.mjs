import test from 'node:test';import assert from 'node:assert/strict';import {createProject,defaultElement} from '../public/src/model.js';import {selectedNotationDescriptor} from '../public/src/selected-element-notation.js';import {rendererDescriptor} from '../public/src/renderer-registry.js';import {relationshipNotation} from '../public/src/notation-contracts.js';
test('TestCase uses deliberate Requirement Diagram notation with planning fields and no fallback',()=>{const p=createProject('P'),c=Object.assign(defaultElement('TestCase',p.root.id),{id:'case',name:'Inspect',verificationCaseId:'VC-1',verificationObjective:'Confirm',verificationMethod:'Inspection',acceptanceCriteria:'No damage'});p.elements.push(c);const n=selectedNotationDescriptor(p,c,'Requirement Diagram'),renderer=rendererDescriptor('TestCase','Requirement Diagram');assert.equal(n.supported,true);assert.equal(n.keyword,'testCase');assert.equal(n.display.verificationCaseId,'VC-1');assert.equal(renderer.supported,true);assert.notEqual(renderer.rendererKey,'diagnostic-presentation')});
test('PartProperty notation is restricted to IBD and Verify uses the correct open dependency notation',()=>{const p=createProject('P'),type=Object.assign(defaultElement('Block',p.root.id),{id:'type',name:'Engine'}),part=Object.assign(defaultElement('PartProperty',p.root.id),{id:'part',name:'engine',typeRef:'type'});p.elements.push(type,part);assert.equal(selectedNotationDescriptor(p,part,'Internal Block Diagram').display.typeName,'Engine');assert.equal(selectedNotationDescriptor(p,part,'Activity Diagram').supported,false);assert.deepEqual(relationshipNotation('Verify'),{line:'dashed',marker:'open',stereotype:'verify'})});
test('ReferenceProperty has deliberate IBD notation and no cross-diagram fallback',()=>{const p=createProject('P'),type=Object.assign(defaultElement('InterfaceBlock',p.root.id),{id:'type',name:'Service'}),ref=Object.assign(defaultElement('ReferenceProperty',p.root.id),{id:'ref',name:'service',typeRef:'type'});p.elements.push(type,ref);const descriptor=selectedNotationDescriptor(p,ref,'Internal Block Diagram');assert.equal(descriptor.presentationType,'ReferencePropertyPresentation');assert.equal(descriptor.rendererKey,'property-renderer');assert.equal(descriptor.display.keyword,'reference');assert.equal(descriptor.display.typeName,'Service');assert.equal(selectedNotationDescriptor(p,ref,'Block Definition Diagram').supported,false)});
