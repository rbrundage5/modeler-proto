import test from'node:test';import assert from'node:assert/strict';import{addWaypoint,moveWaypoint,removeWaypoint,resetRouting,setRelationshipLabel}from'../public/src/relationship-geometry.js';test('label and waypoint mutations are granular, ordered, persistent, and idempotent',()=>{const e={id:'e'};setRelationshipLabel(e,{x:4,y:5});addWaypoint(e,{id:'w',x:1,y:2});addWaypoint(e,{id:'w',x:9,y:9});assert.equal(e.waypoints.length,1);moveWaypoint(e,'w',{x:3,y:4});assert.equal(e.labelPresentation.x,4);assert.equal(e.waypoints[0].x,3);removeWaypoint(e,'w');assert.equal(e.waypoints.length,0);resetRouting(e);assert.equal(e.routingMode,'auto')});
