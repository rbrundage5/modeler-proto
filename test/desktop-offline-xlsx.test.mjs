import test from 'node:test';import assert from 'node:assert/strict';import {deflateRawSync} from 'node:zlib';import {readXlsx} from '../public/src/import/core/xlsx-reader.js';
const encoder=new TextEncoder();
function crc32(bytes){let crc=0xffffffff;for(const byte of bytes){crc^=byte;for(let bit=0;bit<8;bit++)crc=(crc>>>1)^((crc&1)?0xedb88320:0)}return(crc^0xffffffff)>>>0}
function storedZip(files){const locals=[],centrals=[];let offset=0;for(const [name,value] of Object.entries(files)){const nameBytes=encoder.encode(name),data=encoder.encode(value),compressed=new Uint8Array(deflateRawSync(data)),crc=crc32(data),local=new Uint8Array(30+nameBytes.length+compressed.length),lv=new DataView(local.buffer);lv.setUint32(0,0x04034b50,true);lv.setUint16(4,20,true);lv.setUint16(8,8,true);lv.setUint32(14,crc,true);lv.setUint32(18,compressed.length,true);lv.setUint32(22,data.length,true);lv.setUint16(26,nameBytes.length,true);local.set(nameBytes,30);local.set(compressed,30+nameBytes.length);locals.push(local);const central=new Uint8Array(46+nameBytes.length),cv=new DataView(central.buffer);cv.setUint32(0,0x02014b50,true);cv.setUint16(4,20,true);cv.setUint16(6,20,true);cv.setUint16(10,8,true);cv.setUint32(16,crc,true);cv.setUint32(20,compressed.length,true);cv.setUint32(24,data.length,true);cv.setUint16(28,nameBytes.length,true);cv.setUint32(42,offset,true);central.set(nameBytes,46);centrals.push(central);offset+=local.length}const centralSize=centrals.reduce((sum,item)=>sum+item.length,0),end=new Uint8Array(22),view=new DataView(end.buffer);view.setUint32(0,0x06054b50,true);view.setUint16(8,centrals.length,true);view.setUint16(10,centrals.length,true);view.setUint32(12,centralSize,true);view.setUint32(16,offset,true);const output=new Uint8Array(offset+centralSize+end.length);let cursor=0;for(const item of [...locals,...centrals,end]){output.set(item,cursor);cursor+=item.length}return output.buffer}
test('bundled OOXML reader imports shared and inline strings without network globals',async()=>{const buffer=storedZip({'xl/workbook.xml':'<workbook><sheets><sheet name="Elements" r:id="rId1"/></sheets></workbook>','xl/_rels/workbook.xml.rels':'<Relationships><Relationship Id="rId1" Target="worksheets/sheet1.xml"/></Relationships>','xl/sharedStrings.xml':'<sst><si><t>External ID</t></si><si><t>REQ-1</t></si></sst>','xl/worksheets/sheet1.xml':'<worksheet><sheetData><row r="1"><c r="A1" t="s"><v>0</v></c><c r="B1" t="inlineStr"><is><t>Name</t></is></c></row><row r="2"><c r="A2" t="s"><v>1</v></c><c r="B2" t="inlineStr"><is><t>Offline requirement</t></is></c></row></sheetData></worksheet>'});const result=await readXlsx(buffer);assert.deepEqual(result,[{name:'Elements',matrix:[['External ID','Name'],['REQ-1','Offline requirement']]}]);assert.equal(globalThis.XLSX,undefined)});
