import test from'node:test';import assert from'node:assert/strict';import{normalizeRelationshipPresentation,validateRelationshipPresentation}from'../public/src/relationship-editing.js';test('relationship contract separates semantic identities from presentation geometry',()=>{const r={id:'r',sourceId:'a',targetId:'b'},d={id:'d',nodes:[{id:'na'},{id:'nb'}]},e={id:'e',sourceNodeId:'na',targetNodeId:'nb',points:[{x:0,y:0},{x:5,y:6},{x:10,y:10}]};normalizeRelationshipPresentation(e,r,d);assert.equal(e.waypoints[0].id,'waypoint-e-0');assert.equal(e.sourceId,'a');assert.equal(e.routingMode,'auto');assert.deepEqual(validateRelationshipPresentation({elements:[{id:'a'},{id:'b'}],relationships:[r]},d,e),[])});
