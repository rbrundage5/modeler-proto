import test from'node:test';import assert from'node:assert/strict';import{STRUCTURAL_CONTRACTS,STRUCTURAL_DEPENDENCIES,validateStructuralContracts}from'../public/src/e01s-structural-contracts.js';import{AUTHORITATIVE_SUPPORT_INVENTORY}from'../public/src/supported-type-inventory.js';test('structural contracts independently enforce complete inventory gates',()=>{assert.equal(validateStructuralContracts(AUTHORITATIVE_SUPPORT_INVENTORY),true);assert.deepEqual(STRUCTURAL_DEPENDENCIES.ProxyPort,['Port','InterfaceBlock']);assert.equal(STRUCTURAL_CONTRACTS.DataType.gates.browser,'implemented-and-qualified');assert.equal(STRUCTURAL_CONTRACTS.ValueProperty.gates.typeReferences,'implemented-and-qualified')});
