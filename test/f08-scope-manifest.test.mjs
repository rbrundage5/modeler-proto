import test from'node:test';import assert from'node:assert/strict';import{F08_SCOPE}from'../public/src/f08-scope-manifest.js';test('F-08 remains bounded to shared non-sequence relationship services',()=>{assert.deepEqual(F08_SCOPE.eligibleForPromotion,['DelegationConnector']);assert.equal(F08_SCOPE.mustRemainPartial.includes('DelegationConnector'),false);assert.equal(F08_SCOPE.delegationQualification.length,3);for(const k of['services','rootCauses','completionCriteria','requiredTests','exclusions'])assert.ok(F08_SCOPE[k].length)});
