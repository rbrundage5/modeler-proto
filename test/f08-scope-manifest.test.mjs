import test from'node:test';import assert from'node:assert/strict';import{F08_SCOPE}from'../public/src/f08-scope-manifest.js';test('F-08 remains bounded to shared non-sequence relationship services',()=>{assert.equal(F08_SCOPE.eligibleForPromotion.length,0);assert.ok(F08_SCOPE.mustRemainPartial.includes('DelegationConnector'));for(const k of['services','rootCauses','completionCriteria','requiredTests','exclusions'])assert.ok(F08_SCOPE[k].length)});
