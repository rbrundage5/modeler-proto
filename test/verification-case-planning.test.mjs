import test from 'node:test';import assert from 'node:assert/strict';import {createProject,normalizeProject} from '../public/src/model.js';import {createTestCase,validateTestCase} from '../public/src/verification-model.js';
test('verification case planning properties normalize without changing stable identity',()=>{const p=createProject('P'),c=createTestCase(p,{id:'case-1',externalId:'VC-EXT',verificationCaseId:'VC-1',name:'Inspect',verificationObjective:'Confirm fit',acceptanceCriteria:'No interference',plannedEnvironment:'Lab',plannedStatus:'Planned'}),id=c.id;c.name='Renamed';const restored=normalizeProject(structuredClone(p)),r=restored.elements[0];assert.equal(r.id,id);assert.equal(r.verificationCaseId,'VC-1');assert.equal(r.executionStatus,'Unavailable');assert.deepEqual(validateTestCase(restored,r),[])});
test('verification planning validation diagnoses unsupported methods and execution claims',()=>{const p=createProject('P'),c=createTestCase(p,{verificationCaseId:'VC-2',verificationObjective:'Objective',verificationMethod:'Guess',executionStatus:'Passed'});assert.deepEqual(validateTestCase(p,c).map(x=>x.code),['VERIFICATION_METHOD_UNKNOWN','UNSUPPORTED_EXECUTION_STATUS'])});
