import test from 'node:test';import assert from 'node:assert/strict';import {createProject,defaultElement} from '../public/src/model.js';import {REFERENCE_PROPERTY_SCHEMA,validateMultiplicity,validateReferenceProperty} from '../public/src/reference-property.js';
test('ReferenceProperty schema separates authored, structured, multiline, and read-only fields',()=>{assert.ok(REFERENCE_PROPERTY_SCHEMA.editable.includes('typeRef'));assert.ok(REFERENCE_PROPERTY_SCHEMA.multiline.includes('documentation'));assert.ok(REFERENCE_PROPERTY_SCHEMA.readOnly.includes('qualifiedName'));assert.equal(REFERENCE_PROPERTY_SCHEMA.commitPolicy.externalId,'blur-or-confirm')});
test('ReferenceProperty validation diagnoses invalid owner, type, multiplicity, and aggregation actionably',()=>{for(const value of ['1','0..1','1..*','*'])assert.equal(validateMultiplicity(value),true);assert.equal(validateMultiplicity('many'),false);const p=createProject('P'),e=Object.assign(defaultElement('ReferenceProperty',p.root.id),{typeRef:'missing',multiplicity:'many',aggregation:'composite'});assert.deepEqual(validateReferenceProperty(p,e).map(x=>x.code),['REFERENCE_OWNER_INVALID','REFERENCE_TYPE_INVALID','REFERENCE_MULTIPLICITY_INVALID','REFERENCE_AGGREGATION_INVALID'])});
