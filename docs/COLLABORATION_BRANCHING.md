# Collaboration Branching

Each branch has an authoritative snapshot and monotonically increasing sequence. Accepted revisions use `<branch>:<sequence>` IDs and retain parent revision IDs. Existing branch creation/switch/merge behavior remains compatible. First-class base revisions, protection, archival, permissions, merge requests, and complete semantic three-way merge preview are planned dependent milestones.
