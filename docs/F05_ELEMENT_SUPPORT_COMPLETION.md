# F-05 element support completion and capability transparency

## Baseline and evidence boundary

The baseline includes P-03 through P-05 and F-01 through F-04. F-04 remains partially qualified: three workflow rows are blocked, three automated-unverified, Node 22 is unavailable, and broad manual acceptance remains open. P-05 likewise retains Node 22 and manual gates. D-01 Electron work remains absent. F-05 does not disguise these inherited limitations.

## Eight controlled steps

1. Consolidated element, relationship, diagram, and specialized-presentation declarations into one authoritative inventory with the six permitted support states and explicit workflow evidence.
2. Replaced vague `Limited` copy with accessible Complete/Partial/Import only/Read only/Blocked/Unsupported labels and keyboard-visible capability detail.
3. Ranked partials and bounded completion to TestCase and PartProperty. All unselected partials keep explicit reasons.
4. Completed selected semantic ownership, typing, planning properties, validation, stable identity, upsert, persistence, and backward compatibility.
5. Completed TestCase planning notation, PartProperty IBD notation, Verify line/arrow contract, renderer resolution, selection, movement, resizing, deletion, and geometry persistence through shared services.
6. Derived palette/manual creation and drag/drop eligibility from inventory actions; import-only/read-only/unsupported types cannot leak into manual creation. Duplicate containment placement reveals the existing presentation rather than duplicating semantics.
7. Added idempotent legacy status and fallback-presentation migration. Safe selected fallbacks upgrade without identity/geometry changes; unsafe records remain diagnostic.
8. Added deterministic, audit, and real-browser evidence; no binary browser artifacts are committed.

## Bounded completion

**TestCase** is complete on Requirement Diagrams for semantic creation, planning-property editing, Verify compatibility, standards-based rendering, selection/movement/resizing, presentation deletion, save/reload, undo/redo, transactional import/reimport, and browser qualification. Execution and evidence credit remain excluded.

**PartProperty** is complete on Internal Block Diagrams for explicit Block/AssociationBlock/SiteContext ownership, resolved classifier typing, Usage identity, contextual creation, containment reuse, nested paths, connectors, rendering, selection/movement/resizing, presentation deletion, save/reload, undo/redo, import/reimport, and migration. It was removed from the invalid Activity Diagram palette context. Visual containment never invents composition or ownership.

## Manual acceptance and limitations

Automated Chromium evidence was visually inspected for the selected workflows. Broader manual testing of every deferred partial type was not executed. All unselected types retain their inventory-derived state and limitations. The pre-existing F-04 and P-05 limitations remain open. Node 22 qualification is not claimed. The repository has no `npm run build` script; `npm run deploy:dry-run` is the production-bundle equivalent. No reports, traces, videos, caches, downloads, or screenshots are committed.
