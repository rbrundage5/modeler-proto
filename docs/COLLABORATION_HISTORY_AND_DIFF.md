# Collaboration History and Diff

The server exposes paginated operation history filtered by semantic target or actor. `elementHistory` presents attributable accepted events. Read-only reconstruction loads the nearest snapshot and replays ordered operations through a selected sequence. Current APIs provide history/time-travel data primitives; full timeline UI, semantic diff attribution, and diagram overlay diff remain later milestones.
