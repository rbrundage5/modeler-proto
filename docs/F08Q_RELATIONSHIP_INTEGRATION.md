# F-08Q relationship integration evidence

F-08Q connects the existing relationship contracts to production without promoting additional relationship families. `Connector` retains its F-07 structural scope; `DelegationConnector` and all other inspected families remain partial.

## Eight-step result

1. The machine-readable manifest reconciles production entry points and deferrals.
2. Selected connector endpoint handles expose stable semantic and presentation identities plus keyboard-accessible source/target names.
3. Keyboard handle activation enters the same reconnection state as pointer activation; semantic direction is not derived from geometry.
4. Label, waypoint, and routing mutations are accepted by the main granular dispatcher.
5. Canvas waypoint creation now dispatches a stable waypoint operation rather than replacing the project.
6. Project normalization runs conservative relationship-presentation migration; missing semantic identities remain unresolved and recoverable.
7. Deterministic integration tests cover dispatcher replay and unresolved migration.
8. Automated checks provide the evidence below; broad manual acceptance remains not executed.

## Qualification limits

Node 22 and broad manual acceptance remain open release gates. Browser reconnection, deletion, containment drag-and-drop, import/reimport, accessibility, and visual qualification must not be claimed unless their maintained commands pass. No formal conformance claim is expanded.
