# Collaboration Locking

Locks are short-lived and branch-scoped. Current enforcement covers operation semantic targets and supports acquisition, expiry, release, reconnect-safe guest identity, and visible owner information. Lock keys include branch and resource. Property, endpoint, batch acquisition, renewal, administrative release, and lock-order UX are the next locking milestone.
