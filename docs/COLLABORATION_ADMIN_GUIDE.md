# Collaboration Administrator Guide

Use authenticated identity headers in production, inspect Worker observability, run deployment dry-runs, and retain project exports/backups. The first connected identity in a development guest room becomes owner; do not use that mode for governed production data. Operation, revision, snapshot, and audit tables are append-oriented. Do not delete history to restore: reconstruct a revision and create a new recovery revision.
