import {test,expect} from './fixtures.mjs';
test('final BDD surface has complete tracked palettes, deliberate notation, and no browser errors',async({page})=>{const errors=[];page.on('pageerror',error=>errors.push(error.message));page.on('console',message=>{if(message.type()==='error')errors.push(message.text())});await page.goto('/');await page.evaluate(()=>{const p=SystemsModelerAPI.createBlankProject('Final BDD');p.diagrams=[{id:'bdd',name:'Final BDD',diagramType:'Block Definition Diagram',ownerId:p.root.id,contextId:p.root.id,nodes:[],edges:[]}];p.activeDiagramId='bdd';SystemsModelerAPI.setProject(p)});const tools=page.locator('#palette .palette-tool');await expect(tools.first()).toBeVisible();expect(await tools.count()).toBeGreaterThan(30);for(let i=0;i<await tools.count();i++)await expect(tools.nth(i)).toHaveAttribute('data-support','complete');await expect(page.locator('#canvas .diagram-frame-title')).toContainText('bdd');expect(errors).toEqual([])});
