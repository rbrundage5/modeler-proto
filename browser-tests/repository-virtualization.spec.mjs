import {test,expect} from './fixtures.mjs';

test('repository renderer bounds DOM rows independently of semantic repository size',async({page})=>{await page.goto('/');const result=await page.evaluate(async()=>{const {buildProjectIndex}=await import('/src/model-index.js'),{RepositoryTreeRenderer}=await import('/src/repository-tree-renderer.js');const count=100000,project={root:{id:'root',name:'Root',kind:'Model'},elements:Array.from({length:count},(_,i)=>({id:`e${i}`,name:`Element ${String(i).padStart(6,'0')}`,kind:'Block',ownerId:'root'})),relationships:[],diagrams:[]};buildProjectIndex(project);const container=document.createElement('div');Object.assign(container.style,{height:'560px',overflow:'auto'});document.body.append(container);const renderer=new RepositoryTreeRenderer(container,project,{rowHeight:28,maxRows:80,renderRow:row=>{const div=document.createElement('div');div.textContent=row.element.name;return div}});renderer.render();container.scrollTop=1_500_000;const view=renderer.render(),stats=renderer.stats();return{total:view.totalRows,rendered:view.rendered,domRows:stats.domRows,first:view.rows[0]?.id}});expect(result.total).toBe(100001);expect(result.rendered).toBeLessThanOrEqual(80);expect(result.domRows).toBeLessThanOrEqual(80);expect(result.first).toBeTruthy()});
