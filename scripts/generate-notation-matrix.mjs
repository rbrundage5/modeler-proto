import fs from 'node:fs';import {ELEMENTS,RELATIONSHIPS,DIAGRAMS} from '../public/src/sysml-profile.js';import {ELEMENT_NOTATION,RELATIONSHIP_NOTATION} from '../public/src/notation/registry.js';import {DIAGRAM_FRAME_NOTATION} from '../public/src/notation/diagram-frame-notation.js';
const esc=v=>String(v??'').replaceAll('|','\\|').replaceAll('\n',' ');let out=`# SysML v1 visual notation conformance matrix\n\nBaseline: OMG SysML 1.7 and inherited OMG UML 2.5.1 notation. “Supported” means enabled by the current semantic profile; it does not by itself imply conformance. Statuses are conservative. The repository icons are tool chrome and are outside this matrix.\n\n## Elements\n\n| Model kind | Applicable diagram types | Normative visual form | Required keyword/stereotype | Shape | Border style | Fill policy | Compartments | Labels | Endpoint notation | Arrowhead | Line style | Status | Required correction | Automated test ID | Visual fixture ID | Intentionally unsupported alternative |\n|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|\n`;
for(const [kind,def] of Object.entries(ELEMENTS)){const n=ELEMENT_NOTATION[kind],enabled=n.allowedDiagramTypes.length>0,status=enabled?(n.fallback==='none'?'Partially conformant':'Nonconformant'):'Not applicable',correction=!enabled?'Semantic-only kind; add only with a valid diagram rule.':n.fallback==='none'?'Visual renderer is registered; gallery/manual baseline review remains required.':'Replace documented UML classifier fallback with a specialized renderer before enabling new contexts.';out+=`| ${esc(kind)} | ${esc(n.allowedDiagramTypes.join(', ')||'semantic repository only')} | ${esc(n.shape)} | ${esc(n.keyword?`«${n.keyword}»`:'none')} | ${esc(n.shape)} | solid unless kind-specific | neutral/print-safe | ${esc((n.compartments||[]).join(', ')||'none')} | semantic name and generated feature labels | ${n.boundaryAttachment?'boundary-attached':'not applicable'} | not applicable | not applicable | ${status} | ${esc(correction)} | NOT-REG-001 | ${enabled?`gallery-${esc(n.allowedDiagramTypes[0]).toLowerCase().replaceAll(' ','-')}`:'none'} | Icon alternatives and proprietary tool artwork are unsupported. |\n`}
out+=`\n## Relationships\n\n| Model kind | Applicable diagram types | Normative visual form | Required keyword/stereotype | Shape | Border style | Fill policy | Compartments | Labels | Endpoint notation | Arrowhead | Line style | Status | Required correction | Automated test ID | Visual fixture ID | Intentionally unsupported alternative |\n|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|\n`;
for(const [kind,def] of Object.entries(RELATIONSHIPS)){const n=RELATIONSHIP_NOTATION[kind],enabled=n.allowedDiagramTypes.length>0,status=enabled?'Partially conformant':'Not implemented';out+=`| ${kind} | ${esc(n.allowedDiagramTypes.join(', ')||'none')} | semantic relationship | ${esc(n.keyword?`«${n.keyword}»`:'none')} | routed path | ${n.lineStyle} | none | not applicable | semantic stereotype/name and end labels | source/client to target/supplier | ${n.sourceMarker} → ${n.targetMarker} | ${n.lineStyle} | ${status} | ${enabled?'Registry, semantic direction, and markers are testable; complete end-label visual baselines remain required.':'Not exposed by a diagram palette.'} | NOT-REL-001 / NOT-REL-002 | ${enabled?`gallery-${n.allowedDiagramTypes[0].toLowerCase().replaceAll(' ','-')}`:'none'} | Alternate presentation directions that reverse semantics are unsupported. |\n`}
out+=`\n## Diagram types\n\n| Model kind | Applicable diagram types | Normative visual form | Required keyword/stereotype | Shape | Border style | Fill policy | Compartments | Labels | Endpoint notation | Arrowhead | Line style | Status | Required correction | Automated test ID | Visual fixture ID | Intentionally unsupported alternative |\n|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|\n`;
for(const [kind,def] of Object.entries(DIAGRAMS))out+=`| ${kind} | ${kind} | framed diagram | ${DIAGRAM_FRAME_NOTATION[kind]} | rectangular frame with heading tab | solid | transparent canvas | not applicable | kind, name, explicit context | not applicable | not applicable | not applicable | Partially conformant | Frame, persistence, export, and context are implemented; screenshot baseline requires browser CI. | NOT-GAL-001 / NOT-MIG-001 | gallery-${kind.toLowerCase().replaceAll(' ','-')} | Unframed view remains an explicit presentation preference. |\n`;
fs.writeFileSync('docs/SYSML_V1_NOTATION_CONFORMANCE.md',out);
