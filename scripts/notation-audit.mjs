import fs from 'node:fs';import {DIAGRAMS} from '../public/src/sysml-profile.js';import {ELEMENT_NOTATION,RELATIONSHIP_NOTATION,assertNotationRegistry,notationFor} from '../public/src/notation/registry.js';
const errors=assertNotationRegistry();for(const [diagramType,d] of Object.entries(DIAGRAMS)){for(const kind of d.elements)if(!notationFor(kind,diagramType))errors.push(`${diagramType}: ${kind} lacks renderer`);for(const kind of d.relationships)if(!RELATIONSHIP_NOTATION[kind])errors.push(`${diagramType}: ${kind} lacks relationship notation`)}
const app=fs.readFileSync('public/src/app.js','utf8');if(/(?:textContent|return|fallback)\s*[=:]?\s*['"`]\?['"`]/.test(app))errors.push('question-mark fallback found in canvas renderer');if(new Set(Object.keys(ELEMENT_NOTATION)).size!==Object.keys(ELEMENT_NOTATION).length)errors.push('duplicate element notation key');if(new Set(Object.keys(RELATIONSHIP_NOTATION)).size!==Object.keys(RELATIONSHIP_NOTATION).length)errors.push('duplicate relationship notation key');
if(errors.length){console.error(`Notation audit failed:\n${errors.join('\n')}`);process.exit(1)}console.log(`Notation audit passed: ${Object.keys(ELEMENT_NOTATION).length} elements, ${Object.keys(RELATIONSHIP_NOTATION).length} relationships, ${Object.keys(DIAGRAMS).length} diagrams.`)
